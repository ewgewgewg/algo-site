<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>Posts on Terminal</title>
    <link>/posts/</link>
    <description>Recent content in Posts on Terminal</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>en-us</language>
    <lastBuildDate>Sun, 27 Nov 2022 00:42:50 -0500</lastBuildDate><atom:link href="/posts/index.xml" rel="self" type="application/rss+xml" />
    <item>
      <title>JS Language</title>
      <link>/posts/js-language/</link>
      <pubDate>Sun, 27 Nov 2022 00:42:50 -0500</pubDate>
      
      <guid>/posts/js-language/</guid>
      <description>Summary An overview of JavaScript syntax. If you are a beginner, read this before the JS Algosfile.
Variables, Types, and Comments JavaScript stores information (called values) within let and const variables &amp;ndash; const variables cannot be changed, but let variables can be reassigned later. Variables can contain booleans (true or false), strings (of text), or numbers, or have null or undefined values. Booleans, strings, numbers, null, and undefined are called types, and can be detected by the typeof command.</description>
      <content>&lt;h1 id=&#34;summary&#34;&gt;Summary&lt;/h1&gt;
&lt;p&gt;An overview of JavaScript syntax. If you are a beginner, read this before the &lt;a
  href=&#34;/posts/js-algos&#34;
  
  &gt;JS Algos&lt;/a
&gt;
 file.&lt;/p&gt;
&lt;h1 id=&#34;variables-types-and-comments&#34;&gt;Variables, Types, and Comments&lt;/h1&gt;
&lt;p&gt;JavaScript stores information (called values) within &lt;code&gt;let&lt;/code&gt; and &lt;code&gt;const&lt;/code&gt; variables &amp;ndash; &lt;code&gt;const&lt;/code&gt; variables cannot be changed, but &lt;code&gt;let&lt;/code&gt; variables can be reassigned later. Variables can contain booleans (&lt;code&gt;true&lt;/code&gt; or &lt;code&gt;false&lt;/code&gt;), strings (of text), or numbers, or have &lt;code&gt;null&lt;/code&gt; or &lt;code&gt;undefined&lt;/code&gt; values. Booleans, strings, numbers, null, and undefined are called types, and can be detected by the &lt;code&gt;typeof&lt;/code&gt; command. Typing on a line after &lt;code&gt;//&lt;/code&gt; or on lines between &lt;code&gt;/*&lt;/code&gt; and &lt;code&gt;*/&lt;/code&gt; creates notes.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;849632517&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;849632517&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
const test = true
typeof test // this code results in &amp;#34;boolean&amp;#34;

let secondTest = &amp;#34;word&amp;#34;
// quotes in &amp;#39;&amp;#39; and `` are also allowed

secondTest = 8
// secondTest was just redefined

let thirdTest = null

let fourthTest
// fourthTest is undefined

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;h1 id=&#34;functions-with-math&#34;&gt;Functions With Math&lt;/h1&gt;
&lt;p&gt;Functions process input parameters inside &lt;code&gt;()&lt;/code&gt; and typically return results inside the &lt;code&gt;{}&lt;/code&gt; of the function body. Functions can be assigned to variables or explicitly named as functions. Defined functions can be invoked by naming them and adding &lt;code&gt;()&lt;/code&gt;, which can contain parameter values called arguments. Math can be done with &lt;code&gt;+&lt;/code&gt;, &lt;code&gt;-&lt;/code&gt;, &lt;code&gt;*&lt;/code&gt;, &lt;code&gt;/&lt;/code&gt; (as division), and &lt;code&gt;**&lt;/code&gt; (to raise immediately previous number to an exponent after). These five operators can be coupled with assignment, like in &lt;code&gt;+=&lt;/code&gt;. Parentheses also work as in regular math and &lt;a
  href=&#34;https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Operator_Precedence&#34;
  
  target=&#34;_blank&#34;
  &gt;precedence of operations is respected as described in the MDN Web Docs&lt;/a
&gt;
. &lt;code&gt;Math.floor&lt;/code&gt; can be used like a function to round down, while &lt;code&gt;Math.ceil&lt;/code&gt; can be used to round up. &lt;code&gt;Math.max&lt;/code&gt; and &lt;code&gt;Math.min&lt;/code&gt; find maximums and minimums of input. &lt;code&gt;Math.abs&lt;/code&gt; finds absolute value. &lt;code&gt;Math.pow&lt;/code&gt; takes a base value and then an exponent and puts the first to the power of the second. &lt;code&gt;Math.random&lt;/code&gt; creates an approximately random floating point (decimal) number from 0 to just less than 0 with a uniform distribution.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;532478961&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;532478961&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

const square = (value) =&amp;gt; {

    return value  ** 2

}

function addTwoThenMultiplyByThree (value) {

    return (value &amp;#43; 2) * 3

}

const addThree = (value) =&amp;gt; {
    value &amp;#43;= 3
    return value
}

square(4) // returns 16
addTwoThenMultiplyByThree(1) // returns 9
addThree(5) // returns 8

Math.floor(2.1) // returns 2
Math.ceil(4.9) // returns 5
Math.max(1, 2, 3) // returns 3
Math.min(1, 2, 3) // returns 1
Math.abs(-1) // returns 1
Math.pow(2, 3) // returns 8
Math.random() // returns some random floating point number

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;Because JavaScript tries to change types when appropriate, adding a string to a number, or adding two strings, joins characters as a string. &lt;code&gt;String&lt;/code&gt; and &lt;code&gt;Number&lt;/code&gt; also can take a argument each and transform them as appropriate. You can also test if &lt;code&gt;Number.isInteger&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;276354819&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;276354819&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

const elevenString = &amp;#34;1&amp;#34; &amp;#43; 1 // contains &amp;#34;11&amp;#34;
const twelveString = &amp;#34;1&amp;#34; &amp;#43; &amp;#34;2&amp;#34; // contains &amp;#34;12&amp;#34;

const aString = String(1) // contains &amp;#34;1&amp;#34;
const aNumber = Number(&amp;#34;1&amp;#34;) // contains 1

const integerState = Number.isInteger(aNumber) // contains true
const notAnIntegerState = Number.isInteger(&amp;#34;a&amp;#34;) // contains false

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;Assigment of a variable and math can happen at the same time, like so:&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;891456732&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;891456732&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

let transform = 2

transform &amp;#43;= 2 // transform now contains 4
transform -= 3 // transform now contains 1
transform *= 2 // transform now contains 2
transform /= 2 // transform now contains 1

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;You can set variables to infinity and check if they are infinite.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;643298751&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;643298751&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

const small = 0
isFinite(small) // contains true

const large = Infinity
isFinite(large) // contains false

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;You can also check if a value is not a number. The function will recognize valid numbers in strings.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;695731824&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;695731824&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

isNaN(&amp;#34;a&amp;#34;) // contains true
isNaN(&amp;#34;-5&amp;#34;) // contains false
isNaN(-5) // contains false

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;You can convert a string to a number if you pass in the string and then the base.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;614528793&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;614528793&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

parseInt(&amp;#34;4&amp;#34;, 10) // contains 4
parseInt(&amp;#34;100&amp;#34;, 2) // contains 4

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;Default parameters provide values in functions if no values are passed in.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;571648329&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;571648329&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

const add = (a = 1, b = 2) =&amp;gt; a &amp;#43; b

add() // returns 3
add(2) // returns 4
add(2,3) // returns 5

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;h1 id=&#34;conditionals-and-comparison-operators&#34;&gt;Conditionals and Comparison Operators&lt;/h1&gt;
&lt;p&gt;Code can be optional. Code written in brackets after an &lt;code&gt;if&lt;/code&gt; condition in parentheses only runs if the condition is parentheses is &lt;code&gt;true&lt;/code&gt;. An &lt;code&gt;if&lt;/code&gt; condition can be immediately followed by one or more &lt;code&gt;else if&lt;/code&gt;, also with parentetical activation conditions, and then an &lt;code&gt;else&lt;/code&gt; which serves as a catch-all and thus has no explicit conditions. Typical conditions include &lt;code&gt;===&lt;/code&gt; to check if two values are truly equal, &lt;code&gt;&amp;gt;=&lt;/code&gt;, &lt;code&gt;&amp;lt;=&lt;/code&gt;, &lt;code&gt;&amp;gt;&lt;/code&gt;, and &lt;code&gt;&amp;lt;&lt;/code&gt; as relative comparators, &lt;code&gt;!&lt;/code&gt; as negation, and &lt;code&gt;%&lt;/code&gt;, the modulo operator, which returns what would be the remainder of a division between numbers on either side.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;935278614&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;935278614&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

const singleFizzBuzz = (value) =&amp;gt; {

    if (!(value % 15)){
        return &amp;#34;FizzBuzz&amp;#34;
    } else if (!(value % 3){
        return &amp;#34;Fizz&amp;#34;
    } else if (!(value % 5)){
        return &amp;#34;Buzz
    } else {
        return value
    }

}

singleFizzBuzz(6) // returns &amp;#34;Fizz&amp;#34;

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;The modulo operator can also be coupled with assignment, like &lt;code&gt;%=&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;816972534&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;816972534&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

let number = 10
number %= 3 // stored value in number after this operation is 1

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;Ternaries are short boolean conditionals that help with assignment. A condition is followed by &lt;code&gt;?&lt;/code&gt;, then a value to assign if the condition is true, then &lt;code&gt;:&lt;/code&gt;, then a value to assign if the condition is false.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;736892541&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;736892541&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

const value = &amp;#34;green&amp;#34;
const isValueBlue = value === &amp;#34;blue&amp;#34; ? &amp;#34;blue&amp;#34; : &amp;#34;not blue&amp;#34; // stored variable is &amp;#34;not blue&amp;#34;
const isValueGreen = value === &amp;#34;green&amp;#34; ? &amp;#34;green&amp;#34; : &amp;#34;not green&amp;#34; // stored variable is &amp;#34;green&amp;#34; 

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;h1 id=&#34;boolean-operators&#34;&gt;Boolean Operators&lt;/h1&gt;
&lt;p&gt;&lt;code&gt;||&lt;/code&gt; is or, and &lt;code&gt;&amp;amp;&amp;amp;&lt;/code&gt; is and. &lt;code&gt;&amp;amp;&amp;amp;&lt;/code&gt; has higher priority than &lt;code&gt;||&lt;/code&gt; and will evaluate first. Most content can be used in place of true, while an empty string, zero, null, or undefined all can be used in boolean math as false. Using &lt;code&gt;!!&lt;/code&gt; coerces a value to its boolean equivalant.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;239517468&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;239517468&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

const andTrue = true &amp;amp;&amp;amp; true // stored variable is true
const falseFromOneFalse = false &amp;amp;&amp;amp; true // stored variable is false
const falseFromBoth = false &amp;amp;&amp;amp; false // stored variable is false

const orTrue = true || true // stored variable is true
const trueFromOneTrue = true || false // stored variable is true
const falseNeedsBoth = false || false // stored variable is false

const orderOfOperations = false || true &amp;amp;&amp;amp; false // stored variable is false because &amp;amp;&amp;amp; operates first

const typeChangeDemonstrationTruthy = !!(&amp;#34;a&amp;#34; || 1) // stored variable is true
const typeChangeDemonstrationFalsy = !!(&amp;#34;&amp;#34; || 0 || null || undefined) // stored variable is false

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;Boolean operators can also do conditional assigment.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;524869731&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;524869731&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

let color = &amp;#34;blue&amp;#34;

true &amp;amp;&amp;amp; (color = &amp;#34;red&amp;#34;)

// color is &amp;#34;red&amp;#34;

false &amp;amp;&amp;amp; (color = &amp;#34;blue&amp;#34;)

// color is still &amp;#34;red&amp;#34;

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;h1 id=&#34;loops-increments-and-decrements&#34;&gt;Loops, Increments, and Decrements&lt;/h1&gt;
&lt;p&gt;Two basic types of repeating code blocks are &lt;code&gt;while&lt;/code&gt; loops and &lt;code&gt;for&lt;/code&gt; loops. A &lt;code&gt;while&lt;/code&gt; loop continues until a parenthetical condition is false, and may not even run once (&lt;code&gt;do...while&lt;/code&gt; loops guarantee at least one cycle). Meanwhile, &lt;code&gt;for&lt;/code&gt; loops often three parts in their parenthetical, an initialized variable, a check condition that stops the loop if met, and a step condition that is expected to modify the initalized variable at the end of each loop. Alternatively, &lt;code&gt;for&lt;/code&gt; loops can use a &lt;code&gt;let item of [items]&lt;/code&gt; format in their parenthetical, where &lt;code&gt;[items]&lt;/code&gt; can actually be any predefined iterable object, and &lt;code&gt;item&lt;/code&gt;, which can be named arbitrarily in the loop, increments over everything inside. Behavior inside each loop is contained in &lt;code&gt;{}&lt;/code&gt;, just like with functions. The operators &lt;code&gt;++&lt;/code&gt; and &lt;code&gt;--&lt;/code&gt; increase or decrease a number by 1, respectively. They can come before a number to run before a check.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;248367951&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;248367951&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

let i = 0

while(i &amp;lt; 4){

    i&amp;#43;&amp;#43;

}

// i is 4 here

do {

    i&amp;#43;&amp;#43;

} while (i &amp;lt; 2)

// i is 5 here because the do block runs once even though the condition is not met

for (let j = 0; j &amp;lt; 3; j&amp;#43;&amp;#43;){

    i--

}

// i is 2 here

const series = [3, 6, 9, 12]

for (element of series){
    i &amp;#43;= element
}

// i is 32 here

let num = 0
const go = &amp;#43;&amp;#43;num || 0 // contains 1
const stop = --num || 0 // contains 0

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;The keyword &lt;code&gt;break&lt;/code&gt; stops the closest outer loop, while the keyword &lt;code&gt;continue&lt;/code&gt; skips to the next step of the loop.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;675982134&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;675982134&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

let count = 0

for (let i = 0; i &amp;lt; 6; i&amp;#43;&amp;#43;){
    count&amp;#43;&amp;#43;
    if(count === 3) continue
    count&amp;#43;&amp;#43;
}

// count is 11 here (would be 12 without continue)

while(true){
    count&amp;#43;&amp;#43;
    if (count === 20) break
}

//count is 20 here

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;h1 id=&#34;objects-and-logging&#34;&gt;Objects and Logging&lt;/h1&gt;
&lt;p&gt;Data structures called objects are useful for manipulating data. Regular objects are made to store data pairs: keys that point to values. Special objects called maps and arrays also use keys and values. Maps keys can be any data type, including another object, while regular object keys can only be strings and array keys can only be numbers called indexes (array items always have an order). Sets, another type of object, contain unique values without keys.&lt;/p&gt;
&lt;p&gt;Dot notation is used to invoke methods, which are similar to functions&amp;ndash;&lt;code&gt;set.add&lt;/code&gt; with one parenthetical argument adds a value, and &lt;code&gt;map.set&lt;/code&gt; with two arguments adds a key with its value, while &lt;code&gt;set.has&lt;/code&gt; tests for presence of a parenthetical value, and &lt;code&gt;map.has&lt;/code&gt; tests for presence of a parenthetical key. Meanwhile, &lt;code&gt;map.get&lt;/code&gt; can take a key and return a value. Regular objects use dot or bracket notation to set and look up keys and values, while arrays use bracket notation only. Meanwhile, a special command, &lt;code&gt;console.log&lt;/code&gt; prints data in parenthetical arguments to a nearby terminal (aka command line interface), which is usually what is used to run your program.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;487291635&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;487291635&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

const regularObject = {&amp;#34;car&amp;#34;: 2 }
const map = new Map()
const array = [1,2]
const set = new Set([5,6])

regularObject.tree = 4
set.add(4)
array[2] = 4
map.set(&amp;#34;tree&amp;#34;, 4)

console.log(regularObject.car) // prints 2
console.log(regularObject.tree) // prints 4
console.log(regularObject[&amp;#34;tree&amp;#34;]) // prints 4
console.log(set.has(4)) // prints true
console.log(set.has(5)) // prints true
console.log(array[0]) // prints 1 -- the leftmost location in an array is known as the 0 index
console.log(array[1]) // prints 2
console.log(array[2]) // prints 4
console.log(map.has(&amp;#34;tree&amp;#34;)) // prints true
console.log(map.get(&amp;#34;tree&amp;#34;)) // prints 4

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;&lt;code&gt;Keys&lt;/code&gt;, &lt;code&gt;values&lt;/code&gt;, and keys and values as &lt;code&gt;entries&lt;/code&gt; can be extracted with methods of of prototype term &lt;code&gt;Object&lt;/code&gt;, taking the source object as the argument passed into the method. Object keys can be looped by using &lt;code&gt;for&lt;/code&gt; loops can use a &lt;code&gt;let key in [items]&lt;/code&gt; format in their parenthetical, where &lt;code&gt;[items]&lt;/code&gt; can actually be any predefined iterable object, and &lt;code&gt;key&lt;/code&gt;, which can be named arbitrarily in the loop, increments over every &lt;code&gt;key&lt;/code&gt; inside.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;962137854&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;962137854&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

const sampleObject = {color1: &amp;#34;blue&amp;#34;, color2: &amp;#34;red&amp;#34;}

const keys = Object.keys(sampleObject)
console.log(keys) // prints [&amp;#34;color1&amp;#34;, &amp;#34;color2&amp;#34;]

const values = Object.values(sampleObject)
console.log(values) // prints [&amp;#34;blue&amp;#34;, &amp;#34;red&amp;#34;]

const entries = Object.entries(sampleObject)
console.log(entries) // prints [[&amp;#34;color1&amp;#34;, &amp;#34;blue&amp;#34;], [&amp;#34;color2&amp;#34;, &amp;#34;red&amp;#34;]]

for (let key in sampleObject){
    console.log(key) // prints &amp;#34;color1&amp;#34;, then &amp;#34;color2&amp;#34;
}

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;You can &lt;code&gt;delete&lt;/code&gt; keys in objects.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;627198345&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;627198345&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

const sampleObject = {color1: &amp;#34;blue&amp;#34;, color2: &amp;#34;red&amp;#34;}

delete sampleObject.color1

console.log(sampleObject) // prints {color2: &amp;#34;red&amp;#34;}

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;Maps can be initialized with arrays of arrays of pairs, where each pair is converted into a key and value. They also have additional methods. For example, they have &lt;code&gt;size&lt;/code&gt;, and can &lt;code&gt;delete&lt;/code&gt; values and also iterate through their keys.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;347125986&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;347125986&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

const map = new Map([[0, 1]])
map.set(1,2)
map.set(2,3)
map.set(3,4)
map.set(4,5)

map.delete(4)
map.delete(map.keys().next().value)

console.log(map.size) // prints 3
console.log(map.has(4)) // prints false
console.log(map.has(0)) // prints false
console.log(map.has(1)) // prints true

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;Sets also have &lt;code&gt;size&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;932654718&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;932654718&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

const set = new Set()

console.log(set.size) // prints 0

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;Template literal syntax &amp;ndash; strings created with ` marks that contain variables surrounded by &lt;code&gt;${&lt;/code&gt; and &lt;code&gt;}&lt;/code&gt; &amp;ndash; is useful for generating patterns of strings, which can enhance logging or the creation of objects or functions.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;142386579&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;142386579&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

const variable1 = &amp;#34;A&amp;#34;
const variable2 = &amp;#34;B&amp;#34;
console.log(`variable1: ${variable1}, variable2: ${variable2}`) // prints `variable1: A, variable2: B`

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;h1 id=&#34;methods-of-arrays&#34;&gt;Methods of Arrays&lt;/h1&gt;
&lt;p&gt;Arrays have &lt;code&gt;length&lt;/code&gt;, can &lt;code&gt;push&lt;/code&gt; new values to the lowest empty index, &lt;code&gt;pop&lt;/code&gt; to unfill the greatest filled index and return the popped value, &lt;code&gt;shift&lt;/code&gt; the value at 0 index off and return the shift value (meaning every other value in the array is downshifted one index), or &lt;code&gt;unshift&lt;/code&gt; to move in a new 0 index value and rotate every other value up one index. These &lt;code&gt;push&lt;/code&gt;, &lt;code&gt;pop&lt;/code&gt;, &lt;code&gt;shift&lt;/code&gt;, and &lt;code&gt;unshift&lt;/code&gt; all act on the array they are run from and do not require assignment to a new array.&lt;/p&gt;
&lt;p&gt;Arrays can also transform or &lt;code&gt;map&lt;/code&gt; their values into a new array. The &lt;code&gt;map&lt;/code&gt; callback function, a function used as a method argument, itself contains value and index arguments for every array location. Arrays can &lt;code&gt;sort&lt;/code&gt; their values in place. Explicitly comparing two arguments in a callback lets you &lt;code&gt;sort&lt;/code&gt; numbers, and if the return of the callback is positive the second argument will return first, while if the return of the callback is negative the first argument will return first. A tie leaves original order. Otherwise the default is to sort even numbers as strings. Arrays can also &lt;code&gt;slice&lt;/code&gt; to return only the content starting at the index matching the first argument and continuing to the end of the array, or just before the index matching the second argument. The &lt;code&gt;slice&lt;/code&gt; method will always return a copy even if you slice at the 0 index. Arrays can also test &lt;code&gt;every&lt;/code&gt; value for some condition and return a boolean (&lt;code&gt;true&lt;/code&gt; or &lt;code&gt;false&lt;/code&gt;) depending on if every test is passed.&lt;/p&gt;
&lt;p&gt;The &lt;code&gt;fill&lt;/code&gt; method takes an argument that can be used to fill whatever number of items is passed into a &lt;code&gt;new Array&lt;/code&gt;, or an existing array. &lt;code&gt;fill&lt;/code&gt; acts on the array it is run from and does not need to be assigned to a new variable.&lt;/p&gt;
&lt;p&gt;The &lt;code&gt;reverse&lt;/code&gt; method reverses the elements in an array, and &lt;code&gt;join&lt;/code&gt;, which takes an argument, merges the elements of the array and places the value of the argument between each, all in a single string. Array methods, like all methods, can be chained when the return of one has the method of the next.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;251674983&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;251674983&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

const array = [2,4,6]
console.log(array.length) // prints 3

const popped = array.pop()
console.log(popped) // prints 6

array.push(10)
console.log(array[2]) // prints 10

array.shift()
console.log(array[0]) // prints 4

array.unshift(2)
console.log(array[0]) // prints 2

const array2 = array.map((value, index) =&amp;gt; value &amp;#43; index )
console.log(array2) // prints [2, 5, 12]

array.sort((a, b) =&amp;gt; b - a)
console.log(array) // prints [10, 4, 2]
array.sort((a,b) =&amp;gt; a - b)
console.log(array) // prints [2, 4, 10]
array.sort()
console.log(array) // prints [10, 2, 4] because it alphabetizes numbers this way
console.log([&amp;#34;a&amp;#34;,&amp;#34;c&amp;#34;,&amp;#34;b&amp;#34;].sort()) // prints [&amp;#34;a&amp;#34;, &amp;#34;b&amp;#34;, &amp;#34;c&amp;#34;]

const array3 = array.slice(1)
console.log(array3) // copy starts at index 1 of the former array and prints [4, 10]

const array4 = array.slice(1, 2)
console.log(array4) // copy starts at index 1 and ends just below index 3 of former array, printing [4]

const allArrayValuesGreaterThanOneBoolean = array.every((value) =&amp;gt; value &amp;gt; 1)
console.log(allArrayValuesGreaterThanOneBoolean) // prints true

array.fill(4)
console.log(array) // prints [4, 4, 4]

const array5 = new Array(5).fill(5)
console.log(array5) // prints [5, 5, 5, 5, 5]

const string = array2.reverse().join(&amp;#34;&amp;#34;) // prints &amp;#34;1252&amp;#34;

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;The array method &lt;code&gt;indexOf&lt;/code&gt; finds the first index of a given value, or returns &lt;code&gt;-1&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;142386597&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;142386597&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

const array = [1, 1]
console.log(array.indexOf(1)) // prints 0
console.log(array.indexOf(2)) // prints -1

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;The array method &lt;code&gt;concat&lt;/code&gt; can to the parent array an array argument (flattened into one array), or push value arguments into the parent array. Combinations also possible.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;183524679&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;183524679&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

const array = [1, 2, 3].concat([4, 5])
console.log(array) // prints [1, 2, 3, 4, 5]

const array2 = [1, 2, 3].concat(4, 5)
console.log(array2) // prints [1, 2, 3, 4, 5]

const array3 = [1, 2, 3].concat(4, 5, [6, 7, 8])
console.log(array3) // prints [1, 2, 3, 4, 5, 6, 7, 8]

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;The array method &lt;code&gt;splice&lt;/code&gt; takes an insertion location, a deletion count, and any number of insertion items as arguments. It acts on the array it is run from and does not need to be part of direct assignment. It can also take negative numbers and count from the back. If no second argument is provided, all elements from the given index onwards are removed.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;584193276&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;584193276&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

const array = [1, 2, 3]

array.splice(1, 1)
console.log(array) // prints [1, 3]

array.splice(1, 0, 4)
console.log(array) // prints [1, 4, 3]

array.splice(2, 0, 5, 6)
console.log(array) // prints [1, 4, 5, 6, 3]

array.splice(-2,1)
console.log(array) // prints [1, 4, 5, 3]

array.splice(-1)
console.log(array) // prints [1, 4, 5]

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;The array method &lt;code&gt;reduce&lt;/code&gt; may be tricky. For every step along its parent array, it runs a callback function with two arguments, an accumulator and the current value at the given array position. The accumulator defaults to the first value in the array, or can be set in the second argument of the &lt;code&gt;reduce&lt;/code&gt; method (the first argument is the callback function). The return value of the callback at each step turns into the accumulator for the next step, which is ultimately returned out of the function.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;216497853&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;216497853&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

const array = [1, 2, 3]

const arraySum = array.reduce((accumulator, value) =&amp;gt; accumulator &amp;#43; value)
console.log(arraySum) // prints 6

const arraySumPlusOne = array.reduce((accumulator, value) =&amp;gt; accumulator &amp;#43; value, 1)
console.log(arraySumPlusOne) // prints 7

const arraySumOnKey = array.reduce((accumulator, value) =&amp;gt; {return { sum: accumulator.sum &amp;#43; value }}, { sum: 0 })
console.log(arraySumOnKey) // prints { sum: 6 }

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;The spread operator &lt;code&gt;...&lt;/code&gt; takes an array and spreads its items as elements of the new array.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;731925846&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;731925846&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

const primaryColors = [&amp;#34;red&amp;#34;, &amp;#34;blue&amp;#34;, &amp;#34;yellow&amp;#34;]

const shapes = [&amp;#34;square&amp;#34;, &amp;#34;circle&amp;#34;, &amp;#34;triangle&amp;#34;]

const primaryColorsAndShapes = [...primaryColors, ...shapes]
console.log(primaryColorsAndShapes) // prints [&amp;#34;red&amp;#34;, &amp;#34;blue&amp;#34;, &amp;#34;yellow&amp;#34;, &amp;#34;square&amp;#34;, &amp;#34;circle&amp;#34;, &amp;#34;triangle&amp;#34;]

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;You can also check if a specific value is in an array with the &lt;code&gt;includes&lt;/code&gt; method.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;647192583&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;647192583&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

console.log([&amp;#34;red&amp;#34;,&amp;#34;blue&amp;#34;].includes(&amp;#34;red&amp;#34;)) // prints true

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;The &lt;code&gt;filter&lt;/code&gt; method takes a callback as an argument that itself offers arguments for value, index, and the array itself as it runs its body on each location in the array it is run from. Any value that passes the test appears in a result array.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;174398526&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;174398526&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

const filtered = [1, 2, 3].filter((value, index, array) =&amp;gt; value &amp;gt; array[1] || index === 0)

console.log(filtered) // prints [1, 3]

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;The &lt;code&gt;toString&lt;/code&gt; method turns an array into a string.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;856497231&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;856497231&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

console.log([1, 2].toString()) // prints &amp;#34;1,2&amp;#34;

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;h1 id=&#34;methods-of-strings&#34;&gt;Methods of Strings&lt;/h1&gt;
&lt;p&gt;Strings also have &lt;code&gt;length&lt;/code&gt;, and can return new strings &lt;code&gt;toLowerCase&lt;/code&gt;, &lt;code&gt;toUpperCase&lt;/code&gt;, or after running &lt;code&gt;replace&lt;/code&gt; on their own values. Strings can also find &lt;code&gt;charCodeAt&lt;/code&gt; at an index (leftmost location is the zero index, and character codes are numbers for representing unique characters, like &amp;lsquo;a&amp;rsquo;, or &amp;lsquo;.&amp;rsquo;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;268743159&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;268743159&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

const string = &amp;#34;Hello&amp;#34;
console.log(string.length) // prints 5

const lowerCaseString = string.toLowerCase()
console.log(lowerCaseString) // prints &amp;#34;hello&amp;#34;

const upperCaseString = string.toUpperCase()
console.log(upperCaseString) // prints &amp;#34;HELLO&amp;#34;

const firstReplace = string.replace(&amp;#34;l&amp;#34;,&amp;#34;a&amp;#34;)
console.log(firstReplace) // prints &amp;#34;Healo&amp;#34;

const secondReplace = string.replace(/[0-9a-zA-Z]/g,&amp;#39;&amp;#39;)
console.log(secondReplace) // this regex replaces all numbers and letters with an empty string (note the g), printing &amp;#34;&amp;#34;

const thirdReplace = string.replace(/[^0-9a-zA-Z]/g,&amp;#39;&amp;#39;)
console.log(thirdReplace) // this regex would replace any non-alphanumeric characters with an empty string (note the ^), but since there are none, &amp;#34;Hello&amp;#34; is printed

const lowerCaseACharCode = firstReplace.charCodeAt(2)
console.log(lowerCaseACharCode) // returns number 97 (for &amp;#39;a&amp;#39;)
const upperCaseACharCode = &amp;#34;A&amp;#34;.charCodeAt(0)
console.log(upperCaseACharCode) // returns number 65 (for &amp;#39;A&amp;#39;)

console.log(string) // prints &amp;#34;Hello&amp;#34; as the original string is unmodified

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;The &lt;code&gt;includes&lt;/code&gt; method checks if a string is inside a parent string and returns a boolean.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;623179548&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;623179548&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

const substring1 = &amp;#34;abc&amp;#34;.includes(&amp;#34;ab&amp;#34;)
console.log(substring1) // prints true

const substring2 = &amp;#34;abc&amp;#34;.includes(&amp;#34;ac&amp;#34;)
console.log(substring2) // prints false

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;The &lt;code&gt;trim&lt;/code&gt; method removes any whitespace on either side of the string.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;364178925&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;364178925&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

console.log(&amp;#34;  a  &amp;#34;.trim()) // prints &amp;#34;a&amp;#34;
console.log(&amp;#34;    &amp;#34;.trim()) // prints &amp;#34;&amp;#34;
console.log(&amp;#34;a&amp;#34;) // prints &amp;#34;a&amp;#34;

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;The &lt;code&gt;repeat&lt;/code&gt; method returns the string from which the method is called times the number in the first argument.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;612578394&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;612578394&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

console.log(&amp;#34;abc&amp;#34;.repeat(4)) // prints &amp;#34;abcabcabcabc&amp;#34;

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;&lt;code&gt;String&lt;/code&gt; itself has a &lt;code&gt;fromCharCode&lt;/code&gt; method where &amp;ldquo;a&amp;rdquo; starts at 97, and the other lowercase letters go from there.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;174968325&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;174968325&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

console.log(String.fromCharCode(97)) // prints &amp;#34;a&amp;#34;

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;h1 id=&#34;advanced-objects&#34;&gt;Advanced Objects&lt;/h1&gt;
&lt;p&gt;Optional chaining with &lt;code&gt;?.&lt;/code&gt; prevents an error and returns &lt;code&gt;undefined&lt;/code&gt; if something the code is trying to look inside is &lt;code&gt;undefined&lt;/code&gt; or &lt;code&gt;null&lt;/code&gt;. This is useful if you want to stop a process gracefully if data shape is not as expected, instead of crashing the program.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;941628537&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;941628537&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

const hasDepth = {

    test: {
        test2: true
    }

}

const noDepth = {

    test: {}

}

console.log(hasDepth?.test?.test2) // prints true
console.log(noDepth?.test?.test2) // prints undefined

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;Functions in JavaScript can be used as classes, creating objects with class-defined structures that can store independent data. Assigning a function labeled as &lt;code&gt;function&lt;/code&gt; to a variable and defining substructures inside it under &lt;code&gt;this.&lt;/code&gt; allows for these substructures to change independently in class instances while always starting the same. Using the &lt;code&gt;new&lt;/code&gt; keyword in front of the function variable creates a class instance. Shared methods can be created by assinging them to the prototype of the class definition. These will behave the same across all class instances but reference data specific to the instance.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;351729846&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;351729846&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

const test = function () {

    this.store = []

}

const a = new test
a.store.push(4)

const b = new test

console.log(a.store) // prints [4]

console.log(b.store) // prints []

test.prototype.lengthPlusOne = function () {
return this.store.length &amp;#43; 1
}

console.log(a.lengthPlusOne()) prints 2

console.log(b.lengthPlusOne()) prints 1

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;h1 id=&#34;binary-operators&#34;&gt;Binary Operators&lt;/h1&gt;
&lt;p&gt;In binary, or base 2 math, &lt;code&gt;&amp;amp;&lt;/code&gt; is the bitwise AND, only keeping digits that match, while &lt;code&gt;|&lt;/code&gt; is the bitwise OR, and &lt;code&gt;^&lt;/code&gt; is XOR, placing a 1 in a digit in the combination of two XORed numbers when the parent elements mismatch. Meanwhile &lt;code&gt;&amp;lt;&amp;lt;&lt;/code&gt; adds a number of binary 0 to the low end number on its left equal to the number on its right (&lt;code&gt;&amp;lt;&amp;lt; 1&lt;/code&gt; multiplies by 2), and &lt;code&gt;&amp;gt;&amp;gt;&lt;/code&gt; removes a number of binary digits from the low end of the number of its left equal to the number on its right. All of these can be coupled with assignment, such as &lt;code&gt;&amp;amp;=&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;278941635&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;278941635&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

const binaryA = 2 &amp;amp; 2
console.log(binaryA) // prints 2

const binaryB = 2 | 0
console.log(binaryB) // prints 2

const binaryC = 2 ^ 2
console.log(binaryC) // prints 0

const binaryD = 4 &amp;lt;&amp;lt; 1
console.log(binaryD) // prints 8

const binaryE = 4 &amp;gt;&amp;gt; 1
console.log(binaryE) // prints 2

let binaryF = 1
binaryF &amp;amp;= 1
console.log(binaryF) // prints 1

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


</content>
    </item>
    
    <item>
      <title>JS Algos</title>
      <link>/posts/js-algos/</link>
      <pubDate>Sat, 26 Nov 2022 15:36:56 -0500</pubDate>
      
      <guid>/posts/js-algos/</guid>
      <description>Summary A review of algorithm implementation in JavaScript. Adapted from the Tech Interview Handbookand the LeetCodewebsite. If you are a JavaScript beginner, read JS Languagefirst.
Arrays To efficiently find indexes of two array values that sum to a target, create a map, then iterate linearly. At every index, if target - value is a map key, return the current index, and the index under the map key. Otherwise, add value as a key to the map, pointing to the current index.</description>
      <content>&lt;h1 id=&#34;summary&#34;&gt;Summary&lt;/h1&gt;
&lt;p&gt;A review of algorithm implementation in JavaScript. Adapted from the &lt;a
  href=&#34;https://www.techinterviewhandbook.org/grind75?hours=19&#34;
  
  target=&#34;_blank&#34;
  &gt;Tech Interview Handbook&lt;/a
&gt;
 and the &lt;a
  href=&#34;https://www.leetcode.com&#34;
  
  target=&#34;_blank&#34;
  &gt;LeetCode&lt;/a
&gt;
 website. If you are a JavaScript beginner, read &lt;a
  href=&#34;/posts/js-language&#34;
  
  &gt;JS Language&lt;/a
&gt;
 first.&lt;/p&gt;
&lt;h1 id=&#34;arrays&#34;&gt;Arrays&lt;/h1&gt;
&lt;p&gt;To efficiently find indexes of two array values that sum to a target, create a map, then iterate linearly. At every index, if &lt;code&gt;target - value&lt;/code&gt; is a map key, return the current index, and the index under the map key. Otherwise, add &lt;code&gt;value&lt;/code&gt; as a key to the map, pointing to the current index. Because of action order at each step, the map always contains all previous values for direct lookup.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;517389624&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;517389624&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/two-sum/&#34;
  
  target=&#34;_blank&#34;
  &gt;Two Sum&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof nums === &amp;#34;object&amp;#34; (array of numbers)
// typeof target === &amp;#34;number&amp;#34;

const twoSum = (nums, target) =&amp;gt; {

    const map = new Map()

    for (let i = 0; i &amp;lt; nums.length; i&amp;#43;&amp;#43;){
        if(map.has(target-nums[i])){
            return [i, map.get(target-nums[i])]
        }

        map.set(nums[i],i)
    }

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find 0 or the greatest positive difference between a number in an array and a number that appears earlier in the array, which can be analogized to buying and selling stock, initialize a &lt;code&gt;low&lt;/code&gt; &amp;ldquo;price&amp;rdquo; to the first number in the array, and the &lt;code&gt;best&lt;/code&gt; difference to 0. Then loop through the array. At every step, replace &lt;code&gt;best&lt;/code&gt; if appropriate with the &lt;code&gt;price&lt;/code&gt; at the current index minus &lt;code&gt;low&lt;/code&gt;, then update &lt;code&gt;low&lt;/code&gt; if the current &lt;code&gt;price&lt;/code&gt; is lower.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;139428756&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;139428756&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/best-time-to-buy-and-sell-stock/&#34;
  
  target=&#34;_blank&#34;
  &gt;Best Time to Buy and Sell Stock&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof prices === &amp;#34;object&amp;#34; (array of numbers)

const maxProfit = (prices) =&amp;gt; {

    let low = prices[0]
    let best = 0

    for (let price of prices){
        best = Math.max(best, price-low)
        low = Math.min(low, price)
    }

    return best

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;The &lt;a
  href=&#34;https://en.wikipedia.org/wiki/Boyer%E2%80%93Moore_majority_vote_algorithm&#34;
  
  target=&#34;_blank&#34;
  &gt;Boyer-Moore majority vote algorithm&lt;/a
&gt;
 finds a majority element in an array in linear time when a majority element is guaranteed. After initializing &lt;code&gt;count&lt;/code&gt; as 1 and &lt;code&gt;val&lt;/code&gt; as the value at the 0 index, the rest of the array is looped. If the value at the new index is the same as &lt;code&gt;val&lt;/code&gt;, increase &lt;code&gt;count&lt;/code&gt; by 1, otherwise decrease &lt;code&gt;count&lt;/code&gt; by one. If &lt;code&gt;count&lt;/code&gt; reaches 0, increment the index by 1, set &lt;code&gt;val&lt;/code&gt; to the value at that index, and set &lt;code&gt;count&lt;/code&gt; to 1. After as many pairs of different values are removed as possible, the &lt;code&gt;val&lt;/code&gt; remanining is the majority element.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;716845239&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;716845239&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/majority-element/&#34;
  
  target=&#34;_blank&#34;
  &gt;Majority Element&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof nums === &amp;#34;object&amp;#34; (array of numbers)

const majorityElement = (nums) =&amp;gt; {

    let count = 1
    let val = nums[0]

    for (let i = 1; i &amp;lt; nums.length; i&amp;#43;&amp;#43;){
        if(nums[i] === val){
            count&amp;#43;&amp;#43;
        } else {
            count--
        }

        if(!count){
            i&amp;#43;&amp;#43;
            val = nums[i]
            count = 1
        }
    }

    return val

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To see if a duplicate is in an array in linear time and linear space, loop the array, and at each step check to see if the value being processed is in a set. If it is, return &lt;code&gt;true&lt;/code&gt;, else add the number to the set. Return &lt;code&gt;false&lt;/code&gt; if no pairs are found in the array. Alternatively, sorting in n log(n) time allows an O(1) space solution by stepping through every adjacent pair in the sorted array and returning &lt;code&gt;true&lt;/code&gt; if any pair matches.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;652738194&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;652738194&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/contains-duplicate/&#34;
  
  target=&#34;_blank&#34;
  &gt;Contains Duplicate &amp;ndash; O(n) time, O(n) space&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof nums === &amp;#34;object&amp;#34; (array of numbers)

const containsDuplicate = (nums) =&amp;gt; {

    const seen = new Set()

    for (let num of nums){
        if (seen.has(num)) return true
        seen.add(num)
    }

    return false

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;





  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;956724138&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;956724138&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/contains-duplicate/&#34;
  
  target=&#34;_blank&#34;
  &gt;Contains Duplicate &amp;ndash; O(n log(n)) time, O(1) space&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof nums === &amp;#34;object&amp;#34; (array of numbers)

const containsDuplicate = (nums) =&amp;gt; {

    nums.sort((a,b)=&amp;gt;a-b)

    for (let i = 1; i &amp;lt; nums.length; i&amp;#43;&amp;#43;){
        if (nums[i] === nums[i-1]) return true
    }

    return false

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;An array of objects which each contain &lt;code&gt;start&lt;/code&gt; and &lt;code&gt;end&lt;/code&gt; times for meetings can be sorted by starting times to check for overlapping times. After the sort by starting times, check if any ending time is less than an immediately previous starting time. This returns &lt;code&gt;false&lt;/code&gt;. Else return &lt;code&gt;true&lt;/code&gt;. It is not necessary to sort secondarily by ending times because if any indentical starting times are found, &lt;code&gt;false&lt;/code&gt; will naturally be returned.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;938147562&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;938147562&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/meeting-rooms/&#34;
  
  target=&#34;_blank&#34;
  &gt;Meeting Rooms&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof intervals === &amp;#34;object&amp;#34; (array of objects with start and end numbers)

const canAttendMeetings = (intervals) =&amp;gt; 

    intervals.sort((a,b)=&amp;gt;a.start-b.start)

    for (let i = 1; i &amp;lt; intervals.length; i&amp;#43;&amp;#43;){
        if (intervals[i].start &amp;lt; intervals[i-1].end) return false
    }

    return true

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To move all zeros to the end of an array while preserving the order of nonzero elements, create a &lt;code&gt;nextNonzeroLocation&lt;/code&gt; and assign the 0 index of the array, then iterate through the array with a different pointer. When this second pointer finds a nonzero, swap the value with the value at the &lt;code&gt;nextNonzeroLocation&lt;/code&gt; and increment &lt;code&gt;nextNonzeroLocation&lt;/code&gt; by 1. This will ensure that only 0 numbers will be &amp;ldquo;behind&amp;rdquo; the leading pointer, so steadily adding nonzeroes to the &lt;code&gt;nextNonzeroLocation&lt;/code&gt; will never displace a value that needs its relative order preserved.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;287194653&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;287194653&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/move-zeroes/&#34;
  
  target=&#34;_blank&#34;
  &gt;Move Zeroes&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof nums === &amp;#34;object&amp;#34; (array of numbers)

const moveZeroes = (nums) =&amp;gt; {

        let nextNonZeroLocation = 0

		for(let i=0; i &amp;lt; nums.length; i&amp;#43;&amp;#43;){
			if(nums[i] != 0) {
				[nums[i], nums[nextNonZeroLocation]] = [nums[nextNonZeroLocation], nums[i]]
                nextNonZeroLocation&amp;#43;&amp;#43;
			}
		}

        return nums

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;How to square the numbers in a non-decreasing array but keep them in sorted order? A linear time solution (as opposed to squaring each and sorting the whole list) is possible. An implementation with three pointers is relatively simple: the &lt;code&gt;low&lt;/code&gt; pointer starts at the lowest index of the array, the &lt;code&gt;high&lt;/code&gt; pointer starts at the highest, and an iterator &lt;code&gt;i&lt;/code&gt; moves a loop from the last index of the base array to the first. At every step choose the larger in absolute value of the value at &lt;code&gt;low&lt;/code&gt; and the &lt;code&gt;value&lt;/code&gt; at high, then square it, place it at the &lt;code&gt;i&lt;/code&gt; position, and increment &lt;code&gt;low&lt;/code&gt; or decrement &lt;code&gt;high&lt;/code&gt; depending on which pointer was chosen.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;561827934&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;561827934&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/squares-of-a-sorted-array/&#34;
  
  target=&#34;_blank&#34;
  &gt;Squares of a Sorted Array&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof nums === &amp;#34;object&amp;#34; (array of numbers)

const sortedSquares = (nums) =&amp;gt; {

	const result = []

	let low = 0
    let high = nums.length - 1

	for (let i = nums.length - 1; i &amp;gt; -1; i--) {
		if (Math.abs(nums[low]) &amp;gt;= Math.abs(nums[high])) {
			result[i] = nums[low] ** 2
			low&amp;#43;&amp;#43;
		} else {
			result[i] = nums[high] ** 2
			high--
		}
	}

	return result

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;In an array of intervals that do not overlap, with the intervals themselves represented as arrays of [start, end] numbers, and sorted by start, how to add a new [start, end] by updating, merging, or inserting as appropriate? One method is to increment from the beginning of the array until the end is reached or the end value of the current array interval stops being less than the start value of the interval to be inserted. Then, again while there are array items to iterate, the interval to be inserted has its start and end values updated to be more mimumum and more maximum as appropriate, until the start of the array interval to be processed is larger than the end of the (modified or unmodifed) interval to be inserted.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;254683791&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;254683791&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/insert-interval/&#34;
  
  target=&#34;_blank&#34;
  &gt;Insert Interval&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof intervals === &amp;#34;object&amp;#34; (array of arrays with 2 numbers)
// typeof newInterval === &amp;#34;object&amp;#34; (array with 2 numbers)

const insert = (intervals, newInterval) =&amp;gt; {

    let i = 0

    while (i &amp;lt; intervals.length &amp;amp;&amp;amp; intervals[i][1] &amp;lt; newInterval[0]) {
        i&amp;#43;&amp;#43;
    }

    const sliceMark = i

    while (i &amp;lt; intervals.length &amp;amp;&amp;amp; newInterval[1] &amp;gt;= intervals[i][0]) {
        newInterval[0] = Math.min(intervals[i][0], newInterval[0])
        newInterval[1] = Math.max(intervals[i][1], newInterval[1])
        i&amp;#43;&amp;#43;
    }

    return intervals.slice(0, sliceMark).concat([newInterval]).concat(intervals.slice(i))

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find all unique triplets of numbers in an array that sum to 0, you can first sort the array in ascending order. A loop with three pointers can then help the problem. The &lt;code&gt;i&lt;/code&gt; pointer in the definition of the loop will crawl from the 0 index to two less than the last index, while for every step in the loop, &lt;code&gt;left&lt;/code&gt; and &lt;code&gt;right&lt;/code&gt; pointers can be defined starting at the leftmost and rightmost extremes to the right of the &lt;code&gt;i&lt;/code&gt; pointer. When the value at &lt;code&gt;i&lt;/code&gt; is equal to the value at &lt;code&gt;i-1&lt;/code&gt;, the step in the loop can be skipped (to guard against duplicate triplets), and if the value at &lt;code&gt;i&lt;/code&gt; is ever positive, the loop can break (because the values at the three pointers will be positive). Otherwise, as long as &lt;code&gt;left &amp;lt; right&lt;/code&gt;, check to see if the sum of the values at &lt;code&gt;i&lt;/code&gt;, &lt;code&gt;left&lt;/code&gt;, and &lt;code&gt;right&lt;/code&gt; are 0. If the value is too low, increment &lt;code&gt;left&lt;/code&gt; by 1, and if too high decrement &lt;code&gt;right&lt;/code&gt; by 1. If the sum is 0, push the value, and increment past any duplicate values innerward from &lt;code&gt;left&lt;/code&gt; and &lt;code&gt;right&lt;/code&gt; so both pointers point to new values. Whether or not the sum is 0, check again if &lt;code&gt;left &amp;lt; right&lt;/code&gt; to continue the inner loop.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;592743186&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;592743186&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/3sum/&#34;
  
  target=&#34;_blank&#34;
  &gt;3Sum&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof nums === &amp;#34;object&amp;#34; (array of numbers)

const threeSum = (nums) =&amp;gt; {

    nums.sort((a,b)=&amp;gt;a-b)

    const result = []
    for (let i = 0; i &amp;lt; nums.length - 2; i&amp;#43;&amp;#43;){
        if (nums[i] &amp;gt; 0) break
        if (i &amp;gt; 0 &amp;amp;&amp;amp; nums[i] === nums[i-1]) continue
        let left = i &amp;#43; 1
        let right = nums.length - 1

        while(left &amp;lt; right){
            const sum = nums[i] &amp;#43; nums[left] &amp;#43; nums[right]
            if (sum &amp;lt; 0) left &amp;#43;= 1
            else if (sum &amp;gt; 0) right -= 1
            else {
                result.push([nums[i], nums[left], nums[right]])
                while(left &amp;lt; right &amp;amp;&amp;amp; nums[left] === nums[left&amp;#43;1]) left&amp;#43;&amp;#43;
                while(left &amp;lt; right &amp;amp;&amp;amp; nums[right] === nums[right-1]) right--
                left&amp;#43;&amp;#43;
                right--
            }
        }
    }
    
    return result

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find the product at each array location of every value in the array except the value at that location, a linear time approach with two passes involves computing the rolling product forwards of every value up to that at a given index, and then rolling over the same array backwards, multiplying the value given in the first pass by a &lt;code&gt;backwardsRollingProduct&lt;/code&gt; of every value above the given index. Make sure to update the &lt;code&gt;backwardsRollingProduct&lt;/code&gt; only by multiplying from the original array (though it should be used at every step to update the target value in the final array).&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;263417985&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;263417985&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/product-of-array-except-self/&#34;
  
  target=&#34;_blank&#34;
  &gt;Product of Array Except Self&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

const productExceptSelf = (nums) =&amp;gt; {

    const result = []
    result[0] = 1

    for (let i = 1; i &amp;lt; nums.length; i&amp;#43;&amp;#43;) {
        result[i] = nums[i - 1] * result[i - 1]
    }

    let backwardsRollingProduct = 1
    for (let i = nums.length - 1; i &amp;gt; -1; i--) {
        result[i] *= backwardsRollingProduct
        backwardsRollingProduct *= nums[i]
    }

    return result

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;From an array of integers, how to return all unique combinations to a given sum? After creating a &lt;code&gt;results&lt;/code&gt; array, this problem can be solved in a exhaustive recursive manner. You can use helper function &lt;code&gt;go&lt;/code&gt; with parameters of remaining array items (starting with all of them), a remaining &lt;code&gt;target&lt;/code&gt; (starting with the original target), and an array with a possible valid combination summing to the &lt;code&gt;target&lt;/code&gt; (starting as an empty array). Inside the helper function, if &lt;code&gt;target&lt;/code&gt; is negative, return immediately, and if &lt;code&gt;target&lt;/code&gt; is 0, push the third parameter&amp;rsquo;s combination into the &lt;code&gt;results&lt;/code&gt; array before returning. Else, loop the candidates in the first parameter, running a new &lt;code&gt;go&lt;/code&gt; call at each step with the first parameter slicing the candidates at the index of the loop, the second parameter reducing &lt;code&gt;target&lt;/code&gt; by the value at the index in the loop, and third parameter adding the current value to the possible valid combination. This method can use a given number as many times as is helpful.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;458139276&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;458139276&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/combination-sum/&#34;
  
  target=&#34;_blank&#34;
  &gt;Combination Sum&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof candidates === &amp;#34;object&amp;#34; (array of numbers)
// typeof target === &amp;#34;number&amp;#34;

combinationSum = (candidates, target) =&amp;gt; {
    
    const results = []

    const go = (remainingCandidates, remainingTarget, possibleCombination) =&amp;gt; {
        if (remainingTarget &amp;lt; 0) return
        if (remainingTarget === 0){
            results.push(possibleCombination)
            return
        }
        for(let i = 0; i &amp;lt; remainingCandidates.length; i&amp;#43;&amp;#43;){
            go(remainingCandidates.slice(i),remainingTarget-remainingCandidates[i],[...possibleCombination,remainingCandidates[i]])
        }
    }
    
    go(candidates,target,[])
    return results

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;How to merge intervals with two numbers like &lt;code&gt;[start, end]&lt;/code&gt; in a larger array of such pairs? If the array has a length, you can sort the array by each &lt;code&gt;start&lt;/code&gt;. Next, you can seed a &lt;code&gt;result&lt;/code&gt; array with the first item in the original array. A loop through the remaining items in the original array has two cases. If the end of the last item in the &lt;code&gt;result&lt;/code&gt; array is greater or equal to the start of the item being processed in the loop, replace the end of the last item in the &lt;code&gt;result&lt;/code&gt; array by the greater of itself or the end of the item being processed in the loop. If there is no overlap, just push the interval being processed by the loop.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;381264975&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;381264975&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/merge-intervals/&#34;
  
  target=&#34;_blank&#34;
  &gt;Merge Intervals&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof intervals === &amp;#34;object&amp;#34; (array of arrays with 2 numbers)
const merge = (intervals) =&amp;gt; {

    if(!intervals.length) return []
    intervals.sort((a,b)=&amp;gt; a[0] - b[0])
    const result = [intervals[0]]

    for(let i = 1; i &amp;lt; intervals.length; i&amp;#43;&amp;#43;){
        if(result[result.length-1][1] &amp;gt;= intervals[i][0]){
            result[result.length-1][1] = Math.max(intervals[i][1],result[result.length-1][1])
        } else {
            result.push(intervals[i])
        } 
    }

    return result

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;In an array where red objects are represented as &lt;code&gt;0&lt;/code&gt;, white objects are represented as &lt;code&gt;1&lt;/code&gt;, and blue objects are represented as &lt;code&gt;2&lt;/code&gt;, how to sort in-place so that red comes before white which comes before blue? Declare a &lt;code&gt;nextRedLocation&lt;/code&gt; pointing to the 0 index and a &lt;code&gt;nextBlueLocation&lt;/code&gt; pointing to the last valid index. Then loop the array. While the loop index is less than or equal to &lt;code&gt;nextBlueLocation&lt;/code&gt;, check if the value at the index is &lt;code&gt;0&lt;/code&gt;. If so, swap the value at the current location with the value at the &lt;code&gt;nextRedLocation&lt;/code&gt; and move up &lt;code&gt;nextRedLocation&lt;/code&gt; by 1. Next, in the same space in the loop, check if the value at the index is &lt;code&gt;2&lt;/code&gt;. If so, swap the value with that at the &lt;code&gt;nextBlueLocation&lt;/code&gt;, decrease &lt;code&gt;nextBlueLocation&lt;/code&gt; by 1, and decrease the loop index by 1 in case the value moved needs transport as a red.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;397584621&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;397584621&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/sort-colors/&#34;
  
  target=&#34;_blank&#34;
  &gt;Sort Colors&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof nums === &amp;#34;object&amp;#34; (array of numbers)

const sortColors = (nums) =&amp;gt; {

    let nextRedLocation = 0
    let nextBlueLocation = nums.length - 1

    for (let i = 0; i &amp;lt;= nextBlueLocation; i&amp;#43;&amp;#43;){

        if (nums[i] === 0){
            [nums[nextRedLocation], nums[i]] = [nums[i],nums[nextRedLocation]]
            nextRedLocation&amp;#43;&amp;#43;
        }
        if (nums[i] === 2){
            [nums[nextBlueLocation], nums[i]] = [nums[i], nums[nextBlueLocation]]
            nextBlueLocation--
            i--
        }
        

    }
    
}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;With a series of container heights arranged along an axis, how can you choose a pair such that the minimum of the two, times the distance between them, is the maximum for all possible pairs? Notice that if you start candidate pairs as the leftmost and rightmost, the only way to increase the possible area contained between them is if an inner height is greater than an outer height. The specific algorithm involves defaulting the &lt;code&gt;best&lt;/code&gt; area to the area provided by the outermost pairs, then repeatedly choosing the smaller of the edge heights, and moving that pointer one step inwards before checking to see if the &lt;code&gt;best&lt;/code&gt; area should update. If the pointers meet, &lt;code&gt;best&lt;/code&gt; can no longer be improved.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;123948756&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;123948756&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/container-with-most-water/&#34;
  
  target=&#34;_blank&#34;
  &gt;Container With Most Water&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof h === &amp;#34;object&amp;#34; (array of numbers)

const maxArea = (h) =&amp;gt; {

    let best = 0
    let left = 0
    let right = h.length - 1
    
    while(left &amp;lt; right){
        const candidate = Math.min(h[left], h[right], h[left]) * (right-left)
        if (candidate &amp;gt; best) best = candidate

        if(h[left] &amp;lt; h[right]) left&amp;#43;&amp;#43;
        else right--
    }
    
    return best

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;Given two arrays of equal length, one that gives the amount of gas added to a car at a given index station, and the other that gives the cost to travel from that index&amp;rsquo;s station to the next index, is it possible to travel to all stations if they lie on a circular route, and if so, from what index should one start? A foundation for a linear solution lies in noticing that at every index the travel cost of gas can be subtracted from added gas to give the &lt;code&gt;changeInGasAtIndex&lt;/code&gt; after one leaves it. If the &lt;code&gt;sum&lt;/code&gt; of all &lt;code&gt;changeInGasAtIndex&lt;/code&gt; is negative, it will not be possible to complete a loop because there is not enough total gas available. Otherwise, to determine what station index to leave from, you can loop the stations and keep a running total of gas in &lt;code&gt;tank&lt;/code&gt;. Start a &lt;code&gt;station&lt;/code&gt; marker at the 0 index. if the &lt;code&gt;tank&lt;/code&gt; every goes negative, set the &lt;code&gt;station&lt;/code&gt; marker to the next index. Return the &lt;code&gt;station&lt;/code&gt; indicated at the end of the loop. Backtracking is not necessary because every sucessful movement from index to index must at a minimum involve 0 gas remaining.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;387249156&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;387249156&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/gas-station/&#34;
  
  target=&#34;_blank&#34;
  &gt;Gas Station&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof gas === &amp;#34;object&amp;#34; (array of numbers)
// typeof cost === &amp;#34;object&amp;#34; (array of numbers)

const canCompleteCircuit = (gas, cost) =&amp;gt; {
    
    let sum = 0
    let tank = 0
    let station = 0

    for (let i = 0; i &amp;lt; gas.length; i&amp;#43;&amp;#43;) {
        const changeInGasAtIndex = gas[i] - cost[i]
        tank &amp;#43;= changeInGasAtIndex
        sum &amp;#43;= changeInGasAtIndex
        if(tank &amp;lt; 0) {
            tank = 0
            station = i &amp;#43; 1
        }
    }
    return sum &amp;gt; -1 ? station : -1
}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To determine the number of longest consecutive elements in an array, notice that you can start a search at every number where the prior number in the array does not exist. To set up for this, create a set with all &lt;code&gt;numbers&lt;/code&gt;, then loop the array. Whenever the original number does not have an immediate prior, step through &lt;code&gt;next&lt;/code&gt; numbers consecutively until &lt;code&gt;next&lt;/code&gt; cannot be found, then update the &lt;code&gt;best&lt;/code&gt; length as appropriate.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;735468912&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;735468912&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/longest-consecutive-sequence/description/&#34;
  
  target=&#34;_blank&#34;
  &gt;Longest Consecutive Sequence&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof nums === &amp;#34;object&amp;#34; (array of numbers)

const longestConsecutive = (nums) =&amp;gt; {

    let best = 0
    const numbers = new Set(nums)
    
    for (let number of nums){
        if (numbers.has(number-1)) continue
        let next = number &amp;#43; 1
        while (numbers.has(next)) next&amp;#43;&amp;#43;
        best = Math.max(best, next-number)
    }

    return best

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To rotate an array to the right by a certain number of steps, note that this means taking that number of elements from the right and adding them to the left of the array. You can do this in-place by determining how many elements must move. This value can be found by dividing the number of steps to right move the array by the length of the array and taking the remainder (modulo operation). Then simply splice off that number of elements from the back of the array and unshift them to the front.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;147295863&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;147295863&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/rotate-array/&#34;
  
  target=&#34;_blank&#34;
  &gt;Rotate Array&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof nums === &amp;#34;object&amp;#34; (array of numbers)
// typeof k === &amp;#34;number&amp;#34;

const rotate = (nums, k) =&amp;gt; {
    k %= nums.length
    nums.unshift(...nums.splice(-k))
}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find the maximum length with equal &lt;code&gt;0&lt;/code&gt; and &lt;code&gt;1&lt;/code&gt; in an array that only contains &lt;code&gt;0&lt;/code&gt; and &lt;code&gt;1&lt;/code&gt;, you can solve the problem linearly by counting &lt;code&gt;extraOnes&lt;/code&gt;. Set that variable to &lt;code&gt;0&lt;/code&gt;, set a &lt;code&gt;best&lt;/code&gt; to &lt;code&gt;0&lt;/code&gt;, and create a &lt;code&gt;map&lt;/code&gt; that indicates there are &lt;code&gt;0&lt;/code&gt; &lt;code&gt;extraOnes&lt;/code&gt; at the &lt;code&gt;-1&lt;/code&gt; index. Then iterate the array. At every step, keep a running tally of &lt;code&gt;extraOnes&lt;/code&gt; by incrementing if a &lt;code&gt;1&lt;/code&gt; is found and decrementing if a &lt;code&gt;0&lt;/code&gt; is found. Then, if that number has not yet been seen in the &lt;code&gt;map&lt;/code&gt;, add it to the &lt;code&gt;map&lt;/code&gt; with a value of the current index. Otherwise, update &lt;code&gt;best&lt;/code&gt; if the index minus the first time that many &lt;code&gt;extraOnes&lt;/code&gt; were found (use the &lt;code&gt;map&lt;/code&gt;) is greater than &lt;code&gt;best&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;148263759&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;148263759&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/contiguous-array/&#34;
  
  target=&#34;_blank&#34;
  &gt;Contiguous Array&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof nums === &amp;#34;object&amp;#34; (array of numbers)

const findMaxLength = (nums) =&amp;gt; {
    
    const map = new Map()
    map.set(0, -1)

    let best = 0
    let extraOnes = 0
    for (let i = 0; i &amp;lt; nums.length; i&amp;#43;&amp;#43;){
        extraOnes &amp;#43;= nums[i] ? 1 : -1

        if (!map.has(extraOnes)){
            map.set(extraOnes, i)
            continue
        }

        best = Math.max(best, i - map.get(extraOnes))
    }
    
    return best

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;Given an array of numbers, how can the number of subarrays that sum a given integer be found? (Subarrays are contiguous.) One linear method is to create a &lt;code&gt;map&lt;/code&gt; to store the number of ways to sum to a given number already seen (starting it with a key of 0 pointing to a value of 1). Then iterate the array, keeping track of the &lt;code&gt;sum&lt;/code&gt; of all numbers so far seen. At every step, if the &lt;code&gt;sum&lt;/code&gt; counting the newly-seen number can be found in the &lt;code&gt;map&lt;/code&gt;, the &lt;code&gt;result&lt;/code&gt; can be increased by the number stored in the &lt;code&gt;map&lt;/code&gt;. Then add a tally for the &lt;code&gt;sum&lt;/code&gt; seen at the current location to the &lt;code&gt;map&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;816542937&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;816542937&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/subarray-sum-equals-k/&#34;
  
  target=&#34;_blank&#34;
  &gt;Subarray Sum Equals K&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof nums === &amp;#34;object&amp;#34; (array of numbers)
// typeof k === &amp;#34;number&amp;#34;

const subarraySum = (nums, k) =&amp;gt; {

  const map = new Map([[0,1]])

  let sum = 0
  let result = 0
  for (number of nums) {
    sum &amp;#43;= number
    if (map.has(sum - k)) result &amp;#43;= map.get(sum - k)
    map.set(sum, map.get(sum) &amp;#43; 1 || 1)
  }
  return result
  
}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;Given an array containing arrays with paired starting and ending times for meetings, how many meeting rooms are needed? A key to understanding this question is realizing that the number of meeting roms needed is the maximum number of overlapping meetings at any time. To solve, you can first handle the edge case where there are no meetings and return &lt;code&gt;0&lt;/code&gt;. Else, split the input array into two sorted arrays, one that sorts the times of the &lt;code&gt;starts&lt;/code&gt; from earliest to latest, and the other that sorts the times of the &lt;code&gt;ends&lt;/code&gt; from earliest to latest. Set a &lt;code&gt;startPointer&lt;/code&gt; and an &lt;code&gt;endPointer&lt;/code&gt; both to 0, then loop as long as &lt;code&gt;startPointer&lt;/code&gt; is less than the length of the input array. Each round, if the &lt;code&gt;starts&lt;/code&gt; value at &lt;code&gt;startPointer&lt;/code&gt; is less than the &lt;code&gt;ends&lt;/code&gt; value at &lt;code&gt;endPointer&lt;/code&gt;, this means a meeting is starting before the active one is ending. In this case, you can increase a &lt;code&gt;result&lt;/code&gt; counter, starting at 0, by 1, and then increment the &lt;code&gt;startPointer&lt;/code&gt; by 1 before the next round of the loop (since the meeting that just started has just been processed). In the other case, if the &lt;code&gt;startPointer&lt;/code&gt; is pointing to a time greater or equal to the &lt;code&gt;endPointer&lt;/code&gt;, this means there is no overlap between whatever meeting is starting and ending, and you can increment both pointers. The reason &lt;code&gt;result&lt;/code&gt; ends at the correct value is because when &lt;code&gt;result&lt;/code&gt; increments, the &lt;code&gt;startPointer&lt;/code&gt; runs ahead by one step, meaning that overlaps of the same degree will not be double-counted, and only if the &lt;code&gt;startPointer&lt;/code&gt; needs to run ahead even further will a new meeting room result be tallied.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;816795423&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;816795423&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/meeting-rooms-ii/&#34;
  
  target=&#34;_blank&#34;
  &gt;Meeting Rooms II&lt;/a
&gt;
 &amp;ndash; code adapted from &lt;a
  href=&#34;https://svalaks.medium.com/leetcode-253-meeting-rooms-ii-ed40f55663a5&#34;
  
  target=&#34;_blank&#34;
  &gt;svalak&amp;rsquo;s Python code and explanation&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof intervals === &amp;#34;object&amp;#34; (array of arrays with 2 numbers)

const minMeetingRooms = (intervals) =&amp;gt; {
  
  if (!intervals?.length) return 0

  const starts = intervals.sort((a,b) =&amp;gt; a[0] - b[0]).map(a =&amp;gt; a[0])
  const ends = intervals.sort((a,b) =&amp;gt; a[1] - b[1]).map(a =&amp;gt; a[1])
  let startPointer = 0
  let endPointer = 0

  let result = 0
  while (startPointer &amp;lt; intervals.length){
      if (starts[startPointer] &amp;lt; ends[endPointer]){
        result&amp;#43;&amp;#43;
      } else {
        endPointer&amp;#43;&amp;#43;
      }
      startPointer&amp;#43;&amp;#43;
  }
  
  return result

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find three numbers that sum closest to a target number in an array, one solution involves first sorting the array from least to greatest. Then, you can set a &lt;code&gt;best&lt;/code&gt; to &lt;code&gt;Infinity&lt;/code&gt;, and create a loop that takes a &lt;code&gt;firstPointer&lt;/code&gt; from the 0 index to two less than the final index. At every step, the &lt;code&gt;secondPointer&lt;/code&gt; starts in each loop as &lt;code&gt;firstPointer+1&lt;/code&gt;, while the &lt;code&gt;thirdPointer&lt;/code&gt; starts at the last index of the array. At this space in the outer loop, while &lt;code&gt;secondPointer&lt;/code&gt; is less than &lt;code&gt;thirdPointer&lt;/code&gt;, find the &lt;code&gt;sum&lt;/code&gt; of the values at the three indices. Return the &lt;code&gt;sum&lt;/code&gt; directly if it matches the target, or update &lt;code&gt;best&lt;/code&gt; if the absolute value of the target less the &lt;code&gt;sum&lt;/code&gt; is a smaller value than the target less &lt;code&gt;best&lt;/code&gt;. Otherwise, move the &lt;code&gt;secondPointer&lt;/code&gt; forwards until it points to a new value or crosses the &lt;code&gt;thirdPointer&lt;/code&gt;, and pull the &lt;code&gt;thirdPointer&lt;/code&gt; back until it does the same. Then check again if &lt;code&gt;secondPointer&lt;/code&gt; is less than &lt;code&gt;thirdPointer&lt;/code&gt;, and if so, go back to the step where you find the sum again. If the outer loop finishes, return &lt;code&gt;best&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;542918637&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;542918637&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/3sum-closest/&#34;
  
  target=&#34;_blank&#34;
  &gt;3Sum Closest&lt;/a
&gt;
 &amp;ndash; code adapted from &lt;a
  href=&#34;https://leetcode.com/problems/3sum-closest/solutions/1467590/javascript-99/5&#34;
  
  target=&#34;_blank&#34;
  &gt;casmith1987 code and explanation&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof nums === &amp;#34;object&amp;#34; (array of numbers)
// typeof target === &amp;#34;number&amp;#34;

const threeSumClosest = (nums, target) =&amp;gt; {

  nums.sort((a,b) =&amp;gt; a-b)
  let best = Infinity
  
  for (let firstPointer = 0; firstPointer &amp;lt; nums.length - 2; firstPointer&amp;#43;&amp;#43;) {
    if (firstPointer &amp;gt; 0 &amp;amp;&amp;amp; nums[firstPointer] === nums[firstPointer - 1]) continue

    let secondPointer = firstPointer &amp;#43; 1
    let thirdPointer = nums.length - 1
    while (secondPointer &amp;lt; thirdPointer) {
      const sum = nums[firstPointer] &amp;#43; nums[secondPointer] &amp;#43; nums[thirdPointer]
	  
      if (sum === target) return sum
      if (Math.abs(target - best) &amp;gt; Math.abs(target - sum)) best = sum
	  
      if (sum &amp;lt; target) {
        secondPointer&amp;#43;&amp;#43;
        while(secondPointer &amp;lt; thirdPointer &amp;amp;&amp;amp; nums[firstPointer] === nums[firstPointer - 1]) secondPointer&amp;#43;&amp;#43;
      } else {
        thirdPointer--
        while (secondPointer &amp;lt; thirdPointer &amp;amp;&amp;amp; nums[thirdPointer] === nums[thirdPointer &amp;#43; 1]) thirdPointer--
      }
    }
  }

  return best

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;Given intervals with starting and ending values, how to find the minimum count of intervals to remove so that the remaining do not overlap? First, you can sort the input array by the ending values. Next, set the &lt;code&gt;previous&lt;/code&gt; interval to the first interval in the input array, and set a &lt;code&gt;result&lt;/code&gt; counter to &lt;code&gt;0&lt;/code&gt;. Then, loop every other interval in the input array. If the ending value of &lt;code&gt;previous&lt;/code&gt; is ever greater than the starting value of the interval being reviewed, increase &lt;code&gt;result&lt;/code&gt; by 1, else update &lt;code&gt;previous&lt;/code&gt; to be the interval being reviewed in the loop. This way, you are minimizing the ending value of &lt;code&gt;previous&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;916287354&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;916287354&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/non-overlapping-intervals/description/&#34;
  
  target=&#34;_blank&#34;
  &gt;Non-overlapping Intervals&lt;/a
&gt;
 &amp;ndash; code modified from &lt;a
  href=&#34;https://leetcode.com/problems/non-overlapping-intervals/solutions/334377/javascript-es6/&#34;
  
  target=&#34;_blank&#34;
  &gt;junhouse&amp;rsquo;s code&lt;/a
&gt;
 close to TORO21&amp;rsquo;s in the comments&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof intervals === &amp;#34;object&amp;#34; (array of arrays with 2 numbers)

const eraseOverlapIntervals = (intervals) =&amp;gt; {

	intervals.sort((a,b) =&amp;gt; a[1] - b[1])
	let previous = intervals[0]
	let result = 0

    for (let i = 1; i &amp;lt; intervals.length; i&amp;#43;&amp;#43;){

			if (previous[1] &amp;gt; intervals[i][0]){
                result&amp;#43;&amp;#43;
            } else {
                previous = intervals[i]
            }
    }

	return result

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;*** Employee Free Time goes here ***&lt;/p&gt;
&lt;p&gt;To find the maximum value of each sliding window of a given length in an array, you can start by declaring an empty &lt;code&gt;result&lt;/code&gt; array, as well as an empty &lt;code&gt;queue&lt;/code&gt; array, and a &lt;code&gt;left&lt;/code&gt; pointer at 0. Then iterate through all values in the array, naming the moving index controlling the loop as the &lt;code&gt;right&lt;/code&gt; pointer. At a given location, while the &lt;code&gt;queue&lt;/code&gt; has a length and the last value indicated by &lt;code&gt;queue&lt;/code&gt; is less than or equal to the value indicated by the &lt;code&gt;right&lt;/code&gt; pointer, you, can pop that last value off the &lt;code&gt;queue&lt;/code&gt;. Then, push the &lt;code&gt;right&lt;/code&gt; index into the &lt;code&gt;queue&lt;/code&gt;. These last two steps guarantee that the leftmost value in the &lt;code&gt;queue&lt;/code&gt; is at a maximum. To manage the front end of the siding window, if the &lt;code&gt;left&lt;/code&gt; index is greater than the index at the front of the &lt;code&gt;queue&lt;/code&gt;, shift off the front element of the &lt;code&gt;queue&lt;/code&gt;. Finally, if the &lt;code&gt;right&lt;/code&gt; index has progressed far enough that the sliding window is at its appropriate length, push the value indicated by the index in the first position at the &lt;code&gt;queue&lt;/code&gt; to &lt;code&gt;result&lt;/code&gt;, and increase the &lt;code&gt;left&lt;/code&gt; index by 1.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;372814596&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;372814596&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/sliding-window-maximum/&#34;
  
  target=&#34;_blank&#34;
  &gt;Non-overlapping Intervals&lt;/a
&gt;
 &amp;ndash; code modified from &lt;a
  href=&#34;https://leetcode.com/problems/sliding-window-maximum/solutions/2060046/javascript/&#34;
  
  target=&#34;_blank&#34;
  &gt;shekhar90&amp;rsquo;s code&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof nums === &amp;#34;object&amp;#34; (array of numbers)

const maxSlidingWindow = (nums, k) =&amp;gt; {
    const result = []
    const queue = []

    let left = 0
    for (let right = 0; right &amp;lt; nums.length; right&amp;#43;&amp;#43;){
        while (queue.length &amp;amp;&amp;amp; nums[queue[queue.length - 1]] &amp;lt;= nums[right]) {
            queue.pop()
        }
        queue.push(right)

        if (left &amp;gt; queue[0]) queue.shift()
        if (right &amp;#43; 1 &amp;gt;= k) {
            result.push(nums[queue[0]])
            left&amp;#43;&amp;#43;
        }
    }

    return result

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;h1 id=&#34;stacks&#34;&gt;Stacks&lt;/h1&gt;
&lt;p&gt;To see if a string containing only &lt;code&gt;(){}[]&lt;/code&gt; characters closes validly, create a dictionary where closing brackets point to opening brackets, and instantiate a stack. Then, iterate over the string, pushing opening brackets to the stack and popping the stack if a closing bracket is found which can close the stack&amp;rsquo;s top item. If a closing bracket is found that does not close the stack&amp;rsquo;s top item, or the stack is empty when a closing bracket is found, or the stack still has length when the string is fully iterated, return &lt;code&gt;false&lt;/code&gt; &amp;ndash; otherwise &lt;code&gt;true&lt;/code&gt; should be returned.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;754329681&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;754329681&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/valid-parentheses/&#34;
  
  target=&#34;_blank&#34;
  &gt;Valid Parentheses&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof s === &amp;#34;string&amp;#34;

const isValid = (s) =&amp;gt; {

    const dictionary = {&amp;#39;)&amp;#39;:&amp;#39;(&amp;#39;,&amp;#39;]&amp;#39;:&amp;#39;[&amp;#39;,&amp;#39;}&amp;#39;:&amp;#39;{&amp;#39;}
    const stack = []

    for (let c of s){
        if(dictionary[c]){
            if(!stack.length ||
            stack[stack.length-1] !== dictionary[c]
            ){
                return false
            }
            stack.pop()
        } else {
            stack.push(c)
        }
    }

    return !stack.length

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;In JavaScript, a stack can be modeled by an array that only adds and removes values from one end, while a queue can be modeled by an array that only adds values on one end and removes them on the other end. To create &lt;code&gt;push&lt;/code&gt;, &lt;code&gt;pop&lt;/code&gt;, &lt;code&gt;peek&lt;/code&gt;, and &lt;code&gt;empty&lt;/code&gt; methods for a constructed queue, create a queue function and within, assign two arrays that will be treated as stacks under the &lt;code&gt;this&lt;/code&gt; keywords. Implement the &lt;code&gt;push&lt;/code&gt; method by adding a &lt;code&gt;push&lt;/code&gt; method to the prototype of the &lt;code&gt;queue&lt;/code&gt; function, which pushes to the first stack. Implement the &lt;code&gt;empty&lt;/code&gt; method similarly by protoype, and by checking if both stacks are empty. The &lt;code&gt;peek&lt;/code&gt; method, meanwhile, helps with &lt;code&gt;pop&lt;/code&gt; &amp;ndash; in &lt;code&gt;peek&lt;/code&gt;, check if the second stack is empty. If so, pop from the first stack (where new values are added) and push to the second stack until the first stack is empty. This naturally brings the first value added to the first stack to the top of the second stack. The &lt;code&gt;pop&lt;/code&gt; method can simply pop the top of the second stack after a &lt;code&gt;peek&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;853217946&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;853217946&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/implement-queue-using-stacks/&#34;
  
  target=&#34;_blank&#34;
  &gt;Implement Queue Using Stacks&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

const MyQueue = function() {

    this.stack1 = []
    this.stack2 = []

};

MyQueue.prototype.push = function(x) {

    this.stack1.push(x)

};

MyQueue.prototype.pop = function() {

    this.peek()
    return this.stack2.pop()

}

MyQueue.prototype.peek = function() {

    if(!this.stack2.length){
        while(this.stack1.length){
            const test = this.stack1.pop()
            this.stack2.push(test)
        }
    }
    return this.stack2[this.stack2.length-1]

}

MyQueue.prototype.empty = function() {

    return !this.stack1.length &amp;amp;&amp;amp; !this.stack2.length

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;In two strings of lowercase letters and #, where # represents a backspace, are the strings equal once backspaces are considered? A solution involves parsing from the end of both strings, towards the front. At each step in the reverse traversal, a string that has not sucessfully reached its forward end checks for the presence of &lt;code&gt;#&lt;/code&gt; at the location, and a &lt;code&gt;backspace&lt;/code&gt; counter started at 0. If &lt;code&gt;#&lt;/code&gt; is present, the &lt;code&gt;backspace&lt;/code&gt; counter increments by 1, otherwise it decrements by 1, and in either case, the string pointer moves one step towards the front of the string. While the string has not reached its forward end, and the &lt;code&gt;backspace&lt;/code&gt; counter is at least 1, or the string pointer is pointed at a &lt;code&gt;#&lt;/code&gt;, the subloop continues. Once the subloop is completed for both strings, any value at the string indexes should match. If they do, both indices can decrement, otherwise the algorithm returns &lt;code&gt;false&lt;/code&gt;. If the parent loop completes, return &lt;code&gt;true&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;219683574&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;219683574&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/backspace-string-compare/&#34;
  
  target=&#34;_blank&#34;
  &gt;Backspace String Compare&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof s === &amp;#34;string&amp;#34;
// typeof t === &amp;#34;string&amp;#34;

const backspaceCompare = (s, t) =&amp;gt; {

    let sIndex = s.length-1
    let tIndex = t.length-1

    while(sIndex &amp;gt; -1 || tIndex &amp;gt; -1){
        sIndex = backwards(s, sIndex)
        tIndex = backwards(t, tIndex)
        
        if(s[sIndex] !== t[tIndex]){
            return false
        } else {
            sIndex--
            tIndex--
        }
    }

    return true

};

const backwards = (string, index) =&amp;gt; {

    let backspace = 0
    while(index &amp;gt; -1 &amp;amp;&amp;amp; (string[index] === &amp;#39;#&amp;#39; || backspace &amp;gt; 0)){
        backspace &amp;#43;= (string[index] === &amp;#39;#&amp;#39; ? 1 : -1)
        index--
    }
    return index

}

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;&lt;a
  href=&#34;https://en.wikipedia.org/wiki/Reverse_Polish_notation&#34;
  
  target=&#34;_blank&#34;
  &gt;Reverse Polish Notation&lt;/a
&gt;
 involves  pushing numerical values to a stack, and otherwise running a detected operator on the two values found behind it in the stack. When values are used they can be popped, and the new value after operation is pushed to the stack as if it was in the original token series (this is a way the notation represents parentheticals).&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;291578463&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;291578463&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/evaluate-reverse-polish-notation/&#34;
  
  target=&#34;_blank&#34;
  &gt;Evaluate Reverse Polish Notation&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof tokens === &amp;#34;object&amp;#34; (array of strings)

const evalRPN = (tokens) =&amp;gt; {

    const stack = []

    for (let token of tokens){
        if(!&amp;#39;&amp;#43;-*/&amp;#39;.includes(token)) stack.push(Number(token))
        else {
            const b = stack.pop()
            const a = stack.pop()

            if(token === &amp;#39;&amp;#43;&amp;#39;){
                stack.push(a &amp;#43; b)
            } else if (token === &amp;#39;-&amp;#39;){
                stack.push(a - b)
            } else if (token === &amp;#39;*&amp;#39;){
                stack.push(a*b)
            } else {
                const test = a/b
                if(test &amp;lt; 0) stack.push(Math.ceil(test))
                else stack.push(Math.floor(test))
            }
        }
    }

    return stack[0]

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To create a stack that can push, pop, find the top value, and find the minimum value, all in constant time, you can seed two empty arrays in the starter function, &lt;code&gt;values&lt;/code&gt; and &lt;code&gt;minimums&lt;/code&gt;. To push values, you can always push a new value to the &lt;code&gt;values&lt;/code&gt; array, but only push to the &lt;code&gt;minimums&lt;/code&gt; array if that array is empty or the value is less than or equal to the previous minimum. To pop, you can always pop from the &lt;code&gt;values&lt;/code&gt; array but only pop from the &lt;code&gt;minimums&lt;/code&gt; array if the last value there matches the value popped from &lt;code&gt;values&lt;/code&gt;. Top just checks the end of the &lt;code&gt;values&lt;/code&gt; array, while getting the minimum just checks the end of the &lt;code&gt;minimums&lt;/code&gt; array.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;712684953&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;712684953&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/min-stack/description/&#34;
  
  target=&#34;_blank&#34;
  &gt;Min Stack&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof val === &amp;#34;number&amp;#34;

const MinStack = function() {
    this.values = []
    this.minimums = []
}

MinStack.prototype.push = function(val) {
    if(!this.minimums.length || val &amp;lt;= this.minimums[this.minimums.length-1]){
        this.minimums.push(val)
    }
    this.values.push(val)
}

MinStack.prototype.pop = function() {
    const test = this.values.pop()
    if(test === this.minimums[this.minimums.length-1]){
        this.minimums.pop()
    }
    return test
}

MinStack.prototype.top = function() {
    return this.values[this.values.length-1]
}

MinStack.prototype.getMin = function() {
    return this.minimums[this.minimums.length-1]
}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To create an array that gives the distance between a given number in an input array and the next greater number, which can be understood as number of days until a greater temperature, you can create a &lt;code&gt;stack&lt;/code&gt; and loop the input array. While the &lt;code&gt;stack&lt;/code&gt; has length and the current value in the input array is greater than the value indicated at end of the stack, you can pop the index of the &lt;code&gt;dayWithTemperatureJustDefeated&lt;/code&gt; from the end of the &lt;code&gt;stack&lt;/code&gt;, assigning to that index in a &lt;code&gt;result&lt;/code&gt; array the positive difference between the currrent loop index and the index of the &lt;code&gt;dayWithTemperatureJustDefeated&lt;/code&gt;. Then you can push the current loop index to the stack. After the traversal of the input array, any indicies that could not be popped should be used to assign a value to the &lt;code&gt;result&lt;/code&gt; array that indicates no later greater value could be found, which in the LeetCode version of the question is 0.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;619348527&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;619348527&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/daily-temperatures/&#34;
  
  target=&#34;_blank&#34;
  &gt;Daily Temperatures&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof temperatures === &amp;#34;object&amp;#34; (array of numbers)

const dailyTemperatures = (temperatures) =&amp;gt; {

    const stack = []
    const result = []

    for (let i = 0; i &amp;lt; temperatures.length; i&amp;#43;&amp;#43;) {
        while (stack.length &amp;amp;&amp;amp; temperatures[i] &amp;gt; temperatures[stack[stack.length - 1]]){
            const dayWithTemperatureJustDefeated = stack.pop()
            result[dayWithTemperatureJustDefeated] = i - dayWithTemperatureJustDefeated 
        }
        stack.push(i)
    }

    for (let index of stack){
        result[index] = 0
    }

    return result

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;If you have an encoded string where numbers followed by brackets multiply the contents of the brackets, how to return the decoded string? One way to solve this involves a &lt;code&gt;repeatStack&lt;/code&gt; and a &lt;code&gt;stringStack&lt;/code&gt;. Both are arrays, and &lt;code&gt;repeatStack&lt;/code&gt; starts empty, while &lt;code&gt;stringStack&lt;/code&gt; starts with an empty string. A &lt;code&gt;latestRepeats&lt;/code&gt; is also declared as an empty string. As the encoded string is looped, the stacks are used to deode. Numbers are added as string characters to the &lt;code&gt;latestRepeats&lt;/code&gt; variable. The character &lt;code&gt;[&lt;/code&gt; requires the &lt;code&gt;latestRepeats&lt;/code&gt; to be turned in to a number type and pushed to the &lt;code&gt;repeatStack&lt;/code&gt;, while &lt;code&gt;latestRepeats&lt;/code&gt; is reset to an empty string, and the &lt;code&gt;stringStack&lt;/code&gt; also take an empty string. The character &lt;code&gt;]&lt;/code&gt; requires both &lt;code&gt;repeatStack&lt;/code&gt; and &lt;code&gt;stringStack&lt;/code&gt; to pop, and the contents of the popped value from &lt;code&gt;stringStack&lt;/code&gt; be added to new last value of &lt;code&gt;stringStack&lt;/code&gt; a number of times equal to the value popped from &lt;code&gt;repeatStack&lt;/code&gt;. For any other character processed from the enoded string, add it to the last element of &lt;code&gt;stringStack&lt;/code&gt;. After the encoded string is fully processed, return what should be the only value in &lt;code&gt;stringStack&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;479216358&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;479216358&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/decode-string/&#34;
  
  target=&#34;_blank&#34;
  &gt;Decode String&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof s === &amp;#34;string&amp;#34;

const decodeString = (s) =&amp;gt; {

    const repeatStack = []
    const stringStack = [&amp;#34;&amp;#34;]
    let latestRepeats = &amp;#34;&amp;#34;

    for (let character of s){
        if (character === &amp;#34;[&amp;#34;){
            repeatStack.push(Number(latestRepeats))
            latestRepeats = &amp;#34;&amp;#34;
            stringStack.push(&amp;#34;&amp;#34;)
        } else if (character === &amp;#34;]&amp;#34;){
            const string = stringStack.pop()
            const repeat = repeatStack.pop()
            stringStack[stringStack.length-1] &amp;#43;= string.repeat(repeat)
        } else if (Number.isInteger(Number(character))) {
            latestRepeats &amp;#43;= character
        } else {
            stringStack[stringStack.length-1] &amp;#43;= character
        }
    }
    return stringStack[0]

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;An array of integers where positive numbers head right, negative numbers head left, and 0 does not exist can be imagined as a field of asteroids. What is the state of the array after collisions, when in collisions smaller numbers are removed, and in cases of equal numbers both are removed? This problem can be solved with a &lt;code&gt;stack&lt;/code&gt;. After the &lt;code&gt;stack&lt;/code&gt; is created, loop the input array. Push every positive element to the array. In cases of negative elements, as long as there is a positive most recent (rightmost) item on the &lt;code&gt;stack&lt;/code&gt;, pop &lt;code&gt;stack&lt;/code&gt; elements until the absolute value of the negative element is smaller or equal to the most recent item on the &lt;code&gt;stack&lt;/code&gt;. If the &amp;lsquo;asteroid&amp;rsquo; moving left is smaller, continue to the next step in the outermost loop, and if the &amp;lsquo;asteroids&amp;rsquo; are equal, pop the most recent item on the stack before continuing to the next step in the outermost loop. Otherwise, the asteroid moving to the left has cleared the rightwards &amp;lsquo;asteroids&amp;rsquo; on the &lt;code&gt;stack&lt;/code&gt;, and should be pushed to it.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;798136245&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;798136245&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/asteroid-collision/&#34;
  
  target=&#34;_blank&#34;
  &gt;Asteroid Collision&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof asteroids === &amp;#34;object&amp;#34; (array of numbers)

const asteroidCollision = (asteroids) =&amp;gt; {

    const stack = []

    for (let asteroid of asteroids){
        if (asteroid &amp;gt; 0){
            stack.push(asteroid)
            continue
        }

        const absAsteroid = Math.abs(asteroid)
        let addAsteroid = true
        while(stack.length &amp;amp;&amp;amp; stack[stack.length-1] &amp;gt; 0){
            if(stack[stack.length-1] &amp;gt; absAsteroid){
                addAsteroid = false
                break
            } else if (stack[stack.length-1] === absAsteroid){
                addAsteroid = false
                stack.pop()
                break
            }

            stack.pop()
        }

        if (addAsteroid) stack.push(asteroid)

    }

    return stack

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To build a string calculator of non-negative integers that handles blank spaces, &lt;code&gt;+&lt;/code&gt;,&lt;code&gt;-&lt;/code&gt;,&lt;code&gt;*&lt;/code&gt;, and &lt;code&gt;/&lt;/code&gt;, while truncating any &lt;code&gt;/&lt;/code&gt; operations towards 0, you can note that two operators will act immediately &amp;ndash; &lt;code&gt;*&lt;/code&gt; and &lt;code&gt;/&lt;/code&gt;. Meanwhile, &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; may not act immediately depending on whether they are surrounded by higher-priority operators. In order to calculate, an empty &lt;code&gt;stack&lt;/code&gt; can be created, as well as a &lt;code&gt;value&lt;/code&gt; array to collect an active number. A &lt;code&gt;sign&lt;/code&gt; variable can also be started at &lt;code&gt;+&lt;/code&gt;, and the input string can be iterated. In the iteration, empty spaces can be skipped, and numbers can be pushed to the &lt;code&gt;value&lt;/code&gt; array. If a sign is detected at the location in the string, this means it is time for the previous &lt;code&gt;sign&lt;/code&gt; to be processed. If the saved &lt;code&gt;sign&lt;/code&gt; is &lt;code&gt;+&lt;/code&gt;, push the number form of the joined &lt;code&gt;value&lt;/code&gt; to the &lt;code&gt;stack&lt;/code&gt;, and if the saved &lt;code&gt;sign&lt;/code&gt; is &lt;code&gt;-&lt;/code&gt;, push the negative of the numerified joined form of the &lt;code&gt;value&lt;/code&gt; to the &lt;code&gt;stack&lt;/code&gt;. If the saved sign is &lt;code&gt;*&lt;/code&gt;, this means the top item in the &lt;code&gt;stack&lt;/code&gt; can also be processed. Pop it, multiply by the numerified joined form of the &lt;code&gt;value&lt;/code&gt;, and push the result back to the &lt;code&gt;stack&lt;/code&gt;. The process for division is similar &amp;ndash; the difference is to divide and truncate towards 0 before pushing back to the &lt;code&gt;stack&lt;/code&gt;. Finally at the location, if a &lt;code&gt;sign&lt;/code&gt; was detected, update the &lt;code&gt;sign&lt;/code&gt; variable to store the &lt;code&gt;sign&lt;/code&gt; at the current location, and replace &lt;code&gt;value&lt;/code&gt; with an empty array. At the end of the string, evaluate the last &lt;code&gt;sign&lt;/code&gt; and &lt;code&gt;value&lt;/code&gt; per the above rules, then sum all the entries in &lt;code&gt;stack&lt;/code&gt; and return this. (There may be multiple entries left at the end in the &lt;code&gt;stack&lt;/code&gt; because &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; signs do not consolidate items in the &lt;code&gt;stack&lt;/code&gt;.)&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;396254187&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;396254187&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/basic-calculator-ii/&#34;
  
  target=&#34;_blank&#34;
  &gt;Basic Calculator II&lt;/a
&gt;
 &amp;ndash; adapted from &lt;a
  href=&#34;https://leetcode.com/problems/basic-calculator-ii/solutions/345603/JavaScript-stack/&#34;
  
  target=&#34;_blank&#34;
  &gt;haleyysz&amp;rsquo;s code&lt;/a
&gt;
 by way of &lt;a
  href=&#34;https://leetcode.com/problems/basic-calculator-ii/solutions/597084/javascript-stack-o-n/&#34;
  
  target=&#34;_blank&#34;
  &gt;fbecker11&amp;rsquo;s code and explanation&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof s === &amp;#34;string&amp;#34;

const calculate = (s) =&amp;gt; {

  const stack = []
  let value = []
  let sign = &amp;#34;&amp;#43;&amp;#34;
  for(let i = 0; i &amp;lt;= s.length; i&amp;#43;&amp;#43;){    
    const viewing = s[i]

    if(viewing === &amp;#34; &amp;#34;){
        continue
    }

    if(!isNaN(viewing)){
        value.push(viewing)
        continue
    }
    const valueAsNumber = Number(value.join(&amp;#34;&amp;#34;))

    if (sign === &amp;#34;&amp;#43;&amp;#34;){
        stack.push(valueAsNumber)
    } else if (sign === &amp;#34;-&amp;#34;){
        stack.push(-valueAsNumber)
    } else if (sign === &amp;#34;*&amp;#34;){
        stack.push(stack.pop()*valueAsNumber)
    } else if (sign ===&amp;#34;/&amp;#34;){
        const untruncatedDivision = stack.pop()/valueAsNumber
        const step = untruncatedDivision &amp;gt; 0 ? Math.floor(untruncatedDivision) : Math.ceil(untruncatedDivision)
        stack.push(step)
    }

      sign = viewing
      value = []
  }
  return stack.reduce((a,b)=&amp;gt; a &amp;#43; b)

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;Given an array of heights that can be thought to exist at the indicies given in the array, what is the volume of &amp;ldquo;rain water&amp;rdquo; falling straight down that can fill this structure without spilling? To solve this problem, you can first compute a &lt;code&gt;peakIndex&lt;/code&gt; of any maximum value in the array. The amount of &amp;ldquo;water&amp;rdquo; added in any index location can never be greater than the value at the &lt;code&gt;peakIndex&lt;/code&gt;. Next, start a &lt;code&gt;result&lt;/code&gt; counter at &lt;code&gt;0&lt;/code&gt;, and a &lt;code&gt;leftHeight&lt;/code&gt; at &lt;code&gt;0&lt;/code&gt;, and move through the input array until you reach the &lt;code&gt;peakIndex&lt;/code&gt;. At every location, if the height at the location is less than &lt;code&gt;leftHeight&lt;/code&gt;, add the positive difference to &lt;code&gt;result&lt;/code&gt;. Else, update the &lt;code&gt;leftHeight&lt;/code&gt; to the value at the current location. Once &lt;code&gt;peakIndex&lt;/code&gt; is reached, repeat this process from the other end of the array with a &lt;code&gt;rightHeight&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;657324189&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;657324189&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/trapping-rain-water/&#34;
  
  target=&#34;_blank&#34;
  &gt;Trapping Rain Water&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof height === &amp;#34;object&amp;#34; (array of numbers)

const trap = (height) =&amp;gt; {

    const peakIndex = height.indexOf(Math.max(...height))
    let result = 0

    let leftHeight = 0
    for(let i = 0; i &amp;lt; peakIndex; i&amp;#43;&amp;#43;){
        if(height[i] &amp;lt; leftHeight) result &amp;#43;= leftHeight - height[i]
        else leftHeight = height[i]
    }

    let rightHeight = 0
    for(let i = height.length-1; i &amp;gt; peakIndex; i--){
        if(height[i] &amp;lt; rightHeight) result &amp;#43;= rightHeight - height[i]
        else rightHeight = height[i]   
    }

    return result

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To create a calculator from a string that handles &lt;code&gt;+&lt;/code&gt;, &lt;code&gt;-&lt;/code&gt;, and parentheses, you can use recursion. On the outermost call of the main function, create an &lt;code&gt;endParenthesisLookupFromStart&lt;/code&gt; that gives keys for open parentheses indices, each paired with the value of the linked close parentheis index. This and the input string are passed to every layer, along with &lt;code&gt;start&lt;/code&gt; and &lt;code&gt;end&lt;/code&gt; indices that begin in the outermost call as the first and last indices of characters in the input string. Within the body of the main function, create an empty &lt;code&gt;nums&lt;/code&gt; array, and a &lt;code&gt;sign&lt;/code&gt; variable intially set to &lt;code&gt;1&lt;/code&gt;. Then move along characters in the input string from the given &lt;code&gt;start&lt;/code&gt; index to the &lt;code&gt;end&lt;/code&gt;. Skip any place spaces. If a &lt;code&gt;(&lt;/code&gt; is found, run the parent function recursively on the part of the string within that parenthesis and its pair, then push the &lt;code&gt;sectionResult&lt;/code&gt; times the &lt;code&gt;sign&lt;/code&gt; to &lt;code&gt;nums&lt;/code&gt;. If a &lt;code&gt;+&lt;/code&gt; is found, update the &lt;code&gt;sign&lt;/code&gt; to the number &lt;code&gt;1&lt;/code&gt;, and if a &lt;code&gt;-&lt;/code&gt; is found, update the sign to the number &lt;code&gt;-1&lt;/code&gt;. If the character being iterated in the loop itself is a number, collect all numbers immediately to the left to get the full number, and then push this &lt;code&gt;sectionResult&lt;/code&gt; to &lt;code&gt;nums&lt;/code&gt;. Make sure to update the iterating index. When the loop is done, return the sum of all values in &lt;code&gt;nums&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;843691572&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;843691572&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/basic-calculator/&#34;
  
  target=&#34;_blank&#34;
  &gt;Basic Calculator&lt;/a
&gt;
 &amp;ndash; code modified from &lt;a
  href=&#34;https://leetcode.com/problems/basic-calculator/solutions/321991/javascript-solution/&#34;
  
  target=&#34;_blank&#34;
  &gt;bobwei&amp;rsquo;s code&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof s === &amp;#34;string&amp;#34;

const calculate = (s, start = 0, end = s.length - 1, endParenthesisLookupFromStart = findParentheses(s)) =&amp;gt; {
  const nums = []
  let sign = 1
  for (let i = start; i &amp;lt;= end; i&amp;#43;&amp;#43;) {
    if (s[i] === &amp;#34; &amp;#34;) continue

    if (s[i] === &amp;#34;(&amp;#34;) {
      const sectionResult = calculate(s, i &amp;#43; 1, endParenthesisLookupFromStart[i] - 1, endParenthesisLookupFromStart)
      nums.push(sign * sectionResult)
      i = endParenthesisLookupFromStart[i]
    } else if (s[i] === &amp;#34;&amp;#43;&amp;#34;) {
      sign = 1;
    } else if (s[i] === &amp;#34;-&amp;#34;) {
      sign = -1;
    } else if (!isNaN(s[i])) {
      const str = parseNum(s, i)
      const sectionResult = Number(str)
      nums.push(sign * sectionResult)
      i &amp;#43;= str.length - 1
    }
  }
  return nums.reduce((a, b) =&amp;gt; a &amp;#43; b)
}

const findParentheses = (s) =&amp;gt; {
  const endParenthesisLookupFromStart = {}
  const stack = []
  for (let i = 0; i &amp;lt; s.length; i&amp;#43;&amp;#43;) {
    if (s[i] === &amp;#34;(&amp;#34;) {
      stack.push(i)
    } else if (s[i] === &amp;#39;)&amp;#39;) {
      const left = stack.pop()
      endParenthesisLookupFromStart[left] = i
    }
  }
  return endParenthesisLookupFromStart
}

const parseNum = (s, start) =&amp;gt; {
  let i = start;
  let numberAsString = &amp;#34;&amp;#34;
  while (!isNaN(s[i])) {
    numberAsString &amp;#43;= s[i]
    i &amp;#43;= 1
  }
  return numberAsString
}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find the largest rectangle in a histogram, with the x-coordinate represented by the index of an input array and the height represented by the value at that index, you can solve the problem with a &lt;code&gt;stack&lt;/code&gt;. Loop the input array, and at every step consider if the &lt;code&gt;stack&lt;/code&gt; has a length and, if so, if the the value in the pair on top of the &lt;code&gt;stack&lt;/code&gt; representing the height at some earlier location is greater than the height at the current location. Meeting this condition means that the maximum height of any rectangle starting at the current location and looking backwards is clamped to the height at the current location, which represents a ceiling. As long as a height stored on top of the &lt;code&gt;stack&lt;/code&gt; is greater than a height at the current index, pop the top of the &lt;code&gt;stack&lt;/code&gt;, which should contain a &lt;code&gt;height&lt;/code&gt; and a &lt;code&gt;location&lt;/code&gt; from where that &lt;code&gt;height&lt;/code&gt; begins. Every time a pop happens, update a &lt;code&gt;result&lt;/code&gt; starting at &lt;code&gt;0&lt;/code&gt; if the &lt;code&gt;height&lt;/code&gt; in the popped value times the difference between the index and the popped location is larger than &lt;code&gt;result&lt;/code&gt;. Then update a &lt;code&gt;heightStartingLocation&lt;/code&gt; variable started at the original index to the index of the popped value. Once the popping is done (or never started), push to the &lt;code&gt;stack&lt;/code&gt; a pair of numbers &amp;ndash; the height at the current index, and the &lt;code&gt;heightStartingLocation&lt;/code&gt;. This indicates where you can pretend the height began from, as all taller heights were popped off the &lt;code&gt;stack&lt;/code&gt; (and can contribute to a rectangle that is at least as tall as the present height). Finally, after the input array is iterated, pop stack items one by one and update the &lt;code&gt;result&lt;/code&gt; if any &lt;code&gt;height&lt;/code&gt; times the difference between the number of heights and the &lt;code&gt;location&lt;/code&gt; just popped is ever greater than &lt;code&gt;result&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;793214856&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;793214856&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/largest-rectangle-in-histogram/&#34;
  
  target=&#34;_blank&#34;
  &gt;Largest Rectangle in Histogram&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof heights === &amp;#34;object&amp;#34; (array of numbers)

const largestRectangleArea = (heights) =&amp;gt; {

    const stack = []
    let result = 0
    
    for(let i = 0; i &amp;lt; heights.length; i&amp;#43;&amp;#43;){
        let heightStartingLocation = i
        while(stack.length &amp;amp;&amp;amp; stack[stack.length-1][0] &amp;gt; heights[i]){
            const [height, location] = stack.pop()
            const candidateArea = height*(i-location)
            if (candidateArea &amp;gt; result) result = candidateArea
            heightStartingLocation = location
        }
        stack.push([heights[i], heightStartingLocation])
    }
    while(stack.length){
        const [height, location] = stack.pop()
        const candidateArea = height*(heights.length-location)
        if (candidateArea &amp;gt; result) result = candidateArea
    }
    return result

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To create a data structure that lets you push elements and pop the most frequent element, with ties being broken by most recent element added, you can set a function to a variable and set an empty array under &lt;code&gt;this&lt;/code&gt;, &lt;code&gt;elementsByFrequency&lt;/code&gt;, as well as an empty object under &lt;code&gt;this&lt;/code&gt;, &lt;code&gt;elementCounts&lt;/code&gt;. For the push method under the &lt;code&gt;prototype&lt;/code&gt;, increment the input in &lt;code&gt;elementCounts&lt;/code&gt; by 1, or create an entry as 1. If the count is greater than the length of &lt;code&gt;elementsByFrequency&lt;/code&gt;, indicating a count not seen, push an array to &lt;code&gt;elementsByFrequency&lt;/code&gt; containing the only the input. Else, push the input to the location in &lt;code&gt;elementsByFrequency&lt;/code&gt; equal to the count minus 1 (so that the 0 index of &lt;code&gt;elementsByFrequency&lt;/code&gt; stores counts of 1). For the pop method under the &lt;code&gt;prototype&lt;/code&gt;, identify the &lt;code&gt;mostCommonElements&lt;/code&gt; of &lt;code&gt;elementsByFrequency&lt;/code&gt; (the rightmost array). Pop a value from this array to return, but before you do, pop &lt;code&gt;elementsByFrequency&lt;/code&gt; if the &lt;code&gt;mostCommonElements&lt;/code&gt; location is now empty. Also decrease the count of the element popped by 1 in &lt;code&gt;elementCounts&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;138247569&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;138247569&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/maximum-frequency-stack/&#34;
  
  target=&#34;_blank&#34;
  &gt;Maximum Frequency Stack&lt;/a
&gt;
 &amp;ndash; code modified from &lt;a
  href=&#34;https://leetcode.com/problems/maximum-frequency-stack/solutions/264189/javascript-multiply-stack-solution/&#34;
  
  target=&#34;_blank&#34;
  &gt;zengxinhai&amp;rsquo;s code&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

const FreqStack = function() {
    this.elementsByFrequency = []
    this.elementCounts = {}
}

FreqStack.prototype.push = function(x) {

    this.elementCounts[x] = (this.elementCounts[x] || 0) &amp;#43; 1

    if (this.elementsByFrequency.length &amp;lt; this.elementCounts[x]){
        this.elementsByFrequency.push([x])
    } else {
        this.elementsByFrequency[this.elementCounts[x]-1].push(x)
    }
}

FreqStack.prototype.pop = function() {
    const mostCommonElements = this.elementsByFrequency[this.elementsByFrequency.length - 1]
    const result = mostCommonElements.pop()
    if (!mostCommonElements.length) {
        this.elementsByFrequency.pop()
    }
    this.elementCounts[result]--
    return result
}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find the longest set of valid parentheses in a string containing only parentheses, you can iterate the input string, and push a 0 to a &lt;code&gt;stack&lt;/code&gt; starting with a 0 if an &lt;code&gt;(&lt;/code&gt; is seen. Otherwise, if the &lt;code&gt;stack&lt;/code&gt; length is greater than 1, pop the back item off the &lt;code&gt;stack&lt;/code&gt; and add the value of the pop plus 2 to the value of the item before that, then update a &lt;code&gt;result&lt;/code&gt; starting at 0 to be the best of itself or the value of the item just updated. If the &lt;code&gt;stack&lt;/code&gt; length is only 1 and you see a &lt;code&gt;)&lt;/code&gt;, indicating there is no valid &lt;code&gt;(&lt;/code&gt; in the &lt;code&gt;stack&lt;/code&gt; to connect to the &lt;code&gt;)&lt;/code&gt; you are visiting, set the only location in the &lt;code&gt;stack&lt;/code&gt; to 0 to indicate that a parentheses set ending at this point is misformed and cannot contribute to the maximum length of a parentheses set starting later.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;718369245&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;718369245&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/longest-valid-parentheses/&#34;
  
  target=&#34;_blank&#34;
  &gt;Longest Valid Parentheses&lt;/a
&gt;
 &amp;ndash; code modified from &lt;a
  href=&#34;https://leetcode.com/problems/longest-valid-parentheses/solutions/142210/javascript-solution/&#34;
  
  target=&#34;_blank&#34;
  &gt;rabbitmeow&amp;rsquo;s code&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof s === &amp;#34;string&amp;#34;

const longestValidParentheses = (s) =&amp;gt; {
    
	let result = 0
	const stack = [0]
    for (let character of s){
        if (character === &amp;#39;(&amp;#39;) {
			stack.push(0)
            continue
		}

        if (stack.length &amp;gt; 1) {
            stack[stack.length - 2] &amp;#43;= stack.pop() &amp;#43; 2
            result = Math.max(result, stack[stack.length - 1])
            continue
        }

        stack[0] = 0
    }

	return result

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;h1 id=&#34;linked-lists&#34;&gt;Linked Lists&lt;/h1&gt;
&lt;p&gt;LeetCode implements a singly-linked list like this:&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;295418736&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;295418736&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

function ListNode(val, next) {

    this.val = (val===undefined ? 0 : val)
    this.next = (next===undefined ? null : next)

}
&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;&amp;hellip;or sometimes this:&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;169543782&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;169543782&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

function ListNode(val) {

    this.val = val
    this.next = null

}
&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To merge two linked lists, declare an empty &lt;code&gt;link&lt;/code&gt; node, and create an extra pointer to this node. While neither list is empty, select the minimum value from the heads of either list, append this to the pointer, then redefine both the pointer and the head of the list with that minimum value their respective &lt;code&gt;.next&lt;/code&gt; values. Once one list is empty, append whichever list was not emptied to the pointer, and use &lt;code&gt;link.next&lt;/code&gt; as the return value.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;697453821&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;697453821&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/merge-two-sorted-lists/&#34;
  
  target=&#34;_blank&#34;
  &gt;Merge Two Sorted Lists&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof list1 === &amp;#34;object&amp;#34; (linked list)
// typeof list2 === &amp;#34;object&amp;#34; (linked list)

const mergeTwoLists = (list1, list2) =&amp;gt; {

    const link = new ListNode()
    let pointer = link

    while(list1 &amp;amp;&amp;amp; list2){
        if(list1.val &amp;lt; list2.val){
            pointer.next = list1
            list1 = list1.next
        } else {
            pointer.next = list2
            list2 = list2.next
        }
        pointer = pointer.next
    }

    if(list1){
        pointer.next = list1
    } else {
        pointer.next = list2
    }

    return link.next

}
&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;Cycles in linked lists can be detected by setting a &lt;code&gt;slow&lt;/code&gt; pointer to the &lt;code&gt;.next&lt;/code&gt; of the head, if available, and setting a &lt;code&gt;fast&lt;/code&gt; pointer to the &lt;code&gt;.next.next&lt;/code&gt; of the head. At each step in a while loop attempt to advance the &lt;code&gt;slow&lt;/code&gt; pointer by 1 step and the &lt;code&gt;fast&lt;/code&gt; pointer by 2. Then check for equality of the pointers. If the pointers point to exactly the same reference, they will return &lt;code&gt;true&lt;/code&gt;, which signals a cycle, and the &lt;code&gt;fast&lt;/code&gt; pointer overtaking the &lt;code&gt;slow&lt;/code&gt;. If the &lt;code&gt;fast&lt;/code&gt; pointer reaches an empty reference before this happens, don&amp;rsquo;t be in the loop and return &lt;code&gt;false&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;812764359&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;812764359&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/linked-list-cycle/&#34;
  
  target=&#34;_blank&#34;
  &gt;Linked List Cycle&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof head === &amp;#34;object&amp;#34; (linked list)

const hasCycle = (head) =&amp;gt; {

    let slow = head?.next
    let fast = head?.next?.next

    while(fast){
        if (slow === fast) return true
        slow = slow?.next
        fast = fast?.next?.next
    }

    return false

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;&lt;span id=&#34;reverse-linked-list&#34; class=&#34;inline-anchor&#34;&gt;&lt;/span&gt;
To reverse a linked list, create a null &lt;code&gt;link&lt;/code&gt;, then perform the following steps while the original linked list still has a filled &lt;code&gt;head&lt;/code&gt; node:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;1) Save the `head` of the original linked list as `next`.
2) Assign `link` to `head.next` (`link` builds the leading part of the original linked list in reversed order).
3) Assign `head` to `link` (head now points through `.next` to the original `link`).
4) Assign the saved `next` to `head`.
&lt;/code&gt;&lt;/pre&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;652317849&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;652317849&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/reverse-linked-list/&#34;
  
  target=&#34;_blank&#34;
  &gt;Reverse Linked List&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof head === &amp;#34;object&amp;#34; (linked list)

const reverseList = (head) =&amp;gt; {

    let link = null

    while(head){
        const next = head.next
        head.next = link
        link = head
        head = next
    }

    return link

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;&lt;span id=&#34;middle-of-the-linked-list&#34; class=&#34;inline-anchor&#34;&gt;&lt;/span&gt;
To find the middle node in a linked list (second of the two if the number of nodes is even), create &lt;code&gt;fast&lt;/code&gt; and &lt;code&gt;slow&lt;/code&gt; pointers and start them at &lt;code&gt;head&lt;/code&gt;. While there is a &lt;code&gt;fast.next&lt;/code&gt;, assign &lt;code&gt;slow&lt;/code&gt; to the next node and &lt;code&gt;next&lt;/code&gt; two nodes forward along the list. Once this loop resolves &lt;code&gt;slow&lt;/code&gt; should point to the middle node by the problem definition.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;517648932&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;517648932&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/middle-of-the-linked-list/&#34;
  
  target=&#34;_blank&#34;
  &gt;Middle of the Linked List&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof head === &amp;#34;object&amp;#34; (linked list)

const middleNode = (head) =&amp;gt; {
    
   let slow = head
   let fast = head

   while(fast?.next){
         slow = slow.next
         fast = fast.next?.next
   }

   return slow

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find if a linked list contains a palindrome of values, it is necessary to compare values from either end of the list, stepping towards the middle. The head of the list is readily available, but to get to the other end effectively, a traversal is needed. Perform a traversal similar to &lt;a
  href=&#34;/posts/js-algos#middle-of-the-linked-list&#34;
  
  &gt;Middle of the Linked List&lt;/a
&gt;
, just above, which should put a slow pointer at approximately the middle node. The suffix can the be reversed in a pattern similar to &lt;a
  href=&#34;/posts/js-algos#reverse-linked-list&#34;
  
  &gt;Reverse Linked List&lt;/a
&gt;
. Finally, both the original head and the suffix can be stepped through together to see if each pair of numbers matches. Make sure your implementation accounts for the possibility of an odd number of nodes and does not check the middle node against anything.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;492638175&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;492638175&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/palindrome-linked-list/&#34;
  
  target=&#34;_blank&#34;
  &gt;Palindrome Linked List&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof head === &amp;#34;object&amp;#34; (linked list)

const isPalindrome = (head) =&amp;gt; {  
  if (!head.next) return true
  
  let slow = head
  let fast = head
  
  while (fast) {
    slow = slow.next
    fast = fast.next?.next
  }
  
  slow = reverse(slow)
  
  while (slow) {
    if (slow.val !== head.val) return false
    slow = slow.next
    head = head.next
  }
  
  return true
};

const reverse = (head) =&amp;gt; {
  let link = null
  
  while (head) {
    const next = head.next
    head.next = link
    link = head
    head = next
  }
  
  return link
}

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To create a cache that evicts the least recent key when a new key is added that goes over a cache &lt;code&gt;capacity&lt;/code&gt;, we can take advantage of the properties of the JavaScript Map object. One strategy involves declaring a function and assigning it to a variable. When this function is instanced, for example, with  &lt;code&gt;new LRUCache(4)&lt;/code&gt;, assign the argument &lt;code&gt;capacity&lt;/code&gt; to &lt;code&gt;this&lt;/code&gt; (&lt;code&gt;capacity&lt;/code&gt; is 4 in the example) and also assign a new Map under &lt;code&gt;this&lt;/code&gt;. For &lt;code&gt;get&lt;/code&gt;, if there is no key, you can return &lt;code&gt;-1&lt;/code&gt;, else get, delete, and reset the key, and return the value. This makes it so that Map&amp;rsquo;s internal ordering notes this key as most recent. The &lt;code&gt;set&lt;/code&gt; method is similar &amp;ndash; simply &lt;code&gt;delete&lt;/code&gt; the key if it already exists, &lt;code&gt;set&lt;/code&gt; it again, and then use JavaScript map functions to &lt;code&gt;delete&lt;/code&gt; the first key added to the Map. Note that this and many other solutions currently (December 2022) often time out on LeetCode but do periodically pass. The primary difference between this and &lt;a
  href=&#34;https://leetcode.com/problems/lru-cache/solutions/1157689/javascript-map/&#34;
  
  target=&#34;_blank&#34;
  &gt;Cjamm&amp;rsquo;s solution&lt;/a
&gt;
 is that this code will always attempt to delete keys in either method.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;296458137&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;296458137&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/lru-cache/&#34;
  
  target=&#34;_blank&#34;
  &gt;LRU Cache&lt;/a
&gt;
 &amp;ndash; closely adapted from &lt;a
  href=&#34;https://leetcode.com/problems/lru-cache/solutions/1157689/javascript-map/&#34;
  
  target=&#34;_blank&#34;
  &gt;Cjamm&amp;rsquo;s code&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof capacity === &amp;#34;number&amp;#34;

const LRUCache = function(capacity) {
    this.capacity = capacity
    this.map = new Map()
}

LRUCache.prototype.get = function(key) {
    if(!this.map.has(key)) return -1
    const value = this.map.get(key)
    this.map.delete(key)
    this.map.set(key, value)
    return value
}

LRUCache.prototype.put = function(key, value) {
    this.map.delete(key)
    this.map.set(key, value)
    if (this.map.size &amp;gt; this.capacity) this.map.delete(this.map.keys().next().value)
}

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To remove the nth node from the end of a list, you can count the &lt;code&gt;length&lt;/code&gt; of the list by iterating, then subtract from &lt;code&gt;length&lt;/code&gt; the nth item to be removed to find the number of the item to be removed starting from the front. Finally, with a new pointer from the front of the list, iterate until one step before the item to be removed, and from that item, replace the &lt;code&gt;.next&lt;/code&gt; with &lt;code&gt;.next.next&lt;/code&gt; if available, otherwise &lt;code&gt;null&lt;/code&gt;. If the item to be removed is at the &lt;code&gt;head&lt;/code&gt; of the list, simply return the &lt;code&gt;.next&lt;/code&gt; from the &lt;code&gt;head&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;817356924&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;817356924&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/remove-nth-node-from-end-of-list/&#34;
  
  target=&#34;_blank&#34;
  &gt;Remove Nth Node From End of List&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof head === &amp;#34;object&amp;#34; (linked list)
// typeof n === &amp;#34;number&amp;#34;

const removeNthFromEnd = (head, n) =&amp;gt; {

  let counterCopy = head

  let length = 0
  while (counterCopy) {
    length&amp;#43;&amp;#43;
    counterCopy = counterCopy.next
  }
  
  let countToRemove = length - n
  if (!countToRemove) return head.next

  let removerCopy = head
  while (countToRemove &amp;gt; 1) {
    countToRemove--
    removerCopy = removerCopy.next
  }

  removerCopy.next = removerCopy.next?.next || null
  return head

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;How to swap every two adjacent nodes in a linked list? You can run a function recursively &amp;ndash; if there is no &lt;code&gt;.next&lt;/code&gt; simply return &lt;code&gt;head&lt;/code&gt;, otherwise save the head as &lt;code&gt;nodeToBeMovedBack&lt;/code&gt;, save the &lt;code&gt;.next.next&lt;/code&gt; as the &lt;code&gt;recursiveCallArgument&lt;/code&gt;, move the &lt;code&gt;head&lt;/code&gt; to &lt;code&gt;head.next&lt;/code&gt;, and take the &lt;code&gt;nodeToBeMovedBack&lt;/code&gt; as the new &lt;code&gt;head.next&lt;/code&gt;. Finally, assign the recursive call to &lt;code&gt;head.next.next&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;593164728&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;593164728&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/swap-nodes-in-pairs/&#34;
  
  target=&#34;_blank&#34;
  &gt;Swap Nodes in Pairs&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof head === &amp;#34;object&amp;#34; (linked list)

const swapPairs = (head) =&amp;gt; {

    if(head?.next){
        const nodeToBeMovedBack = head
        const recursiveCallArgument = head.next.next
        head = head.next
        head.next = nodeToBeMovedBack
        head.next.next = swapPairs(recursiveCallArgument)
    }

    return head

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To change a linked list so that all odd-indexed elements are followed by all even-indexed elements, if the linked list has a length, you can set three pointers. First, an &lt;code&gt;odd&lt;/code&gt; starting at the &lt;code&gt;head&lt;/code&gt;, second, an &lt;code&gt;even&lt;/code&gt; starting at &lt;code&gt;head.next&lt;/code&gt;, and third, an extra pointer staying at &lt;code&gt;headEven&lt;/code&gt;. While there is an &lt;code&gt;even.next&lt;/code&gt; (aka what should be the next &lt;code&gt;odd&lt;/code&gt;), set the &lt;code&gt;.next&lt;/code&gt; of both &lt;code&gt;odd&lt;/code&gt; and &lt;code&gt;even&lt;/code&gt; to their &lt;code&gt;.next.next&lt;/code&gt; and then set &lt;code&gt;odd&lt;/code&gt; and &lt;code&gt;even&lt;/code&gt; themselves to their &lt;code&gt;.next&lt;/code&gt;. Make sure to do &lt;code&gt;odd&lt;/code&gt; first so the links for &lt;code&gt;even&lt;/code&gt; are not broken. Once there is no more &lt;code&gt;even.next&lt;/code&gt;, set &lt;code&gt;odd.next&lt;/code&gt; to &lt;code&gt;headEven&lt;/code&gt; and retun this.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;142698537&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;142698537&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/odd-even-linked-list/&#34;
  
  target=&#34;_blank&#34;
  &gt;Odd Even Linked List&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof head === &amp;#34;object&amp;#34; (linked list)

const oddEvenList = (head) =&amp;gt; {

    if (head === null) return head
    
    let odd = head
    let even = head.next
    const headEven = even

    while (even?.next) {
        odd.next = odd.next.next
        odd = odd.next
        even.next = even.next.next
        even = even.next
    }
    odd.next = headEven

    return head

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;If two linked lists represent numbers in reversed order, how to add them into a new linked list? You can create a &lt;code&gt;link&lt;/code&gt; pointer to help hold the eventual head, a copy &lt;code&gt;joined&lt;/code&gt; pointer to iterate through the digits of the result, a &lt;code&gt;pointer1&lt;/code&gt; and &lt;code&gt;pointer2&lt;/code&gt; to point to the first node in each input list, and a &lt;code&gt;carry&lt;/code&gt; variable starting at 0 to handle overflow. While &lt;code&gt;pointer1&lt;/code&gt; or &lt;code&gt;pointer2&lt;/code&gt; are not null, add the sum of the values at both, if any, plus &lt;code&gt;carry&lt;/code&gt;, to a &lt;code&gt;sum&lt;/code&gt;. The tens place of this value becomes &lt;code&gt;carry&lt;/code&gt; for the next step, and the ones place is added as the value to the next &lt;code&gt;joined&lt;/code&gt; node. Then &lt;code&gt;pointer1&lt;/code&gt; and &lt;code&gt;pointer2&lt;/code&gt; move to their &lt;code&gt;.next&lt;/code&gt;, if available. At the end of the while loop, if there is still a value in &lt;code&gt;carry&lt;/code&gt;, add it as one final node, and then return the &lt;code&gt;.next&lt;/code&gt; of the original &lt;code&gt;link&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;735628149&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;735628149&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/add-two-numbers/&#34;
  
  target=&#34;_blank&#34;
  &gt;Add Two Numbers&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof l1 === &amp;#34;object&amp;#34; (linked list)
// typeof l2 === &amp;#34;object&amp;#34; (linked list)

const addTwoNumbers = (l1, l2) =&amp;gt; {

    const link = new ListNode()
    let joined = link

    let carry  = 0
    let pointer1 = l1
    let pointer2 = l2

    while(pointer1 || pointer2) {
        const sum = carry &amp;#43; (pointer1?.val || 0) &amp;#43; (pointer2?.val || 0)
        carry = Math.floor(sum / 10)
        joined.next = new ListNode(sum % 10)
        joined = joined.next
        pointer1 = pointer1?.next || null
        pointer2 = pointer2?.next || null
    }

    joined.next = carry ? new ListNode(carry) : null

    return link.next

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;How to return a linked list after sorting its nodes in ascending order? Recursion is a solution. The base case, where the input does not have a &lt;code&gt;.next&lt;/code&gt;, involves returning the input. Else you can use &lt;code&gt;fast&lt;/code&gt; and &lt;code&gt;slow&lt;/code&gt; pointers to find a middle pointer with &lt;code&gt;slow&lt;/code&gt;. Set a &lt;code&gt;breakpoint&lt;/code&gt; at &lt;code&gt;slow&lt;/code&gt; and assign its &lt;code&gt;.next&lt;/code&gt; as the &lt;code&gt;second&lt;/code&gt; half of the series, then make &lt;code&gt;breakpoint&lt;/code&gt;&amp;rsquo;s own &lt;code&gt;.next&lt;/code&gt; null, which separates out the &lt;code&gt;first&lt;/code&gt; half. Make a recursive call for both halves to ensure they are sorted, and then compare the heads of each repeatedly, adding whichever is lower but present to a new series of nodes building off an empty &lt;code&gt;link&lt;/code&gt;. When both heads are exhausted, return &lt;code&gt;link.next&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;912675483&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;912675483&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/sort-list/&#34;
  
  target=&#34;_blank&#34;
  &gt;Sort List&lt;/a
&gt;
 &amp;ndash; code adapted from &lt;a
  href=&#34;https://leetcode.com/problems/sort-list/solutions/496007/javascript-merge-sort/&#34;
  
  target=&#34;_blank&#34;
  &gt;grs&amp;rsquo;s code&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof head === &amp;#34;object&amp;#34; (linked list)

const sortList = (head) =&amp;gt; {
    if (!head?.next) return head
    
    let fast = head
    let slow = head
    while (fast.next?.next) {
        fast = fast.next.next
        slow = slow.next
    }
    
    const first = head
    const breakpoint = slow
    const second = slow.next
    breakpoint.next = null

    let sortedFirst = sortList(first)
    let sortedSecond = sortList(second)
    
    const link = new ListNode()
    let pointer = link
    while (sortedFirst || sortedSecond) {
        if (!sortedSecond || sortedFirst?.val &amp;lt; sortedSecond?.val) {
            pointer.next = sortedFirst
            sortedFirst = sortedFirst.next
        } else {
            pointer.next = sortedSecond
            sortedSecond = sortedSecond.next
        }
        pointer = pointer.next
    }
    pointer.next = null
    
    return link.next

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To reorder a linked list in-place such that the second value would become the old last value, and the fourth value would become the old second-to-last value, you can find the &lt;code&gt;endOfFrontPart&lt;/code&gt; value with fast and slow pointers, then assign its &lt;code&gt;.next&lt;/code&gt; as the leading part of the back half, the first &lt;code&gt;inorderBackNode&lt;/code&gt;. Then you can make &lt;code&gt;endOfFrontPart&lt;/code&gt;&amp;rsquo;s &lt;code&gt;.next&lt;/code&gt; null to pinch off the front of the linked list from the back. The linked list starting with &lt;code&gt;inorderBackNode&lt;/code&gt; can then be &lt;code&gt;reversed&lt;/code&gt;. In the final part of the algorithm, create an empty &lt;code&gt;link&lt;/code&gt; node and a copy of the original linked list head as &lt;code&gt;forward&lt;/code&gt;. While &lt;code&gt;reversed&lt;/code&gt; exists, assign the &lt;code&gt;.next&lt;/code&gt; of &lt;code&gt;link&lt;/code&gt; to &lt;code&gt;forward&lt;/code&gt;, advance both &lt;code&gt;link&lt;/code&gt; and &lt;code&gt;forward&lt;/code&gt; to their &lt;code&gt;.next&lt;/code&gt;, then assign the &lt;code&gt;.next&lt;/code&gt; of &lt;code&gt;link&lt;/code&gt; to &lt;code&gt;reversed&lt;/code&gt; and advance both &lt;code&gt;link&lt;/code&gt; and &lt;code&gt;reversed&lt;/code&gt; to their &lt;code&gt;.next&lt;/code&gt;. Finally, to account for a possible odd number of nodes, assign &lt;code&gt;link.next&lt;/code&gt; to &lt;code&gt;forward&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;526849317&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;526849317&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/reorder-list/&#34;
  
  target=&#34;_blank&#34;
  &gt;Reorder List&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof head === &amp;#34;object&amp;#34; (linked list)

const reorderList = (head) =&amp;gt; {

    let fast = head
    let slow = head
    while(fast?.next){
        fast = fast.next.next
        slow = slow.next
    }

    let endOfFrontPart = slow
    let inorderBackNode = endOfFrontPart.next
    endOfFrontPart.next = null

    let reversed = null
    while (inorderBackNode){
        const next = inorderBackNode.next
        inorderBackNode.next = reversed
        reversed = inorderBackNode
        inorderBackNode = next
    }

    let link = new ListNode()
    let forward = head
    while (reversed){
            link.next = forward
            forward = forward.next
            link = link.next

            link.next = reversed
            reversed = reversed.next
            link = link.next
    }

    link.next = forward

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To rotate a linked list to the right by a certain number of places, you can note that an empty list returns itself. Otherwise, a next step is to count the &lt;code&gt;length&lt;/code&gt; of the list. Update the rotation count to its remainder after dividing by the &lt;code&gt;length&lt;/code&gt;. If the new rotation count is &lt;code&gt;0&lt;/code&gt; or the &lt;code&gt;length&lt;/code&gt;, you can return the original linked list. Otherwise, you can save the &lt;code&gt;last&lt;/code&gt; node, and with the new rotation count, iterate a pointer from the beginning of the list until you are the rotation count of spaces away from the &lt;code&gt;last&lt;/code&gt; node. Save this location as the &lt;code&gt;result&lt;/code&gt;, set the previous node&amp;rsquo;s &lt;code&gt;.next&lt;/code&gt; to &lt;code&gt;null&lt;/code&gt;, and set the &lt;code&gt;last&lt;/code&gt;&amp;rsquo;s &lt;code&gt;.next&lt;/code&gt; to the original head. Then return &lt;code&gt;result&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;963182754&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;963182754&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/rotate-list/&#34;
  
  target=&#34;_blank&#34;
  &gt;Rotate List&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof head === &amp;#34;object&amp;#34; (linked list)
// typeof k === &amp;#34;number&amp;#34;

const rotateRight = (head, k) =&amp;gt; {
  if (!head) return head
  let length = 1
  let pointer = head
  while (pointer.next) {
    length&amp;#43;&amp;#43;
    pointer = pointer.next
  }

  k %= length
  if (!k || k === length) return head

  const last = pointer
  let pointer2 = head

  for (let i = 0; i &amp;lt; length-k-1; i&amp;#43;&amp;#43;){
      pointer2 = pointer2.next
  }

  const result = pointer2.next
  pointer2.next = null
  last.next = head

  return result

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;h1 id=&#34;strings&#34;&gt;Strings&lt;/h1&gt;
&lt;p&gt;To detect if a string is a palindrome, sanitize the string as appropriate (for example, removing spaces and standardizing capitalization), then initialize string &lt;code&gt;start&lt;/code&gt; and &lt;code&gt;end&lt;/code&gt; pointers. While &lt;code&gt;start&lt;/code&gt; is at a lower index than &lt;code&gt;end&lt;/code&gt; check to see if the values at each location match, then move each pointer one index closer to the center. Any mismatch allows the function to immediately return &lt;code&gt;false&lt;/code&gt; while otherwise &lt;code&gt;true&lt;/code&gt; should be the return.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;598436172&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;598436172&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/valid-palindrome/&#34;
  
  target=&#34;_blank&#34;
  &gt;Valid Palindrome&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof s === &amp;#34;string&amp;#34;

const isPalindrome = (s) =&amp;gt; {

    let start = 0
    s = s.replace(/[^0-9a-zA-Z]/g,&amp;#39;&amp;#39;).toLowerCase()
    let end = s.length - 1

    while(start &amp;lt; end){
        if(s[start] !== s[end]) return false
        start&amp;#43;&amp;#43;
        end--
    }

    return true

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;Two strings are valid anagrams of each other if they have exactly the same letter count. A test involves creating an array with zeroed indices for every letter. Loop over one string, incrementing at the appropriate array index as letters are read. Then loop over the second index decrementing at the appropriate array index as letters are read. If the strings are anagrams, the resulting array should be filled with zeroes.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;638471529&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;638471529&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/valid-anagram/&#34;
  
  target=&#34;_blank&#34;
  &gt;Valid Anagram&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof s === &amp;#34;string&amp;#34;
// typeof t === &amp;#34;string&amp;#34;

const isAnagram = (s, t) =&amp;gt; {

    const arr = new Array(26).fill(0)

    for (let i = 0; i &amp;lt; s.length; i&amp;#43;&amp;#43;){
        arr[s.charCodeAt(i)-97]&amp;#43;&amp;#43;
    }

    for (let i = 0; i &amp;lt; t.length; i&amp;#43;&amp;#43;){
        arr[t.charCodeAt(i)-97]--
    }

    return arr.every(a=&amp;gt;!a)

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;The longest palindrome from a case-sensitive string of letters is equivalent to the number of matching pairs plus one, or the length of the string, whichever is shorter. In other versions of the problem, the string may need to be filtered or transformed.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;154789236&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;154789236&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/longest-palindrome/&#34;
  
  target=&#34;_blank&#34;
  &gt;Longest Palindrome&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof s === &amp;#34;string&amp;#34;

const longestPalindrome = (s) =&amp;gt; {

    const upper = new Array(26).fill(0)
    const lower = new Array(26).fill(0)

    for(let i = 0; i &amp;lt; s.length; i&amp;#43;&amp;#43;){
        if(s.charCodeAt(i) &amp;lt; 97){
            upper[s.charCodeAt(i)-65]&amp;#43;&amp;#43;
        } else {
            lower[s.charCodeAt(i)-97]&amp;#43;&amp;#43;
        }
    }

    let pairs = 0
    for (let u of upper){
        pairs &amp;#43;= Math.floor(u/2)
    }
    for (let l of lower){
        pairs &amp;#43;= Math.floor(l/2)
    }

    return Math.min(pairs*2&amp;#43;1, s.length)

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find the longest common prefix in an array of strings, any string can be chosen as the comparator, since the longest common prefix cannot be longer than any arbitrarily chosen string. Cycle through character locations from left to right for every string in the array, starting with the leftmost character in each string. Every time the comparator at a given index matches the character in every other string at that same index, add that character to the &lt;code&gt;prefix&lt;/code&gt; result. As soon as there is a mismatch, or once the double loop ends with no mismatches, return &lt;code&gt;prefix&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;476819532&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;476819532&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/longest-common-prefix/&#34;
  
  target=&#34;_blank&#34;
  &gt;Longest Common Prefix&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof strs === &amp;#34;array of strings&amp;#34;

const longestCommonPrefix = (strs) =&amp;gt; {

  let prefix = []

  for (let i = 0; i &amp;lt; strs[0].length; i&amp;#43;&amp;#43;) {
    for (let j = 1; j &amp;lt; strs.length; j&amp;#43;&amp;#43;) {
      if (strs[0][i] !== strs[j][i]) return prefix.join(&amp;#34;&amp;#34;)
    }

    prefix.push(strs[0][i])
  }

  return prefix.join(&amp;#34;&amp;#34;)

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find the longest substring without repeating characters, you can use two pointers, a leading pointer and a &lt;code&gt;backPointer&lt;/code&gt;, both of which can start at 0. Advance the leading pointer as in a loop, and at each location in the string add the character code of the character to an object of all character codes in the visited string. Then, while a duplicate character code is detected, subtract the character pointed to by the &lt;code&gt;backPointer&lt;/code&gt; from the table, and increment the back pointer. Once the substring between the leading pointer and &lt;code&gt;backPointer&lt;/code&gt; has no repeated characters, use its length to replace a &lt;code&gt;best&lt;/code&gt; string length as needed.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;341869752&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;341869752&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/longest-substring-without-repeating-characters/&#34;
  
  target=&#34;_blank&#34;
  &gt;Longest Substring Without Repeating Characters&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
//typeof s === &amp;#34;string&amp;#34;

const lengthOfLongestSubstring = (s) =&amp;gt; {

    const charCount = new Array(127).fill(0)

    let best = 0
    let backPointer = 0

    for(let i = 0; i &amp;lt; s.length; i&amp;#43;&amp;#43;){
        charCount[s.charCodeAt(i)]&amp;#43;&amp;#43;

        while(charCount[s.charCodeAt(i)] === 2){
            charCount[s.charCodeAt(backPointer)]--
            backPointer&amp;#43;&amp;#43;
        }

        best = Math.max(best,i&amp;#43;1-backPointer)
    }

    return best

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To convert a stringified number to a 32-bit signed integer, you can remove whitespace, parse the number with base 10, return &lt;code&gt;0&lt;/code&gt; if the result is not a number, and clamp the value to the available space if the number is too great or too small.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;672148593&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;672148593&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/string-to-integer-atoi/&#34;
  
  target=&#34;_blank&#34;
  &gt;String to Integer (atoi)&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof str === &amp;#34;string&amp;#34;

const myAtoi = (str) =&amp;gt; {
    
    const integer = parseInt(str.trim(), 10)

    if (isNaN(integer)) {
        return 0
    } else if (integer &amp;gt;= Math.pow(2, 31)) {
        return Math.pow(2, 31) - 1   
    } else if (integer &amp;lt; Math.pow(-2, 31)) {
        return Math.pow(-2, 31)
    } else {
        return integer
    }
}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find the longest palindromic substring of a string, you can simply iterate the string, and for every location make new pointer pairs expand outwards in both directions, continuing if each pointer pair matches and updating the &lt;code&gt;result&lt;/code&gt; string if appropriate. Each location on the string being looped needs two expansions, one that does not test the active index, using the index just before and just after as the first pair (checking odd length strings), and one that uses the active index with the next index as the first pair of the location, and expands outwards from there (checking even length strings). If any index is out of bounds, loops should end as needed.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;742365981&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;742365981&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/longest-palindromic-substring/&#34;
  
  target=&#34;_blank&#34;
  &gt;Longest Palindromic Substring&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof s === &amp;#34;string&amp;#34;

const longestPalindrome = (s) =&amp;gt; {

    let result = s[0]

    for (let i = 0; i &amp;lt; s.length; i&amp;#43;&amp;#43;){

        let left = i - 1
        let right = i &amp;#43; 1
        while(left &amp;gt;= 0 &amp;amp;&amp;amp; right &amp;lt; s.length &amp;amp;&amp;amp; s[left] === s[right]){
            if(right-left&amp;#43;1 &amp;gt; result.length) result = s.slice(left, right&amp;#43;1)
            left--
            right&amp;#43;&amp;#43;
        }

        left = i
        right = i &amp;#43; 1
        while(left &amp;gt;= 0 &amp;amp;&amp;amp; right &amp;lt; s.length &amp;amp;&amp;amp; s[left] === s[right]){
            if(right-left&amp;#43;1 &amp;gt; result.length) result = s.slice(left, right&amp;#43;1)
            left--
            right&amp;#43;&amp;#43;
        }
    }

    return result

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;How to return all start indicies of an anagram of one string in another string? First, if the parent string is shorter than the string that may appear inside as an anagram, you can immediately return an empty array. Then you can create a &lt;code&gt;countMap&lt;/code&gt; of the string to be made into an anagram. Separately, create a count of the number of unique &lt;code&gt;letters&lt;/code&gt; in the potential anagram. The final two variables needed are a &lt;code&gt;result&lt;/code&gt; array and a count of &lt;code&gt;numberOfAnagramLettersFullySeen&lt;/code&gt;. The remainder of the algorithm is a linear pass through the larger parent string. At every new location, if the letter being pointed to is in the &lt;code&gt;countMap&lt;/code&gt;, decrease the value at the appropriate letter in the &lt;code&gt;countMap&lt;/code&gt; by 1. If this &lt;code&gt;newCount&lt;/code&gt; is &lt;code&gt;0&lt;/code&gt;, that means you can increment &lt;code&gt;numberOfAnagramLettersFullySeen&lt;/code&gt; by 1. In the same step in the loop through the parent string, check the location further left by the length of the anagram string, if available &amp;ndash; this location should no longer be part of the examined string. Increase &lt;code&gt;countMap&lt;/code&gt; and decrease &lt;code&gt;numberOfAnagramLettersFullySeen&lt;/code&gt; as needed based on what might have been in the location no longer being considered. As the third and final step for each location in the loop, push the index less the length of the anagram string plus 1 to the &lt;code&gt;results&lt;/code&gt; array if the &lt;code&gt;numberOfAnagramLettersFullySeen&lt;/code&gt; is the same as the number of unique &lt;code&gt;letters&lt;/code&gt;. We can only ever consider strings of the required length.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;753968124&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;753968124&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/find-all-anagrams-in-a-string/&#34;
  
  target=&#34;_blank&#34;
  &gt;Find All Anagrams in a String&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof s === &amp;#34;string&amp;#34;
// typeof p === &amp;#34;string&amp;#34;

const findAnagrams = (s, p) =&amp;gt; {

    if (s.length &amp;lt; p.length) return []

    const countMap = new Map()
    let letters = 0
    for (let character of p){
        if (!countMap.has(character)){
            letters&amp;#43;&amp;#43;
            countMap.set(character, 0)
        } 
        countMap.set(character, countMap.get(character) &amp;#43; 1)
    }

    const results = []
    let numberOfAnagramLettersFullySeen = 0
    
    for (let i = 0; i &amp;lt; s.length; i&amp;#43;&amp;#43;){
        if (countMap.has(s[i])){
            const newCount = countMap.get(s[i]) - 1
            countMap.set(s[i], newCount)
            if (newCount === 0) numberOfAnagramLettersFullySeen&amp;#43;&amp;#43;
        }
        if (i &amp;gt;= p.length &amp;amp;&amp;amp; countMap.has(s[i-p.length])){
            const newCount = countMap.get(s[i-p.length]) &amp;#43; 1
            countMap.set(s[i-p.length], newCount)
            if (newCount === 1) numberOfAnagramLettersFullySeen--
        }
        if (numberOfAnagramLettersFullySeen === letters) results.push(i-p.length&amp;#43;1)
    }
    
    return results
    
}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To group an array&amp;rsquo;s anagrams into arrays within a result array, one method starts with creating a container for the &lt;code&gt;groups&lt;/code&gt;, then looping the strings in the input array. At every step find a &lt;code&gt;standardAnagram&lt;/code&gt; key for the string by turning it into an array, sorting it, and joining it. Add this key to &lt;code&gt;groups&lt;/code&gt; if it does not already exist, then add the original string as a value. Once the loop is complete, return all the array values in &lt;code&gt;groups&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;421853796&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;421853796&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/group-anagrams/&#34;
  
  target=&#34;_blank&#34;
  &gt;Group Anagrams&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof strs === &amp;#34;object&amp;#34; (array of strings)

const groupAnagrams = (strs) =&amp;gt; {

    const groups = {}
    
    for (let str of strs){
        const standardAnagram = str.slice().split(&amp;#39;&amp;#39;).sort().join(&amp;#39;&amp;#39;)
        if (!groups[standardAnagram]) groups[standardAnagram] = []
        groups[standardAnagram].push(str)
    }
    
    return Object.values(groups)

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;Given a string and a count of times you can change a character, what is the longest substring (aka contiguous string inside the parent) that contains all the same letter? One solution involves a &lt;code&gt;counts&lt;/code&gt; object, setting a &lt;code&gt;startIndex&lt;/code&gt; to 0, and a &lt;code&gt;greatestCountSeen&lt;/code&gt; to 0. Loop the input string. At every step, update the &lt;code&gt;counts&lt;/code&gt; object to include the count of the current character. Then for that character&amp;rsquo;s count, udate &lt;code&gt;greatestCountSeen&lt;/code&gt; if needed. Next, if the &lt;code&gt;greatestCountSeen&lt;/code&gt; plus the number of times you can change a character is less than the range from the current index to the &lt;code&gt;startIndex&lt;/code&gt;, inclusive, remove a count from the character at &lt;code&gt;startIndex&lt;/code&gt; and increase &lt;code&gt;startIndex&lt;/code&gt; by 1. Finally, update a &lt;code&gt;best&lt;/code&gt; if the length of the substring between &lt;code&gt;startIndex&lt;/code&gt; and the current index, inclusive, is better. Whenever &lt;code&gt;greatestCountSeen&lt;/code&gt; is updated, you have reached a location with a candidate for the new &lt;code&gt;best&lt;/code&gt;, so you increment &lt;code&gt;startIndex&lt;/code&gt; until you have a valid possible length.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;948267315&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;948267315&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/longest-repeating-character-replacement/&#34;
  
  target=&#34;_blank&#34;
  &gt;Longest Repeating Character Replacement&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof s === &amp;#34;string&amp;#34;
// typeof k === &amp;#34;number&amp;#34;

const characterReplacement = (s, k) =&amp;gt; {
    
    const counts = {}
    let startIndex = 0
    let greatestCountSeen = 0
    let best = 0
    for (let i = 0; i &amp;lt; s.length; i&amp;#43;&amp;#43;){
        counts[s[i]] = counts[s[i]] &amp;#43; 1 || 1

        if(counts[s[i]] &amp;gt; greatestCountSeen) greatestCountSeen = counts[s[i]]

        while(i - startIndex &amp;#43; 1 &amp;gt; k &amp;#43; greatestCountSeen){
            counts[s[startIndex]]--
            startIndex&amp;#43;&amp;#43;
        }

        best = Math.max(best, i - startIndex &amp;#43; 1)

    }
    return best

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find the largest number that can be made out of non-negative integers in an array, you can stringify the numbers and sort them by paired concatenations, such that numbers that when leading the pair make for larger joined numbers, end up on the left.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;164237859&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;164237859&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/largest-number/&#34;
  
  target=&#34;_blank&#34;
  &gt;Largest Number&lt;/a
&gt;
 &amp;ndash; code slightly modified from &lt;a
  href=&#34;https://leetcode.com/problems/largest-number/solutions/864518/javascript-typescript-solution/&#34;
  
  target=&#34;_blank&#34;
  &gt;eddyhdzg&amp;rsquo;s code and explanation&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof nums === &amp;#34;object&amp;#34; (array of numbers)

const largestNumber = (nums) =&amp;gt; {
    const result = nums
    .map(value =&amp;gt; String(value))
    .sort((a, b) =&amp;gt;  a.concat(b) &amp;gt; b.concat(a) ? -1 : 1)
    .join(&amp;#34;&amp;#34;)

    if(result[0] === &amp;#34;0&amp;#34;) return &amp;#34;0&amp;#34;

    return result

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;How can you encode and decode an array of strings into and out of a single string? To encode, you can prefix each string with its length and a separator symbol, and then join them. To decode, you can read numbers until you get to the separator symbol, slice the number of characters after the separator symbol equal to the number just read, and add it to a &lt;code&gt;result&lt;/code&gt; array. Then as long as the string being decoded still has length, read the next characters for numbers until a separator symbol is reached, etc.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;953627814&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;953627814&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/encode-and-decode-strings/&#34;
  
  target=&#34;_blank&#34;
  &gt;Encode and Decode Strings&lt;/a
&gt;
 &amp;ndash; code adapted from &lt;a
  href=&#34;https://medium.com/@miniChang8/leetcode-encode-and-decode-strings-4dde7e0efa1c&#34;
  
  target=&#34;_blank&#34;
  &gt;Mini Chang&amp;rsquo;s Python code and explanation&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof strs === &amp;#34;object&amp;#34; (array of strings)
// typeof s === &amp;#34;string&amp;#34;

const encode = (strs) =&amp;gt; {

  const result = []
  for (let string of strs){
    const encoded = String(string.length) &amp;#43; &amp;#39;/&amp;#39; &amp;#43; string
    result.push(encoded)
  }
  return result.join(&amp;#34;&amp;#34;)

}

const decode = (s) =&amp;gt; {

  const result = []
  let i = 0
  while (i &amp;lt; s.length){
      let numberIndex = i
      while (s[numberIndex] !== &amp;#39;/&amp;#39;){
        numberIndex&amp;#43;&amp;#43;
      }
      let size = Number(s.slice(i,numberIndex))
      const word = s.slice(numberIndex &amp;#43; 1, numberIndex &amp;#43; 1 &amp;#43; size)
      i = numberIndex &amp;#43; 1 &amp;#43; size
      result.push(word)
  }

  return result

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find the minimum length substring (adjacent characters) in a parent input string such that every character if a different string is present, you can begin by constructing a &lt;code&gt;characterCountMap&lt;/code&gt; from the string that has the requirements for the result string. Then you can loop the parent string. At every location, check if the &lt;code&gt;characterCountMap&lt;/code&gt; has the location&amp;rsquo;s &lt;code&gt;character&lt;/code&gt;. If not, you can skip to the next &lt;code&gt;character&lt;/code&gt;. Else, reduce the count at the appropriate &lt;code&gt;characterCountMap&lt;/code&gt; by 1. If this &lt;code&gt;newCount&lt;/code&gt; is 0, increment a &lt;code&gt;zeroCounter&lt;/code&gt; declared outside the loop. While the &lt;code&gt;zeroCounter&lt;/code&gt; is the same size as the &lt;code&gt;characterCountMap&lt;/code&gt;, indicating that all characters in the requirements string are in the currently viewed string up to their count, find the &lt;code&gt;startCharacter&lt;/code&gt; at a &lt;code&gt;startIndex&lt;/code&gt; seeded outside the first loop at 0. If this character is an entry in the &lt;code&gt;characterCountMap&lt;/code&gt; (indicating that its removal will be significant), add 1 to the character&amp;rsquo;s location in the &lt;code&gt;characterCountMap&lt;/code&gt;. If this &lt;code&gt;increasedCount&lt;/code&gt; is 1, meaning that when it is added back the &lt;code&gt;characterCountMap&lt;/code&gt; no longer meets the result requirements, decrement the &lt;code&gt;zeroCounter&lt;/code&gt;. If the &lt;code&gt;increasedCount&lt;/code&gt; is 1 and the length between the index in the outermost loop and the &lt;code&gt;startIndex&lt;/code&gt; is less than a &lt;code&gt;mininimim&lt;/code&gt; started outside the loops at Infinity, update &lt;code&gt;minimum&lt;/code&gt; and &lt;code&gt;bestResultStartIndex&lt;/code&gt; and &lt;code&gt;bestResultEndIndex&lt;/code&gt;. As the last step in the loop checking if &lt;code&gt;zeroCounter&lt;/code&gt; matches the size of the &lt;code&gt;characterCountMap&lt;/code&gt;, increase &lt;code&gt;startIndex&lt;/code&gt; by 1. At the end of the loop of the parent string, use the &lt;code&gt;bestResultStartIndex&lt;/code&gt; and &lt;code&gt;bestResultEndIndex&lt;/code&gt; to return the result from the parent string, unless &lt;code&gt;minimum&lt;/code&gt; is still infinite. In that case you can return an empty string.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;948761325&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;948761325&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/minimum-window-substring/&#34;
  
  target=&#34;_blank&#34;
  &gt;Minimum Window Substring&lt;/a
&gt;
 &amp;ndash; code adapted from &lt;a
  href=&#34;https://leetcode.com/problems/minimum-window-substring/solutions/465059/javascript/&#34;
  
  target=&#34;_blank&#34;
  &gt;art1991&amp;rsquo;s code&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof s === &amp;#34;string&amp;#34;
// typeof t === &amp;#34;string&amp;#34;

const minWindow = (s, t) =&amp;gt; {
    
  const characterCountMap = new Map()
  for (const char of t) {
    characterCountMap.set(char, (characterCountMap.get(char) || 0) &amp;#43; 1)
  }

  let zeroCounter = 0
  let startIndex = 0

  let minimum = Infinity
  let bestResultStartIndex = 0
  let bestResultEndIndex = 0

  for (let i = 0; i &amp;lt; s.length; i&amp;#43;&amp;#43;) {
    const character = s[i]
    if (!characterCountMap.has(character)) continue

    const newCount = characterCountMap.get(character) - 1
    characterCountMap.set(character, newCount)

    if (newCount) continue

    zeroCounter&amp;#43;&amp;#43;

    while (zeroCounter === characterCountMap.size) {

        const startCharacter = s[startIndex]

        if (characterCountMap.has(startCharacter)) {
            const increasedCount = characterCountMap.get(startCharacter) &amp;#43; 1
            characterCountMap.set(startCharacter, increasedCount)
            if (increasedCount === 1) {
                zeroCounter--
                if (i - startIndex &amp;lt; minimum) {
                    minimum = i - startIndex &amp;#43; 1
                    bestResultStartIndex = startIndex
                    bestResultEndIndex = i
                }
            }
            
        }

        startIndex&amp;#43;&amp;#43;
        
    }


  }
  return isFinite(minimum) ? s.slice(bestResultStartIndex, bestResultEndIndex &amp;#43; 1) : &amp;#34;&amp;#34;

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;h1 id=&#34;binary-trees&#34;&gt;Binary Trees&lt;/h1&gt;
&lt;p&gt;One of the ways LeetCode has implemented a binary tree node is like this:&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;817934265&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;817934265&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

function TreeNode(val, left, right) {

     this.val = (val===undefined ? 0 : val)

     this.left = (left===undefined ? null : left)
     this.right = (right===undefined ? null : right)

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;Inverting a binary tree means to &amp;ldquo;turn it over&amp;rdquo; as if it was a page in a book, creating what the original would look like from behind. This means swapping every pair of &lt;code&gt;left&lt;/code&gt; and &lt;code&gt;right&lt;/code&gt; references. A recrusive implementation step returns immediately on a &lt;code&gt;null&lt;/code&gt; node, and otherwise returns after using a holding variable for one of the &lt;code&gt;left&lt;/code&gt; or &lt;code&gt;right&lt;/code&gt; references to help complete the second half of the swap.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;739186452&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;739186452&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/invert-binary-tree/&#34;
  
  target=&#34;_blank&#34;
  &gt;Invert Binary Tree&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof root === &amp;#34;object&amp;#34; (binary tree node)

const invertTree = (root) =&amp;gt; {

    if(!root) return root
    const hold = root.right
    root.right = root.left
    root.left = hold

    invertTree(root.left)
    invertTree(root.right)

    return root

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;A height-balanced binary tree differs in depth by no more than one at each node. To test if a binary tree is height-balanced, do depth-first search from the root node, returning 0 on each base case of a &lt;code&gt;null&lt;/code&gt; root, and otherwise check if the differences in return height values from the left or right nodes is not greater than 1. Because each hopeful step of the depth-first search then returns the maximum of the right or left depth plus 1, the full depth under any node will always bubble up. If a difference greater than one bubbles up, -1 can be returned preferentially as a signal for a non-height balanced tree.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;852349716&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;852349716&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/balanced-binary-tree/&#34;
  
  target=&#34;_blank&#34;
  &gt;Balanced Binary Tree&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof root === &amp;#34;object&amp;#34; (binary tree node)

const isBalanced = (root) =&amp;gt; {

    const height = (root) =&amp;gt; {
        if(!root) return 0
        const left = height(root.left)
        const right = height(root.right)

        if(left === -1 || right === -1 || Math.abs(left - right) &amp;gt; 1) return -1

        return 1 &amp;#43; Math.max(left,right)
    }

    return height(root) !== -1

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find the diameter of a binary tree, or the maximum distance between any two nodes, recursion and depth-first search lead to a solution. At every node, find the maximum distance under the right and left nodes, then update the max if the sum of these is greater than the max. Return the greater of the left or right plus 1 (the plus 1 accounts for the edge between the returning node and the next node up). The base case, reached when recursion decends to find no node, returns 0.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;547912683&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;547912683&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/diameter-of-binary-tree/&#34;
  
  target=&#34;_blank&#34;
  &gt;Diameter of Binary Tree&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof root === &amp;#34;object&amp;#34; (binary tree node)

const diameterOfBinaryTree = (root) =&amp;gt; {

    let max = 0
    
    const go = (loc) =&amp;gt; {
        if(!loc) return 0

        let left = go(loc.left)
        let right = go(loc.right)

        max = Math.max(max,left&amp;#43;right)

        return 1&amp;#43;Math.max(left,right)
    }
    
    go(root)
    return max

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find the maximum depth of a binary tree, counted in number of nodes, you can use recursion with a base case of returning 0 on an empty node. On every other node, the greater of the depth of the left side of the tree plus 1 or the depth of the right side of the tree plus 1 should be returned.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;632941785&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;632941785&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/maximum-depth-of-binary-tree/&#34;
  
  target=&#34;_blank&#34;
  &gt;Maximum Depth of Binary Tree&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof root === &amp;#34;object&amp;#34; (binary tree node)

const maxDepth = (root) =&amp;gt; {

        if (!root) return 0

        return 1 &amp;#43; Math.max(maxDepth(root.left), maxDepth(root.right))
    
}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;Two binary trees have the same structure and values if at every node, their values match, and their left and right subtrees match. This lends nicely to a recursive structure which can return &lt;code&gt;true&lt;/code&gt; on matching empty nodes and bubble a &lt;code&gt;true&lt;/code&gt; all the way up to the final return only if everything matches.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;316254978&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;316254978&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/same-tree/&#34;
  
  target=&#34;_blank&#34;
  &gt;Same Tree&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof p === &amp;#34;object&amp;#34; (binary tree node)
// typeof q === &amp;#34;object&amp;#34; (binary tree node)

const isSameTree = (p, q) =&amp;gt; {
    
        if(!p &amp;amp;&amp;amp; !q) return true
        if(p &amp;amp;&amp;amp; !q || q &amp;amp;&amp;amp; !p || q.val !== p.val) return false

        return isSameTree(p.left,q.left) &amp;amp;&amp;amp; isSameTree(p.right,q.right)

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To detect if a binary tree is symmetric, first note that an empty tree is symmetric. Next, generate two pointers, one to the &lt;code&gt;.left&lt;/code&gt; node and the other to the &lt;code&gt;.right&lt;/code&gt; node of the head, and put these in a recursive call. These &lt;code&gt;.left&lt;/code&gt; and &lt;code&gt;.right&lt;/code&gt; nodes are in locations that should be symmetric and identical. If both are empty, the base case can bubble up &lt;code&gt;true&lt;/code&gt;, if they are different by presence/absence or value, the base case can bubble upp &lt;code&gt;false&lt;/code&gt;, and otherwise, two more recrusive calls can be generated, comparing the outer two nodes that are children of the pair that were checked, and also comparing the inner two nodes. Any &lt;code&gt;false&lt;/code&gt; in a base case should ultimately return &lt;code&gt;false&lt;/code&gt; in the parent function, otherwise the function returns &lt;code&gt;true&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;178236495&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;178236495&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/symmetric-tree/&#34;
  
  target=&#34;_blank&#34;
  &gt;Symmetric Tree&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof root === &amp;#34;object&amp;#34; (binary tree node)

const isSymmetric = (root) =&amp;gt; {

    if (!root) return true

    return equal(root.left,root.right)

}

const equal = (l, r) =&amp;gt; {
    if (!l &amp;amp;&amp;amp; !r) return true
    if (!l || !r || l.val !== r.val) return false
    return equal(l.right,r.left) &amp;amp;&amp;amp; equal(l.left,r.right)
}

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find if a tree &lt;code&gt;subRoot&lt;/code&gt; is truly a subroot of a tree &lt;code&gt;root&lt;/code&gt;, one approach is to check if any node in or below &lt;code&gt;root&lt;/code&gt; decends to a structure that matches &lt;code&gt;subRoot&lt;/code&gt;. However, this approach is )O(&lt;code&gt;root&lt;/code&gt;*&lt;code&gt;subRoot&lt;/code&gt;). Constructing a &lt;code&gt;subRoot&lt;/code&gt; serialization and then checking if this is a substring of &lt;code&gt;root&lt;/code&gt; can be done with any string-matching algorithm to be used at the end, including the JavaScript-native &lt;code&gt;includes&lt;/code&gt; method, which could theoretically have an implementation that is in O(&lt;code&gt;root&lt;/code&gt; + &lt;code&gt;subRoot&lt;/code&gt;) time at the cost of a precompute table, like the &lt;a
  href=&#34;https://en.wikipedia.org/wiki/Knuth%E2%80%93Morris%E2%80%93Pratt_algorithm&#34;
  
  target=&#34;_blank&#34;
  &gt;Knuth-Morris-Pratt String Matching Algorithm&lt;/a
&gt;
.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;492617538&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;492617538&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/subtree-of-another-tree/&#34;
  
  target=&#34;_blank&#34;
  &gt;Subtree of Another Tree&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof root === &amp;#34;object&amp;#34; (binary tree node)
// typeof subRoot === &amp;#34;object&amp;#34; (binary tree node)

const isSubtree = (root, subRoot) =&amp;gt; stringBuilder(root).includes(stringBuilder(subRoot))

const stringBuilder = (node) =&amp;gt; {
    if(!node) return &amp;#39;.&amp;#39;
    const s = `&amp;lt;${stringBuilder(node.left)} &amp;#43; ${node.val} &amp;#43; ${stringBuilder(node.right)}&amp;gt;`
    return s
}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;Returning a level order traversal of binary tree nodes (array of array of values for each level) can be done with three variables. Assuming there is a &lt;code&gt;root&lt;/code&gt;, define &lt;code&gt;result&lt;/code&gt; as an empty array inside an array, define a &lt;code&gt;level&lt;/code&gt; array that contains &lt;code&gt;root&lt;/code&gt;, and define a &lt;code&gt;nextLevel&lt;/code&gt; array that is empty. The &lt;code&gt;level&lt;/code&gt; array will be treated as a queue and repeatedly processed starting from the left, with each value pushed into the last inner array in &lt;code&gt;result&lt;/code&gt;, and with each present &lt;code&gt;.left&lt;/code&gt; and &lt;code&gt;.right&lt;/code&gt; node being pushed in that order to the &lt;code&gt;nextLevel&lt;/code&gt; array. When &lt;code&gt;level&lt;/code&gt; is empty and &lt;code&gt;nextLevel&lt;/code&gt; has length, push an empty array into &lt;code&gt;result&lt;/code&gt; to represent the next level and replace &lt;code&gt;level&lt;/code&gt; with &lt;code&gt;nextLevel&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;789425136&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;789425136&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/binary-tree-level-order-traversal/&#34;
  
  target=&#34;_blank&#34;
  &gt;Binary Tree Level Order Traversal&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof root === &amp;#34;object&amp;#34; (binary tree node)

const levelOrder = (root) =&amp;gt; {

    if(!root) return []
    const result = [[]]
    let level = [root]
    let nextLevel = []

    while(level.length){
        const test = level.shift()
        result[result.length-1].push(test.val)

        if(test.left) nextLevel.push(test.left)
        if(test.right) nextLevel.push(test.right)

        if(!level.length &amp;amp;&amp;amp; nextLevel.length){
            result.push([])
            level = nextLevel
            nextLevel = []
        }
    }
    return result

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find the lowest common ancestor of two uniquely-valued binary tree nodes, an implementation involves an inner function &lt;code&gt;go&lt;/code&gt;. Run this function taking an argument of the &lt;code&gt;root&lt;/code&gt; of the tree. When &lt;code&gt;go&lt;/code&gt; runs, if there is no argument, return &lt;code&gt;0&lt;/code&gt;, else create a &lt;code&gt;count&lt;/code&gt; variable and add to it the results of running &lt;code&gt;go&lt;/code&gt; on the &lt;code&gt;.left&lt;/code&gt; and &lt;code&gt;.right&lt;/code&gt; of the current node, as well as &lt;code&gt;1&lt;/code&gt; if the &lt;code&gt;.val&lt;/code&gt; of the current node if one of the two values being investigated. If &lt;code&gt;count&lt;/code&gt; is then &lt;code&gt;2&lt;/code&gt;, set a &lt;code&gt;result&lt;/code&gt; variable outside of &lt;code&gt;go&lt;/code&gt; to the current node, and return &lt;code&gt;0&lt;/code&gt; so that no higher nodes will falsely be recorded as the lowest common ancestor. Otherwise return &lt;code&gt;count&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;216974385&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;216974385&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/lowest-common-ancestor-of-a-binary-tree/&#34;
  
  target=&#34;_blank&#34;
  &gt;Lowest Common Ancestor of a Binary Tree&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof root === &amp;#34;object&amp;#34; (binary tree node)
// typeof p === &amp;#34;object&amp;#34; (binary tree node)
// typeof q === &amp;#34;object&amp;#34; (binary tree node)

const lowestCommonAncestor = (root, p, q) =&amp;gt; {

    let result
    
    const go = (node) =&amp;gt; {
        if(!node) return 0
        let count = 0
        count &amp;#43;= go(node.left)
        count &amp;#43;= go(node.right)
        count &amp;#43;= (node.val === p.val || node.val === q.val)
        if(count === 2){
            result = node
            return 0
        }
        return count
    }
    
    go(root)
    return result

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find an array of all the rightmost elements from top to bottom in a binary tree, run a helper function &lt;code&gt;go&lt;/code&gt; with the &lt;code&gt;root&lt;/code&gt; node and the current &lt;code&gt;depth&lt;/code&gt; (starting at 0). In &lt;code&gt;go&lt;/code&gt;, if there is no node, return immediately. Else place the &lt;code&gt;.val&lt;/code&gt; of the current node at the &lt;code&gt;depth&lt;/code&gt; index of &lt;code&gt;results&lt;/code&gt; and call &lt;code&gt;go&lt;/code&gt; twice, with the &lt;code&gt;.left&lt;/code&gt; and then the &lt;code&gt;.right&lt;/code&gt; nodes, each with &lt;code&gt;depth+1&lt;/code&gt; as the second argument. Because rights are being called after lefts, each row will be viewed from left to right even though the depth-first search nature of the algorithm means that a row may not be finished before a call goes down to the next level.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;389126745&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;389126745&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/binary-tree-right-side-view/&#34;
  
  target=&#34;_blank&#34;
  &gt;Binary Tree Right Side View&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof root === &amp;#34;object&amp;#34; (binary tree node)

const rightSideView = (root) =&amp;gt; {

    const result = []

    const go = (node, depth) =&amp;gt; {
        if (!node) return
        result[depth] = node.val
        go(node.left, depth&amp;#43;1)
        go(node.right, depth&amp;#43;1)
    }

    go(root,0)
    return result
    
}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;How to construct a binary tree from arrays with unique values containing the &lt;code&gt;preorder&lt;/code&gt; and &lt;code&gt;inorder&lt;/code&gt; traversal? Note that in the &lt;code&gt;preorder&lt;/code&gt; traversal, head nodes appear before children nodes. This means that the 0th location in the &lt;code&gt;preorder&lt;/code&gt; array should be the root value of the binary tree. To construct the tree formally, return a helper function &lt;code&gt;construct&lt;/code&gt; with four arguments &amp;ndash; the leftmost and rightmost indicies of the &lt;code&gt;preorder&lt;/code&gt; and &lt;code&gt;inorder&lt;/code&gt; arrays. If either left is greater than than their right, &lt;code&gt;construct&lt;/code&gt; can immediately return a &lt;code&gt;null&lt;/code&gt; node. Else, get the current root &lt;code&gt;node&lt;/code&gt; &lt;code&gt;value&lt;/code&gt; from the &lt;code&gt;value&lt;/code&gt; of &lt;code&gt;preorder&lt;/code&gt; at &lt;code&gt;preorderLeft&lt;/code&gt;. Start a new &lt;code&gt;node&lt;/code&gt; with the &lt;code&gt;value&lt;/code&gt;, and find the &lt;code&gt;index&lt;/code&gt; of that &lt;code&gt;value&lt;/code&gt; in &lt;code&gt;inorder&lt;/code&gt;. Count the number of &lt;code&gt;leftNodes&lt;/code&gt; by subtracting the &lt;code&gt;inorderLeft&lt;/code&gt; from &lt;code&gt;index&lt;/code&gt;, then build out the &lt;code&gt;.left&lt;/code&gt; and &lt;code&gt;.right&lt;/code&gt; nodes by running two new &lt;code&gt;construct&lt;/code&gt; invocations with updated bounds.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;573189642&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;573189642&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/construct-binary-tree-from-preorder-and-inorder-traversal/description/&#34;
  
  target=&#34;_blank&#34;
  &gt;Construct Binary Tree from Preorder and Inorder Traversal&lt;/a
&gt;
 &amp;ndash; code adapted from &lt;a
  href=&#34;https://leetcode.com/problems/construct-binary-tree-from-preorder-and-inorder-traversal/solutions/34553/&#34;
  
  target=&#34;_blank&#34;
  &gt;jeantimex&amp;rsquo;s code and explanation&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof preorder === &amp;#34;object&amp;#34; (array of numbers)
// typeof inorder === &amp;#34;object&amp;#34; (array of numbers)

const buildTree = (preorder, inorder) =&amp;gt; {
    
    const construct = (preorderLeft, preorderRight, inorderLeft, inorderRight) =&amp;gt; {
        if (preorderRight &amp;lt; preorderLeft || inorderRight &amp;lt; inorderLeft) return null

        const value = preorder[preorderLeft]
        const node = new TreeNode(value)
        const index = mapValueToInorderIndex.get(value)

        const leftNodes = index - inorderLeft
        node.left  = construct(preorderLeft &amp;#43; 1, preorderLeft &amp;#43; leftNodes, inorderLeft, index - 1)
        node.right = construct(preorderLeft &amp;#43; leftNodes &amp;#43; 1, preorderRight, index &amp;#43; 1, inorderRight)

        return node
    }

    const mapValueToInorderIndex = new Map()
    for (let i = 0; i &amp;lt; inorder.length; i&amp;#43;&amp;#43;){
        mapValueToInorderIndex.set(inorder[i], i)
    }
    
    return construct(0, preorder.length - 1, 0, inorder.length - 1)

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find all root-to-leaf paths in a binary tree that sum to a target value, you can create a &lt;code&gt;results&lt;/code&gt; array and then use recursion with a helper function. Run the helper function with the &lt;code&gt;root&lt;/code&gt;, set a &lt;code&gt;sum&lt;/code&gt; of 0, and a &lt;code&gt;valsSoFar&lt;/code&gt; as an empty array. No &lt;code&gt;root&lt;/code&gt; is the base case so then you can just return. Otherwise, create a &lt;code&gt;currentVals&lt;/code&gt; array out of &lt;code&gt;valsSoFar&lt;/code&gt; and the value at the current node, and create a &lt;code&gt;newSum&lt;/code&gt; by adding &lt;code&gt;sum&lt;/code&gt; to the value at the current node. Then, if current node is a leaf node (has no descendents), and the &lt;code&gt;newSum&lt;/code&gt; is the target sum, you can push &lt;code&gt;currentVals&lt;/code&gt; to &lt;code&gt;results&lt;/code&gt;. Else recurse on the &lt;code&gt;.left&lt;/code&gt; and &lt;code&gt;.right&lt;/code&gt; of the current node.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;754329861&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;754329861&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/path-sum-ii/&#34;
  
  target=&#34;_blank&#34;
  &gt;Path Sum II&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof root === &amp;#34;object&amp;#34; (binary tree node)
// typeof targetSum === &amp;#34;number&amp;#34;

const pathSum = (root, targetSum) =&amp;gt; {

    const results = []
    
    const go = (root, sum = 0, valsSoFar = []) =&amp;gt; {
        if(!root) return
        const currentVals = [...valsSoFar, root.val]
        const newSum = sum &amp;#43; root.val

        if(!root.left &amp;amp;&amp;amp; !root.right &amp;amp;&amp;amp; targetSum === newSum) results.push(currentVals)
        go(root.left, newSum, currentVals)
        go(root.right, newSum, currentVals)
    }
    
    go(root)
    
    return results

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;How to find the maximum width of a binary tree? One solution involves depth-first search. Create an empty &lt;code&gt;leftmostLocations&lt;/code&gt; array, then conduct a depth-first search starting with the &lt;code&gt;root&lt;/code&gt; node, a &lt;code&gt;widthLocation&lt;/code&gt; of 0, and a &lt;code&gt;depth&lt;/code&gt; of 0. The base case is a null node, but otherwise, a helper function can update the &lt;code&gt;leftmostLocations&lt;/code&gt; array with the current &lt;code&gt;widthLocation&lt;/code&gt; at the &lt;code&gt;depth&lt;/code&gt; index if the &lt;code&gt;depth&lt;/code&gt; has never before been seen. You can then create an &lt;code&gt;indexNormalizedToLeftmost&lt;/code&gt; but subtracting the leftmost location at the given depth from the &lt;code&gt;widthLocation&lt;/code&gt; (which helps with large LeetCode test cases), update a &lt;code&gt;best&lt;/code&gt; with &lt;code&gt;indexNormalizedToLeftmost+1&lt;/code&gt; if appropriate, and run the helper on the &lt;code&gt;.left&lt;/code&gt; and &lt;code&gt;.right&lt;/code&gt; of the current &lt;code&gt;node&lt;/code&gt;, also passing down the doubled &lt;code&gt;indexNormalizedToLeftmost&lt;/code&gt; and the doubled &lt;code&gt;indexNormalizedToLeftmost&lt;/code&gt; with a 1 added after the multiplication, respectively. The third argument for both depth-first search calls is simply &lt;code&gt;depth+1&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;625748931&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;625748931&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/maximum-width-of-binary-tree/&#34;
  
  target=&#34;_blank&#34;
  &gt;Maximum Width of a Binary Tree&lt;/a
&gt;
 &amp;ndash; code adapted from &lt;a
  href=&#34;https://leetcode.com/problems/maximum-width-of-binary-tree/solutions/154200/javascript-dfs-solution/&#34;
  
  target=&#34;_blank&#34;
  &gt;linfongi&amp;rsquo;s code&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof root === &amp;#34;object&amp;#34; (binary tree node)

const widthOfBinaryTree = (root) =&amp;gt; {
    
  const leftmostLocations = []
  let best = 0

  const dfs = (node, widthLocation, depth) =&amp;gt; {
    if (!node) return
    if (depth === leftmostLocations.length) {
      leftmostLocations[depth] = widthLocation
    }
    
    const indexNormalizedToLeftmost = widthLocation - leftmostLocations[depth]
    best = Math.max(best, indexNormalizedToLeftmost &amp;#43; 1)
    dfs(node.left, indexNormalizedToLeftmost*2, depth &amp;#43; 1)
    dfs(node.right, indexNormalizedToLeftmost*2&amp;#43;1, depth &amp;#43; 1)
  }
  
  dfs(root, 0, 0)
  return best

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To return the values of the nodes of a binary tree in zigzag order, that is, a first row left-to-right, followed by a second row right-to-left, and third row left-to-right, etc., you can perform a level-order traversal and keep track of the row number to reverse the values of every other row. If there is input, create a &lt;code&gt;results&lt;/code&gt; array, fill a &lt;code&gt;row&lt;/code&gt; array with the starting node, and declare an empty &lt;code&gt;nextRow&lt;/code&gt; array. While &lt;code&gt;row&lt;/code&gt; has a length, create a &lt;code&gt;values&lt;/code&gt; array and loop the nodes in &lt;code&gt;row&lt;/code&gt;, pushing every value seen to &lt;code&gt;values&lt;/code&gt;, and pushing every &lt;code&gt;.left&lt;/code&gt; and &lt;code&gt;.right&lt;/code&gt; node seen, in that order, to &lt;code&gt;nextRow&lt;/code&gt;. If the &lt;code&gt;results&lt;/code&gt; array is an odd length, reverse the &lt;code&gt;values&lt;/code&gt; array, and in either case, next push it to the &lt;code&gt;results&lt;/code&gt; array. Then set &lt;code&gt;row&lt;/code&gt; as &lt;code&gt;nextRow&lt;/code&gt; and set &lt;code&gt;nextRow&lt;/code&gt; as an empty array. This is the last part of the outer cycle. It continues if the new &lt;code&gt;row&lt;/code&gt; contains any values.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;613492857&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;613492857&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/binary-tree-zigzag-level-order-traversal/&#34;
  
  target=&#34;_blank&#34;
  &gt;Binary Tree Zigzag Level Order Traversal&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof root === &amp;#34;object&amp;#34; (binary tree node)

const zigzagLevelOrder = (root) =&amp;gt; {
    if (!root) return []

    const results = []
    let row = [root]
    let nextRow = []

    while(row.length){

        let values = []
        
        for (let node of row){
            values.push(node.val)
            if (node.left) nextRow.push(node.left)
            if (node.right) nextRow.push(node.right)
        }
        if (results.length % 2) values = values.reverse()

        results.push(values)
        row = nextRow
        nextRow = []
    }

    return results
    
}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find all possible ways a binary tree sums to a given number on a downwards path (no bending at a node), you can run a recursive call with default parameters for &lt;code&gt;sumsToHere&lt;/code&gt;, which starts as an empty array, as well as a &lt;code&gt;result&lt;/code&gt; count that starts at 0. If there is no node, return &lt;code&gt;result&lt;/code&gt;, else push a 0 value to &lt;code&gt;sumsToHere&lt;/code&gt; and loop the &lt;code&gt;sumsToHere&lt;/code&gt; array. At every step, add the value at the current node, and then check if the new value at the &lt;code&gt;sumsToHere&lt;/code&gt; location is equal to target value. If so, add 1 to the result. Next, run the parent recursively on the &lt;code&gt;.left&lt;/code&gt; and &lt;code&gt;.right&lt;/code&gt; of the current node, and add their values to &lt;code&gt;result&lt;/code&gt;. Make sure to copy the &lt;code&gt;sumsToHere&lt;/code&gt; array, rather than put it in directly. Finally, return &lt;code&gt;result&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;386521497&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;386521497&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/path-sum-iii/&#34;
  
  target=&#34;_blank&#34;
  &gt;Path Sum III&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof root === &amp;#34;object&amp;#34; (binary tree node)
// targetSum === &amp;#34;number&amp;#34;

const pathSum = (root, targetSum, sumsToHere = [], result = 0) =&amp;gt; {

        if(!root) return result

        sumsToHere.push(0)
        for(let i = 0; i &amp;lt; sumsToHere.length; i&amp;#43;&amp;#43;){
            sumsToHere[i] &amp;#43;= root.val
            if(sumsToHere[i] === targetSum) result&amp;#43;&amp;#43;
        }

        result &amp;#43;= pathSum(root.left, targetSum, sumsToHere.slice(), 0)
        result &amp;#43;= pathSum(root.right, targetSum, sumsToHere.slice(), 0)

        return result
        
}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To return an array of nodes that all are a certain distance from a target node in a binary tree, first note that an empty root returns an empty array. Otherwise, you can traverse the array from the root until you find the &lt;code&gt;targetNode&lt;/code&gt;, adding reverse &lt;code&gt;.parent&lt;/code&gt; links at every step. Then you can run recursion from the &lt;code&gt;targetNode&lt;/code&gt;, branching in the &lt;code&gt;.left&lt;/code&gt;, &lt;code&gt;.right&lt;/code&gt;, and &lt;code&gt;.parent&lt;/code&gt; directions until either an end of the tree is reached, or a node of the desired distance from the &lt;code&gt;targetNode&lt;/code&gt; is found. When such a node is found, you can push it to &lt;code&gt;results&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;425183976&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;425183976&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/all-nodes-distance-k-in-binary-tree/&#34;
  
  target=&#34;_blank&#34;
  &gt;All Nodes Distance K in Binary Tree&lt;/a
&gt;
 &amp;ndash; code adapted from &lt;a
  href=&#34;https://leetcode.com/problems/all-nodes-distance-k-in-binary-tree/solutions/843575/javascript-dfs/&#34;
  
  target=&#34;_blank&#34;
  &gt;fbecker11&amp;rsquo;s code&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof root === &amp;#34;object&amp;#34; (binary tree node)
// typeof target === &amp;#34;object&amp;#34; (binary tree node)
// typeof k === &amp;#34;number&amp;#34;

const distanceK = (root, target, k) =&amp;gt; {
  if(!root) return []

  const addReverseLinksAndReturnTargetNode  = (root, parent) =&amp;gt; {
        if(!root) return null
        root.parent = parent
        if(root === target){    
        return root 
        }    
        return addReverseLinksAndReturnTargetNode(root.left, root) ||
         addReverseLinksAndReturnTargetNode(root.right, root)    
  }

  const targetNode = addReverseLinksAndReturnTargetNode(root, null)  
  const results = []

  const getResults = (node, distance) =&amp;gt; {
    if(!node || node.visited) return
    if(!distance){
        results.push(node.val)
        return
    }   
    node.visited = true
    getResults(node.left, distance-1)
    getResults(node.right, distance-1)
    getResults(node.parent, distance-1)
  }

  getResults(targetNode, k)
  return results

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To serialize and deserialize a binary tree into and out of a string, you can use helper functions. In the serialize function, you can create a collection &lt;code&gt;array&lt;/code&gt; and run it with the root of the tree in a recursive helper. In this helper, if given &lt;code&gt;node&lt;/code&gt; is empty, push a symbol like &lt;code&gt;#&lt;/code&gt; to the &lt;code&gt;array&lt;/code&gt; and return, otherwise push the &lt;code&gt;.val&lt;/code&gt; to the &lt;code&gt;array&lt;/code&gt; and run the helper on the &lt;code&gt;.left&lt;/code&gt; and &lt;code&gt;.right&lt;/code&gt; of the &lt;code&gt;node&lt;/code&gt;, in that order. Once all helpers resolve, turn the &lt;code&gt;array&lt;/code&gt; into a string with commas separating the values. To deserialize, you can return &lt;code&gt;null&lt;/code&gt; if there is no data length, otherwise split the input by &lt;code&gt;,&lt;/code&gt; and run a helper function. In the helper, whenever there is no &lt;code&gt;array&lt;/code&gt; length, you can return &lt;code&gt;null&lt;/code&gt;, and after splicing off the first entry in the array, if this is the &lt;code&gt;null&lt;/code&gt; symbol &lt;code&gt;#&lt;/code&gt;, you can also return &lt;code&gt;null&lt;/code&gt;. Otherwise turn the spliced &lt;code&gt;value&lt;/code&gt; into a number and add it as the &lt;code&gt;.val&lt;/code&gt; of a new &lt;code&gt;node&lt;/code&gt;, and then run the function recursively on the &lt;code&gt;array&lt;/code&gt;, first assigning its results to &lt;code&gt;.left&lt;/code&gt; of the &lt;code&gt;node&lt;/code&gt; and then to &lt;code&gt;.right&lt;/code&gt;. This works because the splicing means that &lt;code&gt;.right&lt;/code&gt; will receive the correct array input. At the end of the helper, return the &lt;code&gt;node&lt;/code&gt; and at the end of all recursive calls return the first helper function.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;937451268&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;937451268&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/serialize-and-deserialize-binary-tree/&#34;
  
  target=&#34;_blank&#34;
  &gt;Serialize and Deserialize Binary Tree&lt;/a
&gt;
 &amp;ndash; code adapted from &lt;a
  href=&#34;https://leetcode.com/problems/serialize-and-deserialize-binary-tree/solutions/74398/javascript-solution/&#34;
  
  target=&#34;_blank&#34;
  &gt;ein3108&amp;rsquo;s code&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof root === &amp;#34;object&amp;#34; (binary tree node)
// typeof data === &amp;#34;string&amp;#34;

const serializeHelper = (node, array) =&amp;gt; {
    if (!node) return array.push(&amp;#34;#&amp;#34;)

    array.push(node.val)
    serializeHelper(node.left, array)
    serializeHelper(node.right, array)
}

const serialize = (root) =&amp;gt; {
    const array = []
    serializeHelper(root, array)
    return array.toString()
}

const deserializeHelper = (array) =&amp;gt; {
    if (!array.length) return null
    const value = array.splice(0,1)
    if (value[0] === &amp;#34;#&amp;#34;) return null

    const node = new TreeNode(Number(value))
    node.left = deserializeHelper(array)
    node.right = deserializeHelper(array)
    return node
}

const deserialize = (data) =&amp;gt; {
    if(!data?.length) return null
    return deserializeHelper(data.split(&amp;#34;,&amp;#34;))
}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find the maximum sum of a path in a binary tree, you can ue a helper function. Set a &lt;code&gt;best&lt;/code&gt; to negative Infinity and run a recursive helper function with the root. In the recusive function, if the node is empty, return 0 as the base case. Else, find the &lt;code&gt;bestLeft&lt;/code&gt; as the max of 0 or the helper function run recusively with &lt;code&gt;.left&lt;/code&gt; of the root, and then find &lt;code&gt;bestRight&lt;/code&gt; a the max of 0 or the helper function run recursively with &lt;code&gt;.right&lt;/code&gt; of the root. Then compute a &lt;code&gt;bestAtNode&lt;/code&gt; with the sum of &lt;code&gt;bestLeft&lt;/code&gt;, the value at the current node, and &lt;code&gt;bestRight&lt;/code&gt;. If &lt;code&gt;bestAtNode&lt;/code&gt; is greater than the &lt;code&gt;best&lt;/code&gt; outside the helper function, update &lt;code&gt;best&lt;/code&gt;. Finally, return the maximum of &lt;code&gt;bestLeft&lt;/code&gt; or &lt;code&gt;bestRight&lt;/code&gt; plus the value at the current node (since you can only pass up the maximum sum of one path). When all runs of the helper function finish, return &lt;code&gt;best&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;267139485&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;267139485&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/binary-tree-maximum-path-sum/&#34;
  
  target=&#34;_blank&#34;
  &gt;Binary Tree Maximum Path Sum&lt;/a
&gt;
 &amp;ndash; code using a recursive pattern similar to &lt;a
  href=&#34;https://leetcode.com/problems/binary-tree-maximum-path-sum/solutions/254281/javascript/&#34;
  
  target=&#34;_blank&#34;
  &gt;gany&amp;rsquo;s code&lt;/a
&gt;
 and &lt;a
  href=&#34;https://leetcode.com/problems/binary-tree-maximum-path-sum/solutions/191918/javascript-dfs/&#34;
  
  target=&#34;_blank&#34;
  &gt;jignesh7&amp;rsquo;s code&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof root === &amp;#34;object&amp;#34; (binary tree node)

const maxPathSum = (root) =&amp;gt; {
    let best = -Infinity
    
    const go = (node) =&amp;gt; {
        if(!node) return 0

        const bestLeft = Math.max(0, go(node.left))
        const bestRight = Math.max(0, go(node.right))
        const bestAtNode = bestLeft &amp;#43; node.val &amp;#43; bestRight

        if (bestAtNode &amp;gt; best) best = bestAtNode
        return Math.max(bestLeft, bestRight) &amp;#43; node.val
    }
    
    go(root)
    return best

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;h1 id=&#34;binary-search&#34;&gt;Binary Search&lt;/h1&gt;
&lt;p&gt;Sorted information can be investigated in logarithmic (log(n)) time. Binary search is logarithmic. Each step divides searchable space in half&amp;ndash;much faster than a linear search.&lt;/p&gt;
&lt;p&gt;In a basic kind of binary search on an array, &lt;code&gt;start&lt;/code&gt; and &lt;code&gt;end&lt;/code&gt; variables point to the first and last filled indexes of the array. While &lt;code&gt;end&lt;/code&gt; is not smaller than &lt;code&gt;start&lt;/code&gt;, define a new &lt;code&gt;mid&lt;/code&gt; as the midpoint between &lt;code&gt;start&lt;/code&gt; and &lt;code&gt;end&lt;/code&gt; (rounding allowed). &lt;code&gt;Mid&lt;/code&gt; matching a target value allows immediate return of the value. If the target value is less than the mid index value, use &lt;code&gt;mid-1&lt;/code&gt; as the new &lt;code&gt;end&lt;/code&gt;, and if the target value is greater, use &lt;code&gt;mid+1&lt;/code&gt; as the new &lt;code&gt;start&lt;/code&gt;. Next, test if the new &lt;code&gt;end&lt;/code&gt; is smaller than &lt;code&gt;start&lt;/code&gt; to try another search step. If the search ends without finding the target value, returning -1 can indicate this (returning something else is better if negative targets are allowed).&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;183947265&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;183947265&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/binary-search/&#34;
  
  target=&#34;_blank&#34;
  &gt;Binary Search&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof nums === &amp;#34;object&amp;#34; (array of numbers)
// typeof target === &amp;#34;number&amp;#34;

const search = (nums, target) =&amp;gt; {

    let start = 0
    let end = nums.length - 1

    while(start &amp;lt;= end){
        const mid = Math.floor((end-start)/2) &amp;#43; start
        if (target === nums[mid]) return mid

        if (nums[mid] &amp;gt; target){
            end = mid - 1
        } else {
            start = mid &amp;#43; 1
        }
    }

    return -1

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;A version of binary search involves finding the first instance of a change in a series (such as first bad version). The &lt;code&gt;start&lt;/code&gt; and &lt;code&gt;end&lt;/code&gt; indices are initialized as before, as is &lt;code&gt;mid&lt;/code&gt; within a while loop, and since there is a guarantee of a change the loop will end by finding an index. The test condition uses an externally-provided function to see if the mid index has the change &amp;ndash; if so, &lt;code&gt;end&lt;/code&gt; is set to &lt;code&gt;mid&lt;/code&gt; (as this is a candidate for the first change), and if this index is the only index remaining, &lt;code&gt;mid&lt;/code&gt; can be returned. Else the mid index and everything lower cannot be the result, so &lt;code&gt;start&lt;/code&gt; can be &lt;code&gt;mid+1&lt;/code&gt; for the next loop.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;632715498&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;632715498&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/first-bad-version/&#34;
  
  target=&#34;_blank&#34;
  &gt;First Bad Version&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof isBadVersion === &amp;#34;function&amp;#34;
// typeof n === &amp;#34;number&amp;#34;

const solution = function(isBadVersion) {

    return function(n) {
        
        let start = 1
        let end = n

        while(true){

            const mid = start&amp;#43;Math.floor((end-start)/2)

            if(isBadVersion(mid)){
                end = mid
                if(start === end) return mid
            } else {
                start = mid &amp;#43; 1
            }

        }
        
    }
}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;How to find a number in an array that was divided into two segments that were swapped and reattached, but was before that sorted? To solve this in logarithmic time, begin as if the array was sorted normally, and declare &lt;code&gt;left&lt;/code&gt; and &lt;code&gt;right&lt;/code&gt; pointers at either end of the array. While &lt;code&gt;left&lt;/code&gt; is less than or equal to &lt;code&gt;right&lt;/code&gt;, define a &lt;code&gt;mid&lt;/code&gt; pointer between the two (round either way consistently). If &lt;code&gt;mid&lt;/code&gt; is the value being searched for, you can return the index. Otherwise, notice that one of the section above and the section below the &lt;code&gt;mid&lt;/code&gt; must be in sorted order. To detect which, check if the &lt;code&gt;left&lt;/code&gt;-most value is less than the value at &lt;code&gt;mid&lt;/code&gt; &amp;ndash; this means the left side is sorted. Otherwise the right side is sorted. If the left side is sorted, and the searched-for value is less than the value at &lt;code&gt;mid&lt;/code&gt; but greater or equal to the &lt;code&gt;left&lt;/code&gt;-most value, the left side is the side to investigate &amp;ndash; update the right pointer to &lt;code&gt;mid-1&lt;/code&gt;. Otherwise, if the left side is sorted, the left pointer should go to &lt;code&gt;mid+1&lt;/code&gt; by process of elimination. If instead it is the right side that is sorted, use similar code to check if the the searched-for value is on the sorted or non-sorted side. You can return &lt;code&gt;-1&lt;/code&gt; if it is never found.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;678324195&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;678324195&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/search-in-rotated-sorted-array/&#34;
  
  target=&#34;_blank&#34;
  &gt;Search in Rotated Sorted Array&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof nums === &amp;#34;object&amp;#34; (array of numbers)
// typeof target === &amp;#34;number&amp;#34;

const search = (nums, target) =&amp;gt; {

  let left = 0
  let right = nums.length - 1

  while (left &amp;lt;= right) {
    const mid = left &amp;#43; Math.ceil((right - left) / 2)
    if (nums[mid] === target) return mid

    if (nums[left] &amp;lt; nums[mid]) {
      if (nums[left] &amp;lt;= target &amp;amp;&amp;amp; target &amp;lt; nums[mid]) {
        right = mid - 1
      } else {
        left = mid &amp;#43; 1
      }
    } else {
      if (nums[mid] &amp;lt; target &amp;amp;&amp;amp; target &amp;lt;= nums[right]) {
        left = mid &amp;#43; 1
      } else {
        right = mid - 1
      }
    }
  }

  return -1

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To create a structure that can store multiple values at a &lt;code&gt;key&lt;/code&gt; and return a given one based on &lt;code&gt;timestamp&lt;/code&gt;, set the data structure variable to a &lt;code&gt;function () {}&lt;/code&gt; that contains &lt;code&gt;this.map&lt;/code&gt;. Adding &lt;code&gt;set&lt;/code&gt; to the prototype of the data structure, involves checking if the &lt;code&gt;key&lt;/code&gt; already exists on the &lt;code&gt;map&lt;/code&gt;. If not, set the &lt;code&gt;key&lt;/code&gt; to an empty array. Otherwise, push an object with the &lt;code&gt;timestamp&lt;/code&gt; and &lt;code&gt;value&lt;/code&gt; to the &lt;code&gt;key&lt;/code&gt;. Adding &lt;code&gt;get&lt;/code&gt; can involve binary search. If the key does not exist, you can return an empty string, else get the &lt;code&gt;timestamps&lt;/code&gt; array to be searched off of the key, and set &lt;code&gt;left&lt;/code&gt; and &lt;code&gt;right&lt;/code&gt; to the edges of the array. While &lt;code&gt;left&lt;/code&gt; is less than &lt;code&gt;right&lt;/code&gt;, compute &lt;code&gt;mid&lt;/code&gt;, and check if the &lt;code&gt;timestamp&lt;/code&gt; at &lt;code&gt;mid&lt;/code&gt; is less than the &lt;code&gt;timestamp&lt;/code&gt; coming from the &lt;code&gt;set&lt;/code&gt; argument. If so, set &lt;code&gt;left&lt;/code&gt; to &lt;code&gt;mid+1&lt;/code&gt;, else &lt;code&gt;right&lt;/code&gt; can be &lt;code&gt;mid&lt;/code&gt;. When the pointer cross, check if &lt;code&gt;timestamp&lt;/code&gt; at &lt;code&gt;left&lt;/code&gt; is less than or equal to the &lt;code&gt;timestamp&lt;/code&gt; coming from the &lt;code&gt;set&lt;/code&gt; argument. If so, return its &lt;code&gt;value&lt;/code&gt;. Otherwise, do the same for &lt;code&gt;left-1&lt;/code&gt;, and if a &lt;code&gt;timestamp&lt;/code&gt; there also does not work, you can return an empty string.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;274931586&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;274931586&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/time-based-key-value-store/&#34;
  
  target=&#34;_blank&#34;
  &gt;Time Based Key-Value Store&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof timestamp === &amp;#34;number&amp;#34;

const TimeMap = function() {
  this.map = new Map()
}

TimeMap.prototype.set = function(key, value, timestamp) {
  if (!this.map.has(key)) this.map.set(key, [])
  this.map.get(key).push({ timestamp, value })
}

TimeMap.prototype.get = function(key, timestamp) {
    if (!this.map.has(key)) return &amp;#34;&amp;#34;
    const timestamps = this.map.get(key)
    let left = 0
    let right = timestamps.length - 1
    
    while (left &amp;lt; right) {
        const mid = left &amp;#43; Math.floor((right - left) / 2)
        if (timestamps[mid].timestamp &amp;lt; timestamp) {
            left = mid &amp;#43; 1
        } else {
            right = mid
        }
    }
    
    if (timestamps[left] &amp;amp;&amp;amp; timestamps[left].timestamp &amp;lt;= timestamp) return timestamps[left].value
    if (timestamps[left - 1] &amp;amp;&amp;amp; timestamps[left - 1].timestamp &amp;lt;= timestamp) return timestamps[left - 1].value

    return &amp;#34;&amp;#34;
}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find if a number exists in a matrix of ordered numbers such that each row is sorted in ascending order and rows that have greater indicies contain greater values, two rounds of binary search can be conducted. First, find the row that would contain the number by using the maximum and minimum row indicies as &lt;code&gt;yRight&lt;/code&gt; and &lt;code&gt;yLeft&lt;/code&gt;. Test if the value at the end of the row of a floored &lt;code&gt;mid&lt;/code&gt; is less than the target to increase &lt;code&gt;yLeft&lt;/code&gt; to &lt;code&gt;mid+1&lt;/code&gt;, else &lt;code&gt;yRight&lt;/code&gt; becomes &lt;code&gt;mid&lt;/code&gt;. Once the candidate row has been determined, you can return &lt;code&gt;false&lt;/code&gt; immediately if the target value is outside the bounds of the row. Else, conduct a second binary search on the row. If the search is narrowed down the a single candidate that is not the target value, return &lt;code&gt;false&lt;/code&gt;, else return &lt;code&gt;true&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;382159674&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;382159674&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/search-a-2d-matrix/&#34;
  
  target=&#34;_blank&#34;
  &gt;Search a 2D Matrix&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof matrix === &amp;#34;object&amp;#34; (array of arrays of numbers)
// typeof target === &amp;#34;number&amp;#34;

const searchMatrix = (matrix, target) =&amp;gt; {

    let yLeft = 0
    let yRight = matrix.length - 1

    while (yLeft &amp;lt; yRight){
        const mid = yLeft &amp;#43; Math.floor((yRight-yLeft)/2)
        if (matrix[mid][matrix[0].length-1] &amp;lt; target){
            yLeft = mid &amp;#43; 1
        } else {
            yRight = mid
        }
    }
    
    if (target &amp;gt; matrix[yLeft][matrix[0].length-1] || target &amp;lt; matrix[yLeft][0]) return false
    
    let xLeft = 0
    let xRight = matrix[0].length - 1

    while(xLeft &amp;lt; xRight){
        const mid = xLeft &amp;#43; Math.floor((xRight-xLeft)/2)
        if (matrix[yLeft][mid] &amp;lt; target){
            xLeft = mid &amp;#43; 1
        } else {
            xRight = mid
        }
    }

    return matrix[yLeft][xLeft] === target

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find the minimum in a rotated sorted array of unique values, you can start by noticing that if you divide the array repeatedly into halves, if the value at &lt;code&gt;mid&lt;/code&gt; in a binary search is ever greater than the value at &lt;code&gt;right&lt;/code&gt;, the left side of the search space is sorted and the right side is not. This means that you can move the &lt;code&gt;left&lt;/code&gt; to &lt;code&gt;mid+1&lt;/code&gt;. Otherwise, you can move the &lt;code&gt;right&lt;/code&gt; to &lt;code&gt;mid&lt;/code&gt;. When the &lt;code&gt;left&lt;/code&gt; and &lt;code&gt;right&lt;/code&gt; pointers intersect the value at either pointer is the lowest.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;732691584&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;732691584&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/find-minimum-in-rotated-sorted-array/&#34;
  
  target=&#34;_blank&#34;
  &gt;Find Minimum in Rotated Sorted Array&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof nums === &amp;#34;object&amp;#34; (array of numbers)

const findMin = (nums) =&amp;gt; {

  let left = 0
  let right = nums.length - 1

  while (left &amp;lt; right) {
    const mid = left &amp;#43; Math.floor((right-left)/2)
    if (nums[mid] &amp;gt; nums[right]){
        left = mid &amp;#43; 1
    } else {
        right = mid
    }
  }

  return nums[left]
  
}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find the maximum profit when given job start times, end times, and the profit for each, one way to start is to organize all of these inputs into an array of arrays with three numbers, and then sort this &lt;code&gt;jobs&lt;/code&gt; array by end time from least to greatest. Next, a &lt;code&gt;dp&lt;/code&gt; array can be started as an array with a single value, the profit of the job sorted to end first. Every other location in the &lt;code&gt;jobs&lt;/code&gt; array can then be iterated. For each step in this loop, first save the &lt;code&gt;profit&lt;/code&gt; at the current location. Then use binary search on the portion of the &lt;code&gt;jobs&lt;/code&gt; array to the left of the current location in the iteration. You can stop the binary search when the pointers cross, setting &lt;code&gt;left&lt;/code&gt; to &lt;code&gt;mid+1&lt;/code&gt; whenever the end of the job at &lt;code&gt;mid&lt;/code&gt; is less than or equal to the start of the job being pointed to in the outer loop, and setting &lt;code&gt;right&lt;/code&gt; to &lt;code&gt;mid-1&lt;/code&gt; otherwise. This means that the &lt;code&gt;left&lt;/code&gt; pointer might point to a job that overlaps with the one in the outer iteration, so when the binary search is done, the &lt;code&gt;right&lt;/code&gt; pointer will be pointing to the leftmost job that does not overlap with the one indicated in the iteration. After the binary search, if &lt;code&gt;right&lt;/code&gt; is not -1, you can update &lt;code&gt;profit&lt;/code&gt; by the value in the &lt;code&gt;dp&lt;/code&gt; location indicated by &lt;code&gt;right&lt;/code&gt; (the greatest profit counted previously that does not overlap with the profit in the current location). Finally for this step in the loop, update the &lt;code&gt;dp&lt;/code&gt; location being iterated to the best of either the &lt;code&gt;profit&lt;/code&gt; or the value in &lt;code&gt;dp&lt;/code&gt; one index lower. At the end of the iteration, return the rightmost value in &lt;code&gt;dp&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;298354716&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;298354716&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/maximum-profit-in-job-scheduling/&#34;
  
  target=&#34;_blank&#34;
  &gt;Maximum Profit in Job Scheduling&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof startTime === &amp;#34;object&amp;#34; (array of numbers)
// typeof endTime === &amp;#34;object&amp;#34; (array of numbers)
// typeof profit === &amp;#34;object&amp;#34; (array of numbers)

const jobScheduling = (startTime, endTime, profit) =&amp;gt; {

    const jobs = []
    for (let i = 0; i &amp;lt; startTime.length; i&amp;#43;&amp;#43;) {
        jobs.push([startTime[i], endTime[i], profit[i]])
    }
    jobs.sort((a,b)=&amp;gt;a[1]-b[1])

    const dp = [jobs[0][2]]

    for (let i = 1; i &amp;lt; jobs.length; i&amp;#43;&amp;#43;) {
        let profit = jobs[i][2]
    
        let left = 0
        let right = i - 1
        let mid = left &amp;#43; Math.floor(right-left)
        while(left &amp;lt;= right){
            mid = left &amp;#43; Math.floor(right-left)
            if(jobs[mid][1] &amp;lt;= jobs[i][0]){
                left = mid &amp;#43; 1
            } else {
                right = mid - 1
            }
        }

        if (right != -1) profit &amp;#43;= dp[right]
        dp[i] = Math.max(profit, dp[i-1])
    }
    
    return dp[jobs.length-1]

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;h1 id=&#34;graphs&#34;&gt;Graphs&lt;/h1&gt;
&lt;p&gt;A two-dimensional array can represent a screen of pixels. To represent the &amp;ldquo;flood fill&amp;rdquo; tool in a paint editor, identify the coordinates at which you wish to begin flood fill. If that location is already the desired color, stop the process. Otherwise, you can begin a depth-first search at that location, replacing its color with the target color, and generating recursive calls up, down, left, and right. In each of these recursive calls, if the coordinates are out of bounds or the color is not the color being overwritten, handle this base case by not recursing further from that point. Otherwise, change the color at the location and generate more recursive calls up, down, left, and right.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;459281367&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;459281367&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/flood-fill/&#34;
  
  target=&#34;_blank&#34;
  &gt;Flood Fill&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof nums === &amp;#34;object&amp;#34; (array of numbers)
// typeof target === &amp;#34;number&amp;#34;

const floodFill = (image, sr, sc, color) =&amp;gt; {

    const start = image[sr][sc]
    if (start === color) return image

    const go = (r,c) =&amp;gt; {
        if(r &amp;lt; 0 || c &amp;lt; 0 || r === image.length || c === image[0].length || image[r][c] !== start) return
        image[r][c] = color
        go(r&amp;#43;1,c)
        go(r-1,c)
        go(r,c-1)
        go(r,c&amp;#43;1)
    }

    go(sr,sc)
    return image

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;How to modify a binary matrix to return the closest 0 for each cell? This is a problem that can be solved with breadth-first search. After creating a &lt;code&gt;queue&lt;/code&gt;, a triplet with &lt;code&gt;row&lt;/code&gt;, &lt;code&gt;column&lt;/code&gt;, and &lt;code&gt;distance&lt;/code&gt; from a 0 can be pushed to the &lt;code&gt;queue&lt;/code&gt; for every 0 found in the array (so all distances start as 0). Every other value can be set as Infinity to make it clear which cells have not yet been processed. Then, while the &lt;code&gt;queue&lt;/code&gt; has a length, items can be shifted off the front, the cells they point to can be updated with a &lt;code&gt;distance&lt;/code&gt;, and new items can be pushed to the &lt;code&gt;queue&lt;/code&gt; and processed with distance + 1 and coordinates just up, down, left, and right (but not out of bounds). After shifting, do not further process a queue item if has a non-infinity value at the time it is shifted in.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;984753612&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;984753612&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/01-matrix/&#34;
  
  target=&#34;_blank&#34;
  &gt;01 Matrix&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof mat === &amp;#34;object&amp;#34; (array of arrays of numbers)

const updateMatrix = (mat) =&amp;gt; {
        
    const queue = []
    
    for(let row = 0; row &amp;lt; mat.length; row&amp;#43;&amp;#43;) {
        for(let column = 0; column &amp;lt; mat[0].length; column&amp;#43;&amp;#43;) {

            if(!mat[row][column]){
                queue.push([row, column, 0])
            }
            mat[row][column] = Infinity
        }
    }

    const directions = [[-1, 0], [0, -1], [1, 0], [0, 1]]
    
    while(queue.length) {
        let [row, column, distance] = queue.shift()
        if(mat[row][column] !== Infinity) continue
        
        mat[row][column] = distance
        
        for(let [x, y] of directions) {
            let newRow = x &amp;#43; row;
            let newColumn = y &amp;#43; column;
            
            if(newRow &amp;gt;= 0 &amp;amp;&amp;amp; newRow &amp;lt; mat.length &amp;amp;&amp;amp; newColumn &amp;gt;= 0 &amp;amp;&amp;amp;
            newColumn &amp;lt; mat[0].length &amp;amp;&amp;amp; mat[newRow][newColumn] === Infinity) {
                    queue.push([newRow, newColumn, distance &amp;#43; 1])
            }
        }
    }
    return mat

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To clone a graph of unique values without loops, if the graph exists, you can use recursion with a map. If any value is present in the map you can return whatever is stored there. Otherwise, make a copy of &lt;code&gt;currentNode&lt;/code&gt;, set it by value in the map, and loop the reference to its children to recursively copy these.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;361485792&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;361485792&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/clone-graph/&#34;
  
  target=&#34;_blank&#34;
  &gt;Clone Graph&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// function Node(val, neighbors) {
//     this.val = val === undefined ? 0 : val;
//     this.neighbors = neighbors === undefined ? [] : neighbors;
//  }

const cloneGraph = (node) =&amp;gt; {
    if (!node) return null
    const seen = new Map()

    const copy = (currentNode) =&amp;gt; {
        if(seen.has(currentNode.val)) return seen.get(currentNode.val)
        const copyNode = new Node(currentNode.val, [])

        seen.set(copyNode.val, copyNode)
        for (let neighbor of currentNode.neighbors) {
            copyNode.neighbors.push(copy(neighbor))
        }

        return copyNode

    }

    return copy(node)
    
}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To detect if every course can be taken when prerequisites are given in an array of arrays of number pairs that look like &lt;code&gt;[course, prerequisite]&lt;/code&gt;, an adjacency list can be created that lets all courses under a prerequisite be pointed to from that prerequisite. Then, every course can be investigated to see if any of its descendants (from the perspective of adjacency list lookups) contain its value. This would mean a circular dependency and be the case to return &lt;code&gt;false&lt;/code&gt;. In the depth-first searches starting from each course, caching previously seen course values prevents duplicate work, and just before any course&amp;rsquo;s descendants are investigated, it makes sense to store a &lt;code&gt;flag&lt;/code&gt; with that course&amp;rsquo;s number, so that lower calls can check their value against the set of flags to see if returning &lt;code&gt;false&lt;/code&gt; is needed. On any level of the recursive process, once the lower-level calls are completed, the &lt;code&gt;flag&lt;/code&gt; should be removed, since it would no longer represent a valid ancestor.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;298435671&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;298435671&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/course-schedule/&#34;
  
  target=&#34;_blank&#34;
  &gt;Course Schedule&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof numCourses === &amp;#34;number&amp;#34;
// typeof prerequisites === &amp;#34;object&amp;#34; (array of arrays with 2 numbers)

const canFinish(numCourses, prerequisites) {

  const cache = new Set()
  const flag = new Set()
  const adjacencyList = Array(numCourses).fill().map(r =&amp;gt; [])
  
  for (let [course, prerequisite] of prerequisites) {
    adjacencyList[prerequisite].push(course)
  }
  
  const dfs = (course) =&amp;gt; {
    if (cache.has(course)) return true
    if (flag.has(course)) return false
    
    flag.add(course)
    for (let nextCourse of adjacencyList[course]) {
      if (!dfs(nextCourse)) return false
    }
    flag.delete(course)
    cache.add(course)
    return true
  }

  for (let i = 0; i &amp;lt; numCourses; i&amp;#43;&amp;#43;) {
    if (!dfs(i)) return false
  }

  return true
  
}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;In order to count the number of islands in a grid (unique groups of &amp;ldquo;1&amp;quot;s connected up, down, left, or right to other &amp;ldquo;1&amp;quot;s), an approach involves iterating through all the values of the grid. When a &amp;lsquo;1&amp;rsquo; is found, increment a &lt;code&gt;count&lt;/code&gt; variable, then run a &lt;code&gt;flood&lt;/code&gt; function to check if the location is in bounds and a &amp;ldquo;1&amp;rdquo;. If so, &amp;ldquo;sink&amp;rdquo; the space by assigning a &amp;ldquo;0&amp;rdquo;, then do depth-first search and run &lt;code&gt;flood&lt;/code&gt; up, down, left, and right. Every connected &amp;ldquo;1&amp;rdquo; should disappear the first time one is found in the looping.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;297156843&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;297156843&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/number-of-islands/&#34;
  
  target=&#34;_blank&#34;
  &gt;Number of Islands&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof grid === &amp;#34;object&amp;#34; (array of arrays of numbers)

const numIslands = (grid) =&amp;gt; {

  let count = 0
  
  const flood = (i, j) =&amp;gt; {
      if (i &amp;lt; 0 || j &amp;lt; 0 || i === grid.length || 
      j === grid[0].length || grid[i][j] === &amp;#39;0&amp;#39;) return
      grid[i][j] = &amp;#39;0&amp;#39;
      const directions = [[0,1],[0,-1],[1,0],[-1,0]]
      for (let [y, x] of directions) flood(i&amp;#43;y,j&amp;#43;x)
  }
  
  for (let i = 0; i &amp;lt; grid.length; i&amp;#43;&amp;#43;){
      for (let j = 0; j &amp;lt; grid[0].length; j&amp;#43;&amp;#43;){
          if (grid[i][j] === &amp;#39;1&amp;#39;) {
              count&amp;#43;&amp;#43;
              flood(i,j)
          }
      }
  }
    
    return count

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;In a grid where &lt;code&gt;1&lt;/code&gt; represents a fresh orange, &lt;code&gt;2&lt;/code&gt; represents a rotten orange, &lt;code&gt;0&lt;/code&gt; represents an empty space, and every minute every fresh orange next to a rotten orange beomes rotten, how many minutes, if possible, does it take for all oranges to turn rotten? This problem can be solved with breadth-first search. First, iterate the values in the grid to count the number of oranges into a &lt;code&gt;oranges&lt;/code&gt; variable. In the same iteration, for all rotten oranges, push arrays of 3 numbers each into a &lt;code&gt;queue&lt;/code&gt;&amp;ndash; the orange&amp;rsquo;s coordinates, and and a &lt;code&gt;minutesToRotten&lt;/code&gt; of 0. Temporarily assign rotten oranges in the grid as &lt;code&gt;1&lt;/code&gt;s to make &lt;code&gt;queue&lt;/code&gt; processing easier. For &lt;code&gt;queue&lt;/code&gt; processing, as long as &lt;code&gt;queue&lt;/code&gt; has a length, shift off the first item, make sure its coordinates are on the grid and point to a fresh &lt;code&gt;1&lt;/code&gt; orange, then decrease the &lt;code&gt;oranges&lt;/code&gt; counter by 1, assign the grid location as &lt;code&gt;2&lt;/code&gt;, maximize a &lt;code&gt;depth&lt;/code&gt; variable by comparing it with &lt;code&gt;minutesToRotten&lt;/code&gt;, and push new &lt;code&gt;queue&lt;/code&gt; triplets up, down, left, and right if those are the adjacencies allowed by the question, with the new &lt;code&gt;minutesToRotten&lt;/code&gt; increased &lt;code&gt;+1&lt;/code&gt; compared to the spawning item. When &lt;code&gt;queue&lt;/code&gt; has no length, return &lt;code&gt;depth&lt;/code&gt; if the &lt;code&gt;oranges&lt;/code&gt; variable is 0, else return &lt;code&gt;-1&lt;/code&gt; to show that all oranges will never become rotten under the constraints.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;359472186&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;359472186&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/rotting-oranges/&#34;
  
  target=&#34;_blank&#34;
  &gt;Rotting Oranges&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof grid === &amp;#34;object&amp;#34; (array of arrays of numbers)

const orangesRotting = function(grid) {

    let oranges = 0
    let depth = 0
    const queue = []
    for (let i = 0; i &amp;lt; grid.length; i&amp;#43;&amp;#43;){
        for (let j = 0; j &amp;lt; grid[0].length; j&amp;#43;&amp;#43;){
            if(grid[i][j]) oranges&amp;#43;&amp;#43;
            if(grid[i][j] === 2){
                grid[i][j] = 1
                queue.push([i,j,0])
            }
        }
    }

    const dirs = [[-1,0],[1,0],[0,1],[0,-1]]
    while(queue.length){
        const cur = queue.shift()
        if(cur[0] &amp;lt; 0 || cur[1] &amp;lt; 0 || cur[0] === grid.length
          || cur[1] === grid[0].length || grid[cur[0]][cur[1]] !== 1){
            continue
        }
        depth = Math.max(depth,cur[2])
        oranges--
        grid[cur[0]][cur[1]] = 2
        for (let dir of dirs){
            queue.push([cur[0]&amp;#43;dir[0],cur[1]&amp;#43;dir[1],cur[2]&amp;#43;1])
        }
    }
    
    return oranges ? -1 : depth

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;Given an array of &lt;code&gt;account&lt;/code&gt; arrays where the item at each inner array&amp;rsquo;s first index is a &lt;code&gt;name&lt;/code&gt;, and every other index contains an &lt;code&gt;email&lt;/code&gt;, how to merge &lt;code&gt;accounts&lt;/code&gt; with shared emails? Two arrays might be connected by a third if that third shares emails on both other lists, and the chain can get even longer. In order to solve, first build a &lt;code&gt;nameLookup&lt;/code&gt; and &lt;code&gt;adjacencyList&lt;/code&gt; by looping through each starting &lt;code&gt;account&lt;/code&gt;, extracting the &lt;code&gt;name&lt;/code&gt; from the 0th index, and at each step looping through the emails associated with each starting &lt;code&gt;account&lt;/code&gt;. Assign a &lt;code&gt;name&lt;/code&gt; to each &lt;code&gt;email&lt;/code&gt; in &lt;code&gt;nameLookup&lt;/code&gt;, and use the &lt;code&gt;email&lt;/code&gt; at the 1st index to create adjacency relationships &amp;ndash; adding each 2nd and further index email to an &lt;code&gt;adjacencyList&lt;/code&gt; value array of the 1st index &lt;code&gt;email&lt;/code&gt; key, and adding the 1st index &lt;code&gt;email&lt;/code&gt; to the &lt;code&gt;adjacencyList&lt;/code&gt; array of every other email in the &lt;code&gt;account&lt;/code&gt;. Reorganization complete, &lt;code&gt;email&lt;/code&gt; keys in the &lt;code&gt;adjacencyList&lt;/code&gt; can then be looped. For any &lt;code&gt;email&lt;/code&gt; that has not been &lt;code&gt;seen&lt;/code&gt; in this phase of the algorithm, a depth-first search can be run, first adding the &lt;code&gt;email&lt;/code&gt; to &lt;code&gt;seen&lt;/code&gt;, then seeding a new &lt;code&gt;emails&lt;/code&gt; array with the &lt;code&gt;email&lt;/code&gt; key being processed, then looping items in the array associated with the &lt;code&gt;email&lt;/code&gt; in the &lt;code&gt;adjacencyList&lt;/code&gt;. For every next email on this list that has not been &lt;code&gt;seen&lt;/code&gt;, an additional &lt;code&gt;dfs&lt;/code&gt; can run, and the results can be pushed to the &lt;code&gt;emails&lt;/code&gt; array before returning it. When &lt;code&gt;dfs&lt;/code&gt; is done, the &lt;code&gt;emails&lt;/code&gt; array returned to the outer &lt;code&gt;adjacencyList&lt;/code&gt; loop is a &lt;code&gt;joinedAccount&lt;/code&gt;. Sort as needed, and use &lt;code&gt;nameLookup&lt;/code&gt; to add a &lt;code&gt;name&lt;/code&gt; to the front of the array before pushing it to &lt;code&gt;result&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;721563849&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;721563849&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/accounts-merge/&#34;
  
  target=&#34;_blank&#34;
  &gt;Accounts Merge&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof accounts === &amp;#34;object&amp;#34; (array of arrays of strings)

const accountsMerge = (accounts) =&amp;gt; {  
    
    const nameLookup = {}
    const adjacencyList = {}
    
    for (let account of accounts) {
        const name = account[0]
        for (let i = 1; i &amp;lt; account.length; i&amp;#43;&amp;#43;) {
            nameLookup[account[i]] = name
            if (!adjacencyList[account[i]]) adjacencyList[account[i]] = []

            if (i != 1) {
                adjacencyList[account[1]].push(account[i])
                adjacencyList[account[i]].push(account[1])
            }
        }
    }
    
    const result = []
    const seen = new Set()
    
    const dfs = (email) =&amp;gt; {
        seen.add(email)
        const emails = [email]
        for (let nextEmail of adjacencyList[email]){
            if(!seen.has(nextEmail)) emails.push(...dfs(nextEmail))
        }        
        return emails
    }
    
    for (let email in adjacencyList) {
        if (!seen.has(email)) {
            const joinedAccount = dfs(email)
            joinedAccount.sort()
            result.push([nameLookup[joinedAccount[0]], ...joinedAccount])
        }
    }
    
    return result
}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;How to find if a word exists in a grid of characters when letters of the word can be connected up, down, left, or right? You can iterate through every space on the board. For every space, run a helper function &lt;code&gt;go&lt;/code&gt; taking the coordinates and a &lt;code&gt;letterIndex&lt;/code&gt; variable starting at &lt;code&gt;0&lt;/code&gt; as arguments. The &lt;code&gt;letterIndex&lt;/code&gt; represents the index of the next letter in the target word that must match. Inside &lt;code&gt;go&lt;/code&gt;, return &lt;code&gt;false&lt;/code&gt; if the letter at the coordinates does not match the letter at the &lt;code&gt;letterIndex&lt;/code&gt; of the word. Then, if we have reached the end of the word, return &lt;code&gt;true&lt;/code&gt;. Otherwise, temporarily overwrite the coordinates on the grid with a nonletter character, and run recrsive calls of &lt;code&gt;go&lt;/code&gt; up, down, left, and right on valid coordinates and the next &lt;code&gt;letterIndex&lt;/code&gt;. If any of these return &lt;code&gt;true&lt;/code&gt; the parent &lt;code&gt;go&lt;/code&gt; function can also return &lt;code&gt;true&lt;/code&gt;. Else restore the letter at the current grid coordinates and return &lt;code&gt;false&lt;/code&gt;. This is backtracking&amp;ndash;you only need one &lt;code&gt;true&lt;/code&gt; path to return true overall, otherwise you can &amp;ldquo;pack up&amp;rdquo; the current path and try an adjacent path. If any &lt;code&gt;go&lt;/code&gt; in the original grid iteration returns &lt;code&gt;true&lt;/code&gt;, the overall function can return &lt;code&gt;true&lt;/code&gt;, otherwise return &lt;code&gt;false&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;827314695&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;827314695&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/word-search/description/&#34;
  
  target=&#34;_blank&#34;
  &gt;Word Search&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof board === &amp;#34;object&amp;#34; (array of arrays of strings of 1 character)
// typeof word === &amp;#34;string&amp;#34;

const exist = (board, word) =&amp;gt; {

  const directions = [[-1, 0], [1, 0], [0, 1], [0, -1]]
  const go = (i, j, letterIndex) =&amp;gt; {
    if (board[i][j] !== word[letterIndex]) return false
    if (letterIndex === word.length - 1) return true

    board[i][j] = &amp;#34;.&amp;#34;
    for (const [x, y] of directions) {
      const nextI = i &amp;#43; x
      const nextJ = j &amp;#43; y
      if (nextI &amp;gt;= 0 &amp;amp;&amp;amp; nextI &amp;lt; board.length &amp;amp;&amp;amp; nextJ &amp;gt;= 0 &amp;amp;&amp;amp; nextJ &amp;lt; board[0].length) {
        if (go(nextI, nextJ, letterIndex &amp;#43; 1)) return true
      }
    }
    board[i][j] = word[letterIndex]
    return false
  }

  for (let i = 0; i &amp;lt; board.length; i&amp;#43;&amp;#43;) {
    for (let j = 0; j &amp;lt; board[0].length; j&amp;#43;&amp;#43;) {
      if (go(i, j, 0)) return true
    }
  }

  return false

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find all roots of trees of minimum height from a connected components graph with consistant connections that may be &amp;ldquo;bent&amp;rdquo; to start from any node, notice that a list of responses will always be the most &amp;ldquo;central.&amp;rdquo; To solve, run a number of rounds where each round removes nodes that only have one connection. The last round of pruning should contain valid root nodes. Notice than whenever there are less than 3 nodes, every node can be a root node. Otherwise, create a &lt;code&gt;connectionCounts&lt;/code&gt; array with length equal to the number of nodes, filled to start with 0s, and a &lt;code&gt;connectionLists&lt;/code&gt; array of the same length filled with empty arrays. For each edge pair connecting two nodes, increment each node index by 1 in  &lt;code&gt;connectionCounts&lt;/code&gt;, and push the opposite to each node index in &lt;code&gt;connectionLists&lt;/code&gt;. Now, create another array &lt;code&gt;seen&lt;/code&gt; with length equal to the number of nodes, and fill it with false, create a &lt;code&gt;seenCount&lt;/code&gt; starting as 0, and an empty array as a &lt;code&gt;queue&lt;/code&gt;. For every &lt;code&gt;connectionCount&lt;/code&gt; of 1, flip the &lt;code&gt;seen&lt;/code&gt; boolean to true at that location, increment &lt;code&gt;seenCount&lt;/code&gt; by 1, and push the index to the &lt;code&gt;queue&lt;/code&gt;. For the final part of the algorithm, repeatedly process the &lt;code&gt;queue&lt;/code&gt;. Create a &lt;code&gt;nextQueue&lt;/code&gt; at the start of each processing round, and loop the nodes in the queue. For each node, loop the &lt;code&gt;adjacent&lt;/code&gt; nodes. Ignore the &lt;code&gt;adjacent&lt;/code&gt; node if it has been &lt;code&gt;seen&lt;/code&gt;. Otherwise, decrease its &lt;code&gt;connectionCount&lt;/code&gt; by 1, then check if this value is 1. If so, it is now a leaf at the edge of the graph, so mark it as &lt;code&gt;seen&lt;/code&gt;, increment the &lt;code&gt;seenCount&lt;/code&gt;, and push it to the &lt;code&gt;nextQueue&lt;/code&gt;. Once the &lt;code&gt;queue&lt;/code&gt; is processed, if &lt;code&gt;seenCount&lt;/code&gt; equals the total number of nodes, return &lt;code&gt;nextQueue&lt;/code&gt;, else replace &lt;code&gt;queue&lt;/code&gt; with &lt;code&gt;nextQueue&lt;/code&gt; and start the next round of &lt;code&gt;queue&lt;/code&gt; processing.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;357214689&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;357214689&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/minimum-height-trees/&#34;
  
  target=&#34;_blank&#34;
  &gt;Minimum Height Trees&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof n === &amp;#34;number&amp;#34;
// typeof edges === &amp;#34;object&amp;#34; (array of arrays with 2 numbers)

const findMinHeightTrees = (n, edges) =&amp;gt; {

    if (n &amp;lt; 3) return [...Array(n)].map((_,i)=&amp;gt; i)
    
    const connectionCounts = Array(n).fill(0)
    const connectionLists = [...Array(n)].map(() =&amp;gt; [])
    
    for(let [node1, node2] of edges) {
        connectionCounts[node1]&amp;#43;&amp;#43;
        connectionLists[node1].push(node2)
        connectionCounts[node2]&amp;#43;&amp;#43;
        connectionLists[node2].push(node1)
    }
    
    const seen = Array(n).fill(false)
    let seenCount = 0
    let queue = []

    for(let i = 0; i &amp;lt; n; i&amp;#43;&amp;#43;) {
        if(connectionCounts[i] === 1) {
            seen[i] = true
            seenCount&amp;#43;&amp;#43;
            queue.push(i)
        }
    }
    
    while(true) {
        const nextQueue = []
        
        for(let node of queue) {
            for(let adjacent of connectionLists[node]) {
                if(seen[adjacent]) continue
                if(--connectionCounts[adjacent] === 1) {
                    seen[adjacent] = true
                    seenCount&amp;#43;&amp;#43;
                    nextQueue.push(adjacent)
                }
            }
        }
        if(seenCount === n) return nextQueue       
        queue = nextQueue
    }
}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find a list of coordinates on a grid where a downwards slope (up, down, left, and right) can be found that reaches both the upper or left edges of the grid, as well as the lower or right (which can be thought of as water flowing to two oceans), consider depth-first search. Create two new grids the same shappe as the original grid, and fill both with &lt;code&gt;false&lt;/code&gt;. Then loop the original array. At every location on the upper or left edge of the graph, begin a depth-first search process that places &lt;code&gt;true&lt;/code&gt; at that location in one of the helper grids, and then continues planting &lt;code&gt;true&lt;/code&gt; in every direction up, down, left, or right direction until an edge is reached, another &lt;code&gt;true&lt;/code&gt; is reached, or a value in the given direction on the original grid is less than the original gird value at the location that is trying to be reached. (This would indicate the slope in the given direction is not sloping down towards the upper or left sides of the grid.) Fill the other helper grid by doing the indentical process spawning fom the bottom or the right side of the grid. Finally, add to a &lt;code&gt;results&lt;/code&gt; array every location that is &lt;code&gt;true&lt;/code&gt; on both helper grids, and return &lt;code&gt;results&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;942735168&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;942735168&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/pacific-atlantic-water-flow/&#34;
  
  target=&#34;_blank&#34;
  &gt;Pacific Atlantic Water Flow&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof grid === &amp;#34;object&amp;#34; (array of arrays of numbers)

const pacificAtlantic = (heights) =&amp;gt; {

    const Atlantic = [...Array(heights.length)].map(() =&amp;gt; new Array(heights[0].length).fill(false))
    const Pacific = [...Array(heights.length)].map(() =&amp;gt; new Array(heights[0].length).fill(false))
    const directions = [[1, 0], [-1, 0], [0, -1], [0, 1]]

    const dfs = (i, j, seen) =&amp;gt; {
        seen[i][j] = true
        for (let direction of directions) {
            const newI = i &amp;#43; direction[0]
            const newJ = j &amp;#43; direction[1]
            if (newI &amp;lt; 0 || newJ &amp;lt; 0 || newI === heights.length || newJ === heights[0].length || seen[newI][newJ]) continue
            if (heights[newI][newJ] &amp;gt;= heights[i][j]) {
                dfs(newI, newJ, seen)
            }
        }
    }
    
    for (let i = 0; i &amp;lt; heights.length; i&amp;#43;&amp;#43;) {
        for (let j = 0; j &amp;lt; heights[0].length; j&amp;#43;&amp;#43;) {
            if (i === 0 || j === 0) {
                dfs(i, j, Pacific)
            }
            if (i === heights.length - 1 || j === heights[0].length - 1) {
                dfs(i, j, Atlantic)
            }
        }
    }
    const results = []
    for (let i = 0; i &amp;lt; heights.length; i&amp;#43;&amp;#43;) {
        for (let j = 0; j &amp;lt; heights[0].length; j&amp;#43;&amp;#43;) {
            if (Pacific[i][j] &amp;amp;&amp;amp; Atlantic[i][j]) {
                results.push([i, j])
            }
        }
    }
    return results

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;In a grid that contains a start location, food, empty spaces, and obstacles, what is the shortest number of steps to any food cell? You can use breadth-first search starting at the start location and pushing new legal adjacent locations (not off the board or previously seen) until a food is seen. If you keep a counter of number of steps away from the origin, you can return this when you reach the food symbol. If you never do you can return &lt;code&gt;-1&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;967314528&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;967314528&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/shortest-path-to-get-food/&#34;
  
  target=&#34;_blank&#34;
  &gt;Shortest Path to Get Food&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof grid === &amp;#34;object&amp;#34; (array of arrays of 1 character)

const getFood = (grid) =&amp;gt; {

    const queue = []
    
    for(let row = 0; row &amp;lt; grid.length; row&amp;#43;&amp;#43;) {
        for(let column = 0; column &amp;lt; grid[0].length; column&amp;#43;&amp;#43;) {
            if(grid[row][column] === &amp;#34;*&amp;#34;){
                queue.push([row, column, 0])
                break
            }
        }
    }

    const directions = [[-1, 0], [0, -1], [1, 0], [0, 1]]
    const seen = new Set()
    
    while(queue.length) {
        let [row, column, distance] = queue.shift()
        if(grid[row][column] === &amp;#34;#&amp;#34;) return distance
        const lookup =`${row}&amp;#43;${column}`
        if(seen.has(lookup) continue
        seen.add(lookup)
                
        for(let [x, y] of directions) {
            let newRow = x &amp;#43; row;
            let newColumn = y &amp;#43; column;
            
            if(newRow &amp;gt;= 0 &amp;amp;&amp;amp; newRow &amp;lt; grid.length &amp;amp;&amp;amp; newColumn &amp;gt;= 0 &amp;amp;&amp;amp;
            newColumn &amp;lt; grid[0].length &amp;amp;&amp;amp; grid[newRow][newColumn] !== &amp;#34;X&amp;#34;) {
                    queue.push([newRow, newColumn, distance &amp;#43; 1])
            }
        }
    }

    return -1

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;Given a list of edges (connected node pairs), does the collection make up a valid tree? In order to determine this, you can turn the edge list into an &lt;code&gt;adjacencyList&lt;/code&gt;, and then run a depth-first search on the &lt;code&gt;adjacencyList&lt;/code&gt;, starting from an arbitrary node. Add each node to a &lt;code&gt;seen&lt;/code&gt; set as you visit it, and make sure not to backtrack as you proceed along a depth-first search. If ever a duplicate node is detected, return &lt;code&gt;false&lt;/code&gt;, and if at the end of the traversal, the number of nodes &lt;code&gt;seen&lt;/code&gt; is not equal to the number of nodes detected (indicateing split graphs) return &lt;code&gt;false&lt;/code&gt;. Else return &lt;code&gt;true&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;763948215&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;763948215&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/graph-valid-tree/&#34;
  
  target=&#34;_blank&#34;
  &gt;Graph Valid Tree&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof n === &amp;#34;number&amp;#34;
// typeof edges === &amp;#34;object&amp;#34; (array of arrays with 2 numbers)

const validTree = (n, edges) =&amp;gt; {

    const adjacencyList = [...Array(n)].map(() =&amp;gt; [])
    const seen = new Set()

    for (let edge of edges){
        const [node1, node2] = edge
        adjacencyList[node1].push(node2)
        adjacencyList[node2].push(node1)
    }

    const dfs = (current, last) =&amp;gt; {
        if(seen.has(current)) return false
        seen.add(current)

        for(let next of adjacencyList[current]){
            if(next === last) continue
            if(!dfs(next, current)) return false
        }
        return true
    }

    if (!dfs(0, -1)) return false

    return n === seen.size

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;How to finish all courses given an array of their prerequisites, if this is possible? You can first construct an &lt;code&gt;adjacencyList&lt;/code&gt; using prerequisites as keys and all courses that are a direct requirement of that &lt;code&gt;prerequisite&lt;/code&gt; pushed to an array that is the key value. As the &lt;code&gt;adjacencyList&lt;/code&gt; is being constructed, build up an &lt;code&gt;indegrees&lt;/code&gt; array the size of the number of nodes, increasing the indegree by 1 of any direct consequent &lt;code&gt;course&lt;/code&gt; whenever a new &lt;code&gt;prerequisite&lt;/code&gt; is detected. Then, take all courses with an indegree of 0 and add them to a &lt;code&gt;queue&lt;/code&gt;. Process the &lt;code&gt;queue&lt;/code&gt; in a breadth-first search manner, repeatedly shifting out the top &lt;code&gt;course&lt;/code&gt; from the queue, subtracting one from the indegree of any course it points to, and then adding to the &lt;code&gt;queue&lt;/code&gt; any courses that now have an indegree of zero (for a later step in the breadth-first search). Finally, push the &lt;code&gt;queue&lt;/code&gt; course being processed to the &lt;code&gt;result&lt;/code&gt;, and move on to the next &lt;code&gt;course&lt;/code&gt; in the &lt;code&gt;queue&lt;/code&gt;, if any. If at the end not all nodes are processed, you can return an empty array to indiate not all courses can be reached. Otherwise, return &lt;code&gt;result&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;687913452&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;687913452&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/course-schedule-ii/&#34;
  
  target=&#34;_blank&#34;
  &gt;Course Schedule II&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof numCourses === &amp;#34;number&amp;#34;
// typeof prerequisites === &amp;#34;object&amp;#34; (array of arrays of 2 numbers)

const findOrder = (numCourses, prerequisites) =&amp;gt; {

  const adjacencyList = new Map()
  const indegrees = new Array(numCourses).fill(0)

  for (const [course, prerequisite] of prerequisites) {
    if (!adjacencyList.has(prerequisite)) adjacencyList.set(prerequisite, [])
    adjacencyList.get(prerequisite).push(course)
    indegrees[course]&amp;#43;&amp;#43;
  }

  const queue = []
  for (let i = 0; i &amp;lt; indegrees.length; i&amp;#43;&amp;#43;) {
    if (!indegrees[i]) queue.push(i)
  }

  const result = []
  while (queue.length) {
    const course = queue.shift()
    if (adjacencyList.has(course)) {
      for (const nextCourse of adjacencyList.get(course)) {
        indegrees[nextCourse]--
        if (!indegrees[nextCourse]) queue.push(nextCourse)
      }
    }
    result.push(course)
  }

  return numCourses === result.length ? result : []

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To count the number of connected components in a undirected graph, one method involves converting connections or edges into an &lt;code&gt;adjacencyList&lt;/code&gt; that maps out every connection between two nodes. Then every node on the &lt;code&gt;adjacencyList&lt;/code&gt; can be iterated. If the node has been &lt;code&gt;visited&lt;/code&gt; it can be passed over, but if it has not yet been &lt;code&gt;visited&lt;/code&gt;, a &lt;code&gt;result&lt;/code&gt; counter starting at 0 can increase by 1. Then, if the &lt;code&gt;result&lt;/code&gt; counter incremented, perform a depth-first search starting at the iterated node and continuing to each node that can be reached from it through any number of &lt;code&gt;adjacencyList&lt;/code&gt; connections that has not yet been &lt;code&gt;visited&lt;/code&gt;. Mark each as &lt;code&gt;visited&lt;/code&gt; and only stop (base case) on a node that has already been &lt;code&gt;visited&lt;/code&gt; to prevent infinite loops. The depth-first search will allow only the first visit to a new connected component to add to the &lt;code&gt;result&lt;/code&gt; tally.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;953862174&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;953862174&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/number-of-connected-components-in-an-undirected-graph/&#34;
  
  target=&#34;_blank&#34;
  &gt;Number of Connected Components in a Undirected Graph&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof n === &amp;#34;number&amp;#34;
// typeof edges === &amp;#34;object&amp;#34; (array of arrays with 2 numbers)

const countComponents = (n, edges) =&amp;gt; {

  const adjacencyList = Array(n).fill().map(r =&amp;gt; [])
  const visited = new Set()
  
  for (let [node1, node2] of edges) {
    adjacencyList[node1].push(node2)
    adjacencyList[node2].push(node1)
  }
  
  const dfs = (node) =&amp;gt; {
    if (visited.has(node)) return
    visited.add(node)
    for (let nextNode of adjacencyList[node]) {
      dfs(nextNode)
    }
  }

  let result = 0

  for (let i = 0; i &amp;lt; n; i&amp;#43;&amp;#43;) {
    if (visited.has(i)) continue
    result&amp;#43;&amp;#43;
    dfs(i)
  }

  return result

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To determine how many moves to takes to get from position &lt;code&gt;0,0&lt;/code&gt; to position &lt;code&gt;x,y&lt;/code&gt; on an infinite chess board, first you can choose to investigate absolute value of both x and y to take advantage of the problem&amp;rsquo;s symmetry. Then, create a &lt;code&gt;queue&lt;/code&gt; seeded with the origin point, and perform breadth-first search with a &lt;code&gt;nextQueue&lt;/code&gt;. For every item processed from the &lt;code&gt;queue&lt;/code&gt;&amp;rsquo;s front, if it is at the desired end position, return a &lt;code&gt;moves&lt;/code&gt; counter that starts at 0. Else, consider the eight directions a knight can move &amp;ndash; changing x or y coordinates 1 space or 2 spaces, and then changing whichever coordinate remains whichever of 1 or 2 was not already chosen. If a position has already been &lt;code&gt;seen&lt;/code&gt; or is below -2, it can be ignored, otherwise add the coordinates to the &lt;code&gt;nextQueue&lt;/code&gt;. Once the &lt;code&gt;queue&lt;/code&gt; is emptied, replace it with &lt;code&gt;nextQueue&lt;/code&gt;, make &lt;code&gt;nextQueue&lt;/code&gt; empty, and increase the &lt;code&gt;moves&lt;/code&gt; counter by 1.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;571396248&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;571396248&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/minimum-knight-moves/&#34;
  
  target=&#34;_blank&#34;
  &gt;Minimum Knight Moves&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof x === &amp;#34;number&amp;#34;
// typeof y === &amp;#34;number&amp;#34;

const minKnightMoves = (x, y) =&amp;gt; {
    const absX = Math.abs(x)
    const absY = Math.abs(y)

    const directions = [[1,2],[2,1],[-1,-2],[-2,-1],[-1,2],[2,-1],[1,-2],[-2,1]]
    const seen = new Set()
    seen.add(`0-0`)

    let moves = 0
    let queue = [[0,0]]
    let nextQueue = []

    while(queue.length){
        const current = queue.shift()
        if(absX === current[0] &amp;amp;&amp;amp; absY === current[1]) return distance
        for (let direction of directions){
            const newX = current[0] &amp;#43; direction[0]
            const newY = current[1] &amp;#43; direction[1]
            if(newX &amp;lt; -2 || newY &amp;lt; -2 || seen.has(`${newX}&amp;#43;${newY}`)){
              continue
            }
            seen.add(`${newX}&amp;#43;${newY}`)
            nextQueue.push([newX, newY])
        }
        if(!queue.length){
            moves&amp;#43;&amp;#43;
            queue = nextQueue
            nextQueue = []
        }
    }

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;*** Cheapest Flights Within K Stops goes here ***&lt;/p&gt;
&lt;p&gt;To count how many steps it takes to transform one word into a different word of equal length, given a list of valid intermediate words, you can set a &lt;code&gt;changes&lt;/code&gt; variable to &lt;code&gt;1&lt;/code&gt;, a &lt;code&gt;queue&lt;/code&gt; array to contain the starting word, a &lt;code&gt;visited&lt;/code&gt; set to contain the starting word, and a &lt;code&gt;dictionary&lt;/code&gt; to contain all legal intermediates. While &lt;code&gt;queue&lt;/code&gt; has a length, loop each &lt;code&gt;word&lt;/code&gt; in the &lt;code&gt;queue&lt;/code&gt;. For each, if any is the intended end word, return &lt;code&gt;changes&lt;/code&gt; immediately. Else, for that &lt;code&gt;word&lt;/code&gt; in the &lt;code&gt;queue&lt;/code&gt;, test replacing all one-character-changes: every letter with every possible letter. If the new word is already in &lt;code&gt;visited&lt;/code&gt; or is not in the &lt;code&gt;dictionary&lt;/code&gt; you can ignore it, else push to a &lt;code&gt;nextQueue&lt;/code&gt;. If every &lt;code&gt;word&lt;/code&gt; in the &lt;code&gt;queue&lt;/code&gt; finishes, replace the &lt;code&gt;queue&lt;/code&gt; with &lt;code&gt;nextQueue&lt;/code&gt; and start the next round. If the &lt;code&gt;queue&lt;/code&gt; is empty without finding a solution you can return &lt;code&gt;0&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;269847513&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;269847513&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/word-ladder/&#34;
  
  target=&#34;_blank&#34;
  &gt;Word Ladder&lt;/a
&gt;
 &amp;ndash; code slightly modified from &lt;a
  href=&#34;https://leetcode.com/problems/word-ladder/solutions/152214/&#34;
  
  target=&#34;_blank&#34;
  &gt;linfongi&amp;rsquo;s code&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof beginWord === &amp;#34;string&amp;#34;
// typeof endWord === &amp;#34;string&amp;#34;
// typeof wordList === &amp;#34;object&amp;#34; (array of strings)

const ladderLength = (beginWord, endWord, wordList) =&amp;gt; {

  let changes = 1
  let queue = [beginWord]
  const visited = new Set(queue)
  const dictionary = new Set(wordList)
  
  while (queue.length) {
    const nextQueue = []
    for (let word of queue) {
      if (word === endWord) {
        return changes
      }
      
      const wordArrayForChanges = word.split(&amp;#39;&amp;#39;)
      for (let i = 0; i &amp;lt; wordArrayForChanges.length; i&amp;#43;&amp;#43;) {
        for (let replacementLetter of &amp;#34;abcdefghijklmnopqrstuvwxyz&amp;#34;) {
          wordArrayForChanges[i] = replacementLetter
          const newWord = wordArrayForChanges.join(&amp;#39;&amp;#39;)
          if (!visited.has(newWord) &amp;amp;&amp;amp; dictionary.has(newWord)) {
            nextQueue.push(newWord)
            visited.add(newWord)
          }
          wordArrayForChanges[i] = word[i]
        }
      }
    }
    queue = nextQueue
    changes&amp;#43;&amp;#43;
  }
  
  return 0
  
}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find the longest increasing path in a matrix of numbers, first note that an empty matrix can return &lt;code&gt;0&lt;/code&gt;. Otherwise, you can set &lt;code&gt;best&lt;/code&gt; to &lt;code&gt;1&lt;/code&gt;, then loop the input matrix. At every step, run a helper function to see if &lt;code&gt;best&lt;/code&gt; can be updated. The helper function takes the location of the matrix iteration, returns a value in a &lt;code&gt;cache&lt;/code&gt; if a value is present, and otherwise creates a &lt;code&gt;localBest&lt;/code&gt; stating at &lt;code&gt;1&lt;/code&gt; (because a series of 1 is technically increasing). Run the function recursively as many as four times, in the up, down, left, and right directions, as long as the given direction contains a value less than or greater than the current value (choose one direction of comparison for the whole function). Therefore, &lt;code&gt;localBest&lt;/code&gt; four has chances to update with each helper return &lt;code&gt;+1&lt;/code&gt; depending on which direction gives the best result. At the end of the recusive calls, update the &lt;code&gt;cache&lt;/code&gt; with the &lt;code&gt;localBest&lt;/code&gt; and then return &lt;code&gt;localBest&lt;/code&gt;. At the end of the original matrix traversal, return &lt;code&gt;best&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;357124689&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;357124689&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/longest-increasing-path-in-a-matrix/&#34;
  
  target=&#34;_blank&#34;
  &gt;Longest Increasing Path in a Matrix&lt;/a
&gt;
 &amp;ndash; code slightly modified from &lt;a
  href=&#34;https://leetcode.com/problems/longest-increasing-path-in-a-matrix/solutions/423521/clean-javascript-solution/&#34;
  
  target=&#34;_blank&#34;
  &gt;Hongbo-Miao&amp;rsquo;s code&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof matrix === &amp;#34;object&amp;#34; (array of arrays of numbers)

const longestIncreasingPath = (matrix) =&amp;gt; {
  if (!matrix?.length) return 0

  const directions = [[-1, 0], [1, 0], [0, 1], [0, -1]]
  const cache = [...Array(matrix.length)].map(() =&amp;gt; Array(matrix[0].length).fill(0))

  const go = (x, y) =&amp;gt; {
    if (cache[x][y]) return cache[x][y]
    let localBest = 1
    for (const [dx, dy] of directions) {
      const i = x &amp;#43; dx
      const j = y &amp;#43; dy
      if (i &amp;gt;= 0 &amp;amp;&amp;amp; i &amp;lt; matrix.length &amp;amp;&amp;amp; j &amp;gt;= 0 &amp;amp;&amp;amp; j &amp;lt; matrix[0].length &amp;amp;&amp;amp; matrix[i][j] &amp;lt; matrix[x][y]) {
        localBest = Math.max(localBest, go(i, j) &amp;#43; 1)
      }
    }
    cache[x][y] = localBest
    return localBest
  };

  let best = 1
  for (let i = 0; i &amp;lt; matrix.length; i&amp;#43;&amp;#43;) {
    for (let j = 0; j &amp;lt; matrix[0].length; j&amp;#43;&amp;#43;) {
      best = Math.max(best, go(i, j))
    }
  }
  return best

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;In order to find which words from a list can be spelled through up/down/left/right adjacent characters on a grid of characters, you can begin by creating an empty &lt;code&gt;results&lt;/code&gt; array. Then construct a &lt;a
  href=&#34;/posts/js-algos#Tries&#34;
  
  &gt;trie&lt;/a
&gt;
&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;413692758&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;413692758&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/word-search-ii/&#34;
  
  target=&#34;_blank&#34;
  &gt;Word Search II&lt;/a
&gt;
 &amp;ndash; code slightly modified from &lt;a
  href=&#34;https://leetcode.com/problems/word-search-ii/solutions/138279/clean-javascript-solution/&#34;
  
  target=&#34;_blank&#34;
  &gt;Hongbo-Miao&amp;rsquo;s code&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof board === &amp;#34;object&amp;#34; (array of array of 1 character strings)
// typeof words === &amp;#34;object&amp;#34; (array of strings)


&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;*** Alien Dictionary goes here ***&lt;/p&gt;
&lt;p&gt;To determine the minimum number of buses to take when given a number of looping bus routes with stops (each with a unique bus), if the source is not the same as the target (which allows immediate return of &lt;code&gt;0&lt;/code&gt;), you can create a new route data such that each bus stop points to an array with all possible buses that reach it. Then a &lt;code&gt;queue&lt;/code&gt; array starting with only the source bus stop can be reviewed in breadth-first search style. At each step, the front &lt;code&gt;busStop&lt;/code&gt; in the &lt;code&gt;queue&lt;/code&gt; is shifted off, and all &lt;code&gt;buses&lt;/code&gt; that reach that &lt;code&gt;busStop&lt;/code&gt; are collected and iterated. If the &lt;code&gt;bus&lt;/code&gt; has already been &lt;code&gt;visited&lt;/code&gt;, continue (preventing loops), else add the bus to a &lt;code&gt;visited&lt;/code&gt; set and loop all stops the bus reaches (using a location in the original routes input array). If the destination is found, return a &lt;code&gt;count&lt;/code&gt; tally started at &lt;code&gt;1&lt;/code&gt;, else push the new stop number into a &lt;code&gt;nextQueue&lt;/code&gt;. If after reviewing all &lt;code&gt;buses&lt;/code&gt; that go to the &lt;code&gt;busStop&lt;/code&gt; currently processed, there are no more stops in the &lt;code&gt;queue&lt;/code&gt;, increment &lt;code&gt;count&lt;/code&gt; by 1, replace &lt;code&gt;queue&lt;/code&gt; with &lt;code&gt;nextQueue&lt;/code&gt;, and replace &lt;code&gt;nextQueue&lt;/code&gt; with an empty array. If &lt;code&gt;queue&lt;/code&gt; is still exhausted, you can return &lt;code&gt;-1&lt;/code&gt; to indicate it is not possible to reach the target.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;691745238&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;691745238&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/bus-routes/&#34;
  
  target=&#34;_blank&#34;
  &gt;Bus Routes&lt;/a
&gt;
 &amp;ndash; code slightly modified from &lt;a
  href=&#34;https://leetcode.com/problems/bus-routes/solutions/181820/readable-javascript-solution/&#34;
  
  target=&#34;_blank&#34;
  &gt;codingbarista&amp;rsquo;s code&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof routes === &amp;#34;object&amp;#34; (array of arrays of numbers)
// typeof source === &amp;#34;number&amp;#34;
// typeof target === &amp;#34;number&amp;#34;

const numBusesToDestination = (routes, source, target) =&amp;gt; {
    if (source === target) return 0

    const busStops = new Map()
    for (let i = 0; i &amp;lt; routes.length; i&amp;#43;&amp;#43;) {
        for (let j = 0; j &amp;lt; routes[i].length; j&amp;#43;&amp;#43;) {
            const busStop = routes[i][j]
            const buses = busStops.get(busStop) || []
            buses.push(i)
            busStops.set(busStop, buses)
        }
    }
    
    const visited = new Set()
    let count = 1
    let queue = [source]
    let nextQueue = []
    while (queue.length) {
        const busStop = queue.shift()
        const buses = busStops.get(busStop)
        for (let bus of buses) {
            if (visited.has(bus)) continue
            visited.add(bus)
            for (let j = 0; j &amp;lt; routes[bus].length; j&amp;#43;&amp;#43;) {
                const nextBusStop = routes[bus][j]
                if (nextBusStop === target) return count
                nextQueue.push(nextBusStop)
            }
        }
        if (!queue.length){
            count&amp;#43;&amp;#43;
            queue = nextQueue
            nextQueue = []
        }
    }
    
    return -1

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;h1 id=&#34;binary-search-trees&#34;&gt;Binary Search Trees&lt;/h1&gt;
&lt;p&gt;Binary search trees are binary trees that have the property that every node bisects the search space &amp;ndash; you can tell which side of a node to go down for further investigation based on its value. In order to find the lowest common ancestor of two nodes, consider that as you decend the tree, as long as the current value is less than the lower value of the two target nodes, or greater than the upper value of the two target nodes, both target nodes will be on the same side of the next step down of the tree. Decend recursively in the direction of both nodes until you no longer can, and return the stopping value.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;172834569&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;172834569&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/lowest-common-ancestor-of-a-binary-search-tree/&#34;
  
  target=&#34;_blank&#34;
  &gt;Lowest Common Ancestor of a Binary Search Tree&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof root === &amp;#34;object&amp;#34; (binary search tree node)
// typeof p === &amp;#34;object&amp;#34; (binary search tree node)
// typeof q === &amp;#34;object&amp;#34; (binary search tree node)

const lowestCommonAncestor = (root, p, q) =&amp;gt; {

    const large = Math.max(p.val,q.val)
    const small = Math.min(p.val,q.val)

    while(root.val &amp;gt; large || root.val &amp;lt; small){
        if(root.val &amp;gt; large) root = root.left
        else if (root.val &amp;lt; small) root = root.right

    }

    return root

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;Converting an array of numbers in ascending order into a height-balanced binary search tree can be handled by bisection and recursion. The height of the tree never has to be explictly measured. As long as there is at least one value in an examined range of the array, the central node of the segment of the array can become the head node of the segment of the binary search tree, and all numbers lower in the segment can be the subject of a recursive call to fill the &lt;code&gt;.left&lt;/code&gt; node, while all numbers higher can fill &lt;code&gt;.right&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;123984657&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;123984657&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/convert-sorted-array-to-binary-search-tree/&#34;
  
  target=&#34;_blank&#34;
  &gt;Convert Sorted Array to Binary Search Tree&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof nums === &amp;#34;object&amp;#34; (array of numbers)

const sortedArrayToBST = (nums) =&amp;gt; {
    return recursion(0, nums.length - 1, nums)
}

const recursion = (left, right, nums) =&amp;gt; {

    if(left &amp;gt; right) return null

    const node = new TreeNode()
    const mid = Math.floor((left&amp;#43;right)/2)

    node.val = nums[mid]
    node.left = recursion(left, mid - 1, nums)
    node.right = recursion(mid &amp;#43; 1, right, nums)

    return node

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To check if a binary tree is a valid binary search tree, recursion is valuable. Set &lt;code&gt;left&lt;/code&gt; and &lt;code&gt;right&lt;/code&gt; default parameters of -Infinity and Infinity on the function, and run it so that an empty node returns &lt;code&gt;true&lt;/code&gt;, while otherwise two recursive calls are spawned. One takes the &lt;code&gt;.left&lt;/code&gt; node with a &lt;code&gt;left&lt;/code&gt; of &lt;code&gt;left&lt;/code&gt; and a &lt;code&gt;right&lt;/code&gt; of &lt;code&gt;root.val&lt;/code&gt;, while the other takes the &lt;code&gt;.right&lt;/code&gt; node with a &lt;code&gt;right&lt;/code&gt; of &lt;code&gt;right&lt;/code&gt; and a &lt;code&gt;left&lt;/code&gt; of &lt;code&gt;root.val&lt;/code&gt;. Then return &lt;code&gt;true&lt;/code&gt; only if the left node and the right node return &lt;code&gt;true&lt;/code&gt;, and &lt;code&gt;root.val&lt;/code&gt; is between &lt;code&gt;left&lt;/code&gt; and &lt;code&gt;right&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;825791463&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;825791463&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/validate-binary-search-tree/description/&#34;
  
  target=&#34;_blank&#34;
  &gt;Validate Binary Search Tree&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
//typeof root === &amp;#34;object&amp;#34; (binary search tree node)

const isValidBST = (root, left = -Infinity, right = Infinity) =&amp;gt; {
    if (!root) return true
    return (isValidBST(root.left, left, root.val)
    &amp;amp;&amp;amp; isValidBST(root.right,root.val, right)
    &amp;amp;&amp;amp; left &amp;lt; root.val &amp;amp;&amp;amp; root.val &amp;lt; right)
    
}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find the Kth smallest element in a binary search tree, you can search the tree with an inorder depth-first search from left to right, and find the Kth value. Every time you reach a value, decrease by 1 a counter equal to &lt;code&gt;k&lt;/code&gt;, and then, at the location where you decremented, check if &lt;code&gt;k&lt;/code&gt; has reached 0. If so, return the value at the node, and use a nonallowed value to return otherwise, making sure that only the correct value bubbles up to the final return statement.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;815297463&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;815297463&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/kth-smallest-element-in-a-bst/&#34;
  
  target=&#34;_blank&#34;
  &gt;Kth Smallest Element in a BST&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof root === &amp;#34;object&amp;#34; (binary search tree node)
// typeof k === &amp;#34;number&amp;#34;

const kthSmallest = (root, k) =&amp;gt; {
    const dfs = (node) =&amp;gt; {
        if(!node) return -1

        const leftResult = dfs(node.left)
        if (leftResult &amp;gt; -1) return leftResult

        if (--k === 0) return node.val

        const rightResult = dfs(node.right)
        if (rightResult &amp;gt; -1) return rightResult

        return -1
    }
    return dfs(root)

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find the inorder successor of a node in a binary search tree, there are two basic cases. First, if the node for which the successor is being searched has a &lt;code&gt;.right&lt;/code&gt;, the higher parts of the tree do not need to be considered, and the result is one &lt;code&gt;.right&lt;/code&gt; followed by as many &lt;code&gt;.left&lt;/code&gt; as are available. Otherwise, you can declare a &lt;code&gt;successor&lt;/code&gt; variable and set it to null, then navigate through nodes starting at the root as long as the active node is not the node for which the successor is being considered. To do this, at every step in this section option for the algorithm, if the value at the current node is greater than the node for which the &lt;code&gt;successor&lt;/code&gt; is trying to be found, set &lt;code&gt;successor&lt;/code&gt; to the active node and then the active node to the &lt;code&gt;.left&lt;/code&gt;. Otherwise, if a &lt;code&gt;.right&lt;/code&gt; is available, you can go to the &lt;code&gt;.right&lt;/code&gt; without moving the &lt;code&gt;successor&lt;/code&gt; (because doing that would put the &lt;code&gt;successor&lt;/code&gt; behind the node searched for, not ahead of it). In a case where there is no further &lt;code&gt;.right&lt;/code&gt;, but the value is still less, not only do you not have to move the &lt;code&gt;successor&lt;/code&gt;, but you can also &lt;code&gt;break&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;945162783&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;945162783&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/inorder-successor-in-bst/&#34;
  
  target=&#34;_blank&#34;
  &gt;Inorder Successor in BST&lt;/a
&gt;
 &amp;ndash; code adapted from &lt;a
  href=&#34;https://github.com/chihungyu1116/leetcode-javascript/blob/master/285%20Inorder%20Successor%20in%20BST.js&#34;
  
  target=&#34;_blank&#34;
  &gt;chihungyu1116&amp;rsquo;s code and explanation&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof root === &amp;#34;object&amp;#34; (binary search tree node)
// typeof p === &amp;#34;object&amp;#34; (binary search tree node)

const inorderSuccessor = (root, p) =&amp;gt; {

    if(p.right){
        p = p.right
        while(p.left) p = p.left
        return p
    }

    const successor = null

    while(root !== p){
        if (root.val &amp;gt; p.val){
            successor = root
            root = root.left
        } else if (root.right){
            root = root.right
        } else {
            break
        }
    }

    return successor

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;h1 id=&#34;hash-tables&#34;&gt;Hash Tables&lt;/h1&gt;
&lt;p&gt;Whether one group of characters in a string is completely included in a different string can be described as being able to cut letters out of a magazine for a ransom note. If the only characters that can appear are lower case letters, then these can be added by character code into an array from the &amp;ldquo;magazine&amp;rdquo; string, and then what appears in the &amp;ldquo;note&amp;rdquo; string can be subtracted. If any count in the array goes negative, the ransom note cannot be completed.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;185629473&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;185629473&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/ransom-note/&#34;
  
  target=&#34;_blank&#34;
  &gt;Ransom Note&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof ransomNote === &amp;#34;string&amp;#34;
// typeof magazine === &amp;#34;string&amp;#34;

const canConstruct = function(ransomNote, magazine) {

    const arr = new Array(26).fill(0)

    for (let i = 0; i &amp;lt; magazine.length; i&amp;#43;&amp;#43;){
        arr[magazine.charCodeAt(i)-97]&amp;#43;&amp;#43;
    }

    for (let i = 0; i &amp;lt; ransomNote.length; i&amp;#43;&amp;#43;){
        arr[ransomNote.charCodeAt(i)-97]--
        if(arr[ransomNote.charCodeAt(i)-97] &amp;lt; 0) return false
    }

    return true

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To create a data struture such that elements can be inserted (returning a boolean, &lt;code&gt;true&lt;/code&gt; if the element is not already present), removed (returning a boolean, &lt;code&gt;true&lt;/code&gt;, if the element is already present), or randomly selected with equal probability, a function set to a variable can instantiate an empty &lt;code&gt;array&lt;/code&gt; and an empty &lt;code&gt;object&lt;/code&gt;, both set under &lt;code&gt;this&lt;/code&gt;. To insert, at a &lt;code&gt;prototype&lt;/code&gt; method, if the input is already found by direct lookup in the &lt;code&gt;object&lt;/code&gt;, return &lt;code&gt;false&lt;/code&gt;. Else, push the input to the &lt;code&gt;array&lt;/code&gt;, note its &lt;code&gt;index&lt;/code&gt;, and set a key-value pair in the &lt;code&gt;object&lt;/code&gt; such that the key is the input being set and the value is the &lt;code&gt;index&lt;/code&gt; in the array. Then return &lt;code&gt;true&lt;/code&gt;. To remove, at a &lt;code&gt;prototype&lt;/code&gt; method, if the input cannot be found by &lt;code&gt;object&lt;/code&gt; lookup, return &lt;code&gt;false&lt;/code&gt;. Else, pop the &lt;code&gt;last&lt;/code&gt; item in the &lt;code&gt;array&lt;/code&gt;, then used the popped item as a key lookup in the &lt;code&gt;object&lt;/code&gt; and replace its index value with the index value of the input to be removed. Next, delete the index lookup in the &lt;code&gt;object&lt;/code&gt; of the input being removed, but use its former index location, already stored in the &lt;code&gt;object&lt;/code&gt; under the item being moved, to assign the &lt;code&gt;last&lt;/code&gt; item to that location. Then return &lt;code&gt;true&lt;/code&gt;. Finally, to get a random value on the &lt;code&gt;prototype&lt;/code&gt;, floor a random value to the length of the &lt;code&gt;array&lt;/code&gt; and return whatever item is at that &lt;code&gt;index&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;895631427&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;895631427&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/insert-delete-getrandom-o1/&#34;
  
  target=&#34;_blank&#34;
  &gt;Insert Delete GetRandom O(1)&lt;/a
&gt;
 &amp;ndash; code slightly modified from &lt;a
  href=&#34;https://leetcode.com/problems/insert-delete-getrandom-o1/solutions/532747/javascript/&#34;
  
  target=&#34;_blank&#34;
  &gt;stevenkinouye&amp;rsquo;s code&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

const RandomizedSet = function() {
    this.array = []
    this.object = {}
}

RandomizedSet.prototype.insert = function(val) {
    if(this.object[val] !== undefined) return false
    this.array.push(val)
    const index = this.array.length - 1
    this.object[val] = index
    return true
}
RandomizedSet.prototype.remove = function(val) {
    if(this.object[val] === undefined) return false
    const last = this.array.pop()
    this.object[last] = this.object[val]
    delete this.object[val]
    this.array[this.object[last]] = last
    return true
}

RandomizedSet.prototype.getRandom = function() {
    const index = Math.floor(Math.random()*this.array.length)
    return this.array[index]
}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;h1 id=&#34;dynamic-programming&#34;&gt;Dynamic Programming&lt;/h1&gt;
&lt;p&gt;If someone can climb one or two steps at a time, how many ways can they get to the top? Dynamic programming helps recognize that the number of ways to reach any given step acts as a multiplier on the ways to reach steps further up. A linear solution involves noticing the one way of getting to the 0th step, adding the number of ways to get to the 0th step to the 1st and 2nd steps, then &amp;ldquo;stepping&amp;rdquo; from the 1st step to the 2nd and 3rd steps, and adding counts so on and so on. Only three locations need to be available at a time.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;685421739&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;685421739&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/climbing-stairs/&#34;
  
  target=&#34;_blank&#34;
  &gt;Climbing Stairs&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof n === &amp;#34;number&amp;#34;

const climbStairs = (n) =&amp;gt; {

    let steps = [1,0,0]
    let count = 0

    while(count &amp;lt; n){
        let next0 = steps[1] &amp;#43; steps[0]
        let next1 = steps[2] &amp;#43; steps[0]
        
        steps = [next0, next1, 0]
        count&amp;#43;&amp;#43;
    }

    return steps[0]

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find the maximum sum of a subarray (consecutive numbers) in an array with a length, you can initialize a &lt;code&gt;best&lt;/code&gt; variable and a &lt;code&gt;currentMax&lt;/code&gt; variable both to the value of the first number in the array. Then, looping the rest of the numbers, at every step you can first update &lt;code&gt;currentMax&lt;/code&gt; to be the better of &lt;code&gt;currentMax&lt;/code&gt; plus the pointed value, or just the pointed value (this will remove negative values of &lt;code&gt;currentMax&lt;/code&gt;). As the second part of each array step, update &lt;code&gt;best&lt;/code&gt; to be &lt;code&gt;currentMax&lt;/code&gt; if &lt;code&gt;currentMax&lt;/code&gt; is ever the greater of the two. At the end of the loop, return &lt;code&gt;best&lt;/code&gt;. This is a version of &lt;a
  href=&#34;https://en.wikipedia.org/wiki/Maximum_subarray_problem#Kadane%27s_algorithm&#34;
  
  target=&#34;_blank&#34;
  &gt;Kadane&amp;rsquo;s algorithm&lt;/a
&gt;
.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;281374596&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;281374596&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/maximum-subarray/&#34;
  
  target=&#34;_blank&#34;
  &gt;Maximum Subarray&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof nums === &amp;#34;object&amp;#34; (array of numbers)

const maxSubArray = (nums) =&amp;gt; {

    let best = nums[0]
    let currentMax = nums[0]

    for (let i = 1; i &amp;lt; nums.length; i&amp;#43;&amp;#43;){
        currentMax = Math.max(currentMax&amp;#43;nums[i], nums[i])
        best = Math.max(best,currentMax)
    }

    return best

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find the fewest number of coins that sum to a value, with an infinite amount of coins but specific coin types, a &lt;code&gt;dp&lt;/code&gt; array can be used with length equal to &lt;code&gt;1 + amount&lt;/code&gt; of the goal value. In this dynamic programming approach, seed the 0th location in &lt;code&gt;dp&lt;/code&gt; with 0 to indicate it takes 0 coins (the value) to reach a partial amount of 0 (the index). Then, loop through the &lt;code&gt;dp&lt;/code&gt; array for each available coin, and when a given value is noninfinite, update the value at the location a coin amount up from the index to be the minimum of itself or the source location plus 1. Each new coin can fill in new intervals from each available starting index. At the end, you can return a finite value at the last location in &lt;code&gt;dp&lt;/code&gt;, or -1.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;129736458&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;129736458&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/coin-change/&#34;
  
  target=&#34;_blank&#34;
  &gt;Coin Change&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof coins === &amp;#34;object&amp;#34; (array of numbers)
// typeof amount === &amp;#34;number&amp;#34;

const coinChange = (coins, amount) =&amp;gt; {

    const dp = new Array(amount&amp;#43;1).fill(Infinity)
    dp[0] = 0

    for (let coin of coins){
        for (let i = 0; i &amp;lt; dp.length-coin; i&amp;#43;&amp;#43;){
            dp[i&amp;#43;coin] = Math.min(dp[i&amp;#43;coin], dp[i]&amp;#43;1)
        }
    }

    if(isFinite(dp[amount])) return dp[amount]
    return -1

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;Can a non-empty array containing only positive integers be split into two subsets that are equal? To determine, you can collect the total &lt;code&gt;sum&lt;/code&gt; and immediately retrun false if that &lt;code&gt;sum&lt;/code&gt; is an odd number. Else, you can use dynamic programming to see if there is some combination of numbers that sums to half the total sum. Make a &lt;code&gt;dp&lt;/code&gt; array equal to one more than the &lt;code&gt;sum&lt;/code&gt;, seed the 0 location (representing a sum of 0) with &lt;code&gt;true&lt;/code&gt;, and for each number in the original array, loop the &lt;code&gt;dp&lt;/code&gt; array from right to left, checking if the current value location minus the value being considered is &lt;code&gt;true&lt;/code&gt;, and if so, updating the current value location to &lt;code&gt;true&lt;/code&gt;. Looping from left to right would reuse numbers. At the end, return the boolean status of the right end of the array.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;829153647&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;829153647&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/partition-equal-subset-sum/&#34;
  
  target=&#34;_blank&#34;
  &gt;Partition Equal Subset Sum&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof nums === &amp;#34;object&amp;#34; (array of numbers)

canPartition = (nums) =&amp;gt; {
    
    const sum = nums.reduce((a,b)=&amp;gt;a&amp;#43;b)

    if(sum % 2) return false
    const dp = new Array(sum/2 &amp;#43; 1).fill(false)
    dp[0] = true
    
    for(let num of nums) {
        for(let i = sum; i &amp;gt;= num; i--){
            dp[i] = dp[i] || dp[i - num]
        }
    }
    
    return dp[sum/2]

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;How many ways are there for a robot in an array, that can only go to the right and down, to get from the upper-left to the lower-right? This problem can be solved with the help of a &lt;code&gt;dp&lt;/code&gt; array. Create it as the length of a row, &lt;code&gt;n&lt;/code&gt;, and fill all locations with 0, but update the 0th index with 1, to show there is 1 way for the robot to get to (be in) the upper-left. Then, loop for the number of rows &lt;code&gt;m&lt;/code&gt;, and in each of those loops, create a &lt;code&gt;nextDP&lt;/code&gt; array and loop each location in the row. For each location in the inner loop, update that location in &lt;code&gt;nextDP&lt;/code&gt; as the sum of any values in locations just above (just above is represented by the &lt;code&gt;dp&lt;/code&gt; array) and just to the left. At the end of each inner loop, replace &lt;code&gt;dp&lt;/code&gt; with &lt;code&gt;nextDP&lt;/code&gt;. Return the value at the rightmost index in &lt;code&gt;dp&lt;/code&gt; at the end of the outer loop. Note that for the first inner loop, &lt;code&gt;dp&lt;/code&gt; represents a theoretical &lt;code&gt;m-1&lt;/code&gt; row that allows the 0th location in the 0th row to be seeded correctly.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;789461532&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;789461532&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/unique-paths/&#34;
  
  target=&#34;_blank&#34;
  &gt;Unique Paths&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof m === &amp;#34;number&amp;#34;
// typeof n === &amp;#34;number&amp;#34;

const uniquePaths = (m, n) =&amp;gt; {

    let dp = new Array(n).fill(0)
    dp[0] = 1

    for (let i = 0; i &amp;lt; m; i&amp;#43;&amp;#43;){
        let nextDP = []
        for (let j = 0; j &amp;lt; n; j&amp;#43;&amp;#43;){
            let up = dp[j]
            let left = j ? nextDP[j-1] : 0
            nextDP[j] = up&amp;#43;left
        }
        dp = nextDP
    }

    return dp[dp.length-1]

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find the best maximum sum of nonadjacent values in an array (which in LeetCode is themed stealing from nonadjacent houses), consider a linear approach that uses two helper variables, a &lt;code&gt;back1Best&lt;/code&gt; and a &lt;code&gt;back2Best&lt;/code&gt;. Initialize both to 0, and iterate through the array from left to right. At each step, save a copy of &lt;code&gt;back1Best&lt;/code&gt;, update &lt;code&gt;back1Best&lt;/code&gt; to be the max of itself or the current value plus &lt;code&gt;back2Best&lt;/code&gt;, then update &lt;code&gt;back2Best&lt;/code&gt; to be the saved copy of the old &lt;code&gt;back1Best&lt;/code&gt;. At the end of the array iteration, &lt;code&gt;back1Best&lt;/code&gt; will contain the maximum sum.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;657943812&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;657943812&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/house-robber/&#34;
  
  target=&#34;_blank&#34;
  &gt;House Robber&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
typeof nums === &amp;#34;object&amp;#34; (array of numbers)

const rob = (nums) =&amp;gt; {

    let back1best = 0
    let back2best = 0

    for (let house of nums){
        const temp = back1best
        back1best = Math.max(house &amp;#43; back2best, back1best)
        back2best = temp
    }
    return back1best

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find the maximum product in any subarray (contiguous series) of an array, you can use three helpers, a &lt;code&gt;minimum&lt;/code&gt;, a &lt;code&gt;maximum&lt;/code&gt;, and a &lt;code&gt;best&lt;/code&gt;. Intialize each to the first value, then loop the remainder, at every step updating the &lt;code&gt;minimum&lt;/code&gt; and &lt;code&gt;maximum&lt;/code&gt; to be the new &lt;code&gt;maximum&lt;/code&gt; or &lt;code&gt;minimum&lt;/code&gt; of the current value in the loop, the value indicated in the loop times the &lt;code&gt;maximum&lt;/code&gt;, or the value indicated in the loop times the &lt;code&gt;minimum&lt;/code&gt;. Then update &lt;code&gt;best&lt;/code&gt;. The &lt;code&gt;minimum&lt;/code&gt; is saved because negative numbers multiplied turn positive.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;596217843&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;596217843&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/maximum-product-subarray/&#34;
  
  target=&#34;_blank&#34;
  &gt;Maximum Product Subarray&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof nums === &amp;#34;object&amp;#34; (array of numbers)

const maxProduct = (nums) =&amp;gt; {
    
    let maximum = nums[0]
    let minimum = nums[0]
    let best = nums[0]
    
    for(let i = 1; i &amp;lt; nums.length; i&amp;#43;&amp;#43;){
        const maximumCopy = maximum
        maximum = Math.max(nums[i], maximum*nums[i], minimum*nums[i])
        minimum = Math.min(nums[i], maximumCopy*nums[i], minimum*nums[i])
        best = Math.max(best, maximum)
    }
    return best

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find the longest strictly increasing subsequence in an array (numbers have to be in order, but do not need to be adjacent), binary search can help insert the &lt;code&gt;lowestValueReachingCount&lt;/code&gt; into a helper array. To do this, for every number in the input array, declare a &lt;code&gt;left&lt;/code&gt; of 0 and a &lt;code&gt;right&lt;/code&gt; equal to the current &lt;code&gt;best&lt;/code&gt; length (starting at 0). Then, while &lt;code&gt;left&lt;/code&gt; is not &lt;code&gt;right&lt;/code&gt; create a while loop, generating a &lt;code&gt;mid&lt;/code&gt; between &lt;code&gt;left&lt;/code&gt; and &lt;code&gt;right&lt;/code&gt; at every round. If the value at index &lt;code&gt;mid&lt;/code&gt; in &lt;code&gt;lowestValueReachingCount&lt;/code&gt; is less than the number being investigated in the outer loop, this means the longest strictly increasing subsequence ending at the original array number being investigated is at least equal to &lt;code&gt;mid+1&lt;/code&gt;, so set the new &lt;code&gt;left&lt;/code&gt; to &lt;code&gt;mid+1&lt;/code&gt; and continue. Else, &lt;code&gt;right&lt;/code&gt; can be &lt;code&gt;mid&lt;/code&gt;. When the &lt;code&gt;left&lt;/code&gt; and &lt;code&gt;right&lt;/code&gt; pointers meet, update the location in &lt;code&gt;lowestValueReachingCount&lt;/code&gt; with the number indicated in the outer loop (because if it were not the &lt;code&gt;lowestValueReachingCount&lt;/code&gt;, the &lt;code&gt;mid+1&lt;/code&gt; process would have moved the index up), and update &lt;code&gt;best&lt;/code&gt; with the location index found in the binary search, plus 1, if appropriate.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;847623951&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;847623951&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/longest-increasing-subsequence/&#34;
  
  target=&#34;_blank&#34;
  &gt;Maximum Product Subarray&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof nums === &amp;#34;object&amp;#34; (array of numbers)

const lengthOfLIS = (nums) =&amp;gt; {

    const lowestValueReachingCount = new Array(nums.length).fill(0)
    let best = 0
    
    for (let num of nums){
        let left = 0
        let right = best
        while(left !== right){
            const mid = left &amp;#43; Math.floor((right-left)/2)
            if (lowestValueReachingCount[mid] &amp;lt; num) left = mid &amp;#43; 1
            else right = mid
        }
        lowestValueReachingCount[right] = num
        best = Math.max(best,right&amp;#43;1)
    }
    
    return best

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;Given a starting position at the 0th index of an array, if the value at any position indicates the maximum range one can &amp;ldquo;hop&amp;rdquo; further to the right, can any series of hops reach the final value? In order to solve, you can linearly traverse the array starting at the 1st index, keeping track of a maximum &lt;code&gt;range&lt;/code&gt;, which starts at the value at the 0th index. If at any point the index of the traversal is larger than the index given by the &lt;code&gt;range&lt;/code&gt;, return &lt;code&gt;false&lt;/code&gt;, otherwise update the &lt;code&gt;range&lt;/code&gt; to be the best of itself or the index plus the value at the index. If the entire array loop is completed without returning &lt;code&gt;false&lt;/code&gt;, you can return &lt;code&gt;true&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;345278961&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;345278961&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/jump-game/&#34;
  
  target=&#34;_blank&#34;
  &gt;Jump Game&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof nums === &amp;#34;object&amp;#34; (array of numbers)

const canJump = (nums) =&amp;gt; {

    let range = nums[0]
    
    for(let i = 1; i &amp;lt; nums.length; i&amp;#43;&amp;#43;){
        if(range &amp;lt; i) return false

        range = Math.max(range, i &amp;#43; nums[i])
    }
    return true

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find the maximum area of a square all of &amp;ldquo;1&amp;rdquo; inside a 2D input array, you can create a 2D array for dynamic programming with length and width both equal to that of the input array plus 1. Fill all locations with 0. Then loop through the input matrix, and every time you find a &amp;ldquo;1&amp;rdquo;, replace the equivalent location in the dynamic programming array (which should be 1 ahead in both the x and y dimensions) with the minimum of its left, upper, and upper-left location plus 1. This is a candidate for the &lt;code&gt;best&lt;/code&gt; square, so update a &lt;code&gt;best&lt;/code&gt; variable with the value assigned to the dynamic programming array location, if appropriate. At the end of reviewing the input matrix, return the square of &lt;code&gt;best&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;379286451&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;379286451&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/maximal-square/&#34;
  
  target=&#34;_blank&#34;
  &gt;Maximal Square&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof matrix === &amp;#34;object&amp;#34; (array of arrays of numbers)

const maximalSquare = (matrix) =&amp;gt; {
    const dp = [...Array(matrix.length&amp;#43;1)].map(_ =&amp;gt; [...Array(matrix[0].length&amp;#43;1)].fill(0))
    
    let best = 0
    for (let i = 0; i &amp;lt; matrix.length; i&amp;#43;&amp;#43;) {
        for (let j = 0; j &amp;lt; matrix[0].length; j&amp;#43;&amp;#43;) {
            if (matrix[i][j] === &amp;#34;1&amp;#34;) {
                const last = Math.min(dp[i][j], dp[i][j&amp;#43;1], dp[i&amp;#43;1][j])
                dp[i&amp;#43;1][j&amp;#43;1] = last &amp;#43; 1
                best = Math.max(best, dp[i&amp;#43;1][j&amp;#43;1])
            }
        }
    }
    
    return best ** 2

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To determine the number of ways a string of numbers can be decoded as letters, where &amp;ldquo;A&amp;rdquo; is &amp;ldquo;1&amp;rdquo; and so on until &amp;ldquo;Z&amp;rdquo; is &amp;ldquo;26&amp;rdquo;, you can notice that if the string has no length or the first digit is &amp;ldquo;0&amp;rdquo;, there are 0 possible mappings. Otherwise, you can create a small &lt;code&gt;dp&lt;/code&gt; array with length 2, and both digits initially filled with 1. The first 1 gives a hypothetical number of ways for the -1 digit of the string to be decoded (useful for a later loop), while the second 1 gives the number of ways for the 0 index digit of the string to be decoded (since we know it is not 0). Next, loop the string, starting at the 1st index. At every step, create a &lt;code&gt;next&lt;/code&gt; variable starting at 0, and add the value of the 0 index of the &lt;code&gt;dp&lt;/code&gt; array to &lt;code&gt;next&lt;/code&gt; if the last and current indicies of the input array together range from &amp;ldquo;10&amp;rdquo; to &amp;ldquo;26&amp;rdquo;. Because only a single 2-digit number is possible, this means you can add to &lt;code&gt;next&lt;/code&gt; the value/number of combinations from two indicies ago (this is how the hypothetical 1 makes sense). Then, if the current index of the input array is not &amp;ldquo;0&amp;rdquo;, you can add to &lt;code&gt;next&lt;/code&gt; the value/number of combinations from one index ago &amp;ndash; &amp;ldquo;0&amp;rdquo; does not add anything because it is the only digit that must combine with the previous digit to count, so does not add any possibilities that rely on the previous digit being an ending digit. At the end of each step of the loop, change the 0 index of the &lt;code&gt;dp&lt;/code&gt; array to the value in the 1 index, and then use &lt;code&gt;next&lt;/code&gt; to update the 1 index. At the end of the loop, return the value in the 1 index.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;261374589&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;261374589&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/decode-ways/&#34;
  
  target=&#34;_blank&#34;
  &gt;Decode Ways&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof s === &amp;#34;string&amp;#34;

const numDecodings =(s)=&amp;gt; {

  if (!s.length || s[0] === &amp;#34;0&amp;#34;) return 0

  const dp = [1, 1]
    
  for (let i = 1; i &amp;lt; s.length; i&amp;#43;&amp;#43;) {
      let next = 0
    if (s[i-1] === &amp;#39;1&amp;#39; || s[i-1] === &amp;#39;2&amp;#39; &amp;amp;&amp;amp; s[i] &amp;lt;= &amp;#39;6&amp;#39;) {
      next &amp;#43;= dp[0]
    }
    if (s[i] !== &amp;#39;0&amp;#39;) {
      next &amp;#43;= dp[1]
    }
    dp[0] = dp[1]
    dp[1] = next
  }

  return dp[1]

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To count the number of ways to sum to a target in an array of unique integers, you can create a &lt;code&gt;dp&lt;/code&gt; array, sort the input array from least to greatest, and fill the &lt;code&gt;dp&lt;/code&gt; array with a single count at every location equal to a value in the input array. Then, you can use dynamic programming to loop to every integer from &lt;code&gt;1&lt;/code&gt; to the target value. At each, declare a &lt;code&gt;count&lt;/code&gt; variable started at 0. Then, for each integer being targeted, loop the sorted input array until the loop runs out or the value at the input array location meets or exceeds the integer being targeted in the outer loop. For each of these inner loop steps, add to the &lt;code&gt;count&lt;/code&gt; any value in &lt;code&gt;dp&lt;/code&gt; at an index equal to the target integer less the value indicated at the inner loop. At the end of the inner loop, set &lt;code&gt;dp&lt;/code&gt; at the index pointed to by the outer array equal to its current value, if any, plus &lt;code&gt;count&lt;/code&gt;. At the end of the looping, return the value at the location of the target in &lt;code&gt;dp&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;657219834&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;657219834&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/combination-sum-iv/&#34;
  
  target=&#34;_blank&#34;
  &gt;Combination Sum IV&lt;/a
&gt;
 &amp;ndash; code slightly modified from &lt;a
  href=&#34;https://leetcode.com/problems/combination-sum-iv/solutions/264274/javascript/&#34;
  
  target=&#34;_blank&#34;
  &gt;gnay&amp;rsquo;s code&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof nums === &amp;#34;object&amp;#34; (array of numbers)
// typeof target === &amp;#34;number&amp;#34;

const combinationSum4 = (nums, target) =&amp;gt; {

    const dp = []
    nums.sort((a,b) =&amp;gt; a-b)
    for (let num of nums){
        dp[num] = 1
    }
    for(let i = 1; i &amp;lt;= target; i&amp;#43;&amp;#43;){
        let count = 0
        for(let j = 0; j &amp;lt; nums.length &amp;amp;&amp;amp; nums[j] &amp;lt; i; j&amp;#43;&amp;#43;){
            count &amp;#43;= dp[i - nums[j]]
        }
        dp[i] = dp[i] &amp;#43; count || count
    }
    return dp[target]

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;h1 id=&#34;binary&#34;&gt;Binary&lt;/h1&gt;
&lt;p&gt;To sum two binary strings, set indices to point to the rightmost (lowest) element in both. Set a &lt;code&gt;carry&lt;/code&gt; variable to 0. Loop until both indices have reached the leftmost end of their strings. At each step in the loop, add the numerical sum of valid index values to &lt;code&gt;carry&lt;/code&gt; (ignoring any index that is less than 0). Next, and also in each loop, push &lt;code&gt;carry%2&lt;/code&gt; to a &lt;code&gt;result&lt;/code&gt; array, and reset &lt;code&gt;carry&lt;/code&gt; to its own base 2 overflow. Once both strings have been looped, add any final value in &lt;code&gt;carry&lt;/code&gt; to the &lt;code&gt;result&lt;/code&gt; array, if a final value exists, then reverse the array and join it into the result string.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;287495316&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;287495316&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/add-binary/&#34;
  
  target=&#34;_blank&#34;
  &gt;Add Binary&lt;/a
&gt;
 &amp;ndash; code adapted from &lt;a
  href=&#34;https://leetcode.com/problems/add-binary/solutions/1679423/well-detailed-explaination-java-c-python-easy-for-mind-to-accept-it/&#34;
  
  target=&#34;_blank&#34;
  &gt;hi-malik&amp;rsquo;s code and explanation&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof a === &amp;#34;string&amp;#34;
// typeof b === &amp;#34;string&amp;#34;

const addBinary = (a, b) =&amp;gt; {
    const result = []
    
    let aIndex = a.length-1
    let bIndex = b.length-1
    let carry = 0

    while(aIndex !== -1 || bIndex !== -1){
        if(bIndex &amp;gt; -1){
            carry &amp;#43;= Number(b[bIndex])
            bIndex--
        }
        if (aIndex &amp;gt; -1){
            carry &amp;#43;= Number(a[aIndex])
            aIndex--
        }

        result.push(carry%2)
        carry = Math.floor(carry/2)
    }

    if (carry) result.push(1)
    
    return result.reverse().join(&amp;#39;&amp;#39;)

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To count the number of 1 in a binary representation of numbers from 0 to some arbitrary number, seed a results array with a 0 (there are 0 of 1 digits in 0). Then loop upwards as far as needed, seeding each next value in the results array with the value stored at half of it, rounded down, plus 1 if the leftmost digit in the new number is filled. Why look at the location half of it, rounded down? Because that is the equivalent of a bitshift right, so we only need to worry afterwards about one additional digit.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;134975268&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;134975268&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/counting-bits/&#34;
  
  target=&#34;_blank&#34;
  &gt;Counting Bits&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof n === &amp;#34;number&amp;#34;

const countBits = (n) =&amp;gt; {

    const results = [0]

    for (let i = 1; i &amp;lt;= n; i&amp;#43;&amp;#43;){
        results[i] = results[i &amp;gt;&amp;gt; 1] &amp;#43; (i &amp;amp; 1)
    } 

    return results
       
}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;A string&amp;rsquo;s &lt;a
  href=&#34;https://en.wikipedia.org/wiki/Hamming_weight&#34;
  
  target=&#34;_blank&#34;
  &gt;Hamming weight&lt;/a
&gt;
 is the number of its nonzeros. In binary, Hamming weight is all of the 1s. From the target number, we can run a loop that increments a counter at every step, and also replaces the original number with the bitwise AND of itself and itself - 1. This guarantees that the number of loop steps while there is a number is the Hamming weight. Why minus 1 on an odd number removes 1 nonzero should be self-explanatory (the bitwise AND here does nothing), while the bitwise AND with an even original interacts with the minus 1 to create the same effect because the subtraction will cascade to be pullled from the lowest avaiable 1 on the original, and all 1s created by the process will be wiped out by the bitwise AND.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;659781324&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;659781324&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/number-of-1-bits/&#34;
  
  target=&#34;_blank&#34;
  &gt;Number of 1 Bits&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof n === &amp;#34;number&amp;#34;

const hammingWeight = (n) =&amp;gt; {

    let weight = 0

    while(n){
        n &amp;amp;= n-1
        weight&amp;#43;&amp;#43;
    }

    return weight

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;If in an array of numbers all numbers appear twice except a single number that appears once, a bitwise XOR trick can detect the lonely number in linear time. By XORing the entire series and returning the result, because XOR will return 0 on each digit for each matched digits, even identical numbers that are not adjacent will eventually cancel each other out.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;347592186&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;347592186&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/single-number/&#34;
  
  target=&#34;_blank&#34;
  &gt;Single Number&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof nums === &amp;#34;object&amp;#34; (array of numbers)

const singleNumber = (nums) =&amp;gt; nums.reduce((a,b)=&amp;gt;a^b)

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find a missing number in an array of numbers that would otherwise include all values between 0 and a given n, XOR all values in the array together with all values from 0 to the n. With the XOR, all doubled values should cancel out, leaving the missing value.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;291745683&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;291745683&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/missing-number/&#34;
  
  target=&#34;_blank&#34;
  &gt;Missing Number&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof nums === &amp;#34;object&amp;#34; (array of numbers)

const missingNumber = (nums) =&amp;gt; {

    let result = nums.length

    for(let i = 0; i &amp;lt; nums.length; i&amp;#43;&amp;#43;){
        result ^= i ^ nums[i]
    }

    return result

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;In a 32-bit unsigned integer, what is the number represented by the reversal of the bits? In order to solve, start a &lt;code&gt;result&lt;/code&gt; variable at 0, then loop all 32 digits of the binary version of the number to be reversed. At each step, double the &lt;code&gt;result&lt;/code&gt; variable (a bitshift up by 1, but doubling with &lt;code&gt;*&lt;/code&gt; in JavaScript as opposed to a direct bitshift prevents negative overflow). Then fill the new ones place on &lt;code&gt;result&lt;/code&gt; with the presence or absence of the lowest digit remaining on the original number. Finally, for each step, bitshift down the number being read by 1.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;482317659&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;482317659&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/reverse-bits/&#34;
  
  target=&#34;_blank&#34;
  &gt;Reverse Bits&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof n === &amp;#34;number&amp;#34;

const reverseBits = (n) =&amp;gt; {

  let result = 0
  let count = 32

  while (count--) {
    result *= 2
    result &amp;#43;= n &amp;amp; 1
    n &amp;gt;&amp;gt;= 1
  }

  return result

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find the single duplicate number in an array of otherwise consecutive numbers starting at 1, you can use a &lt;code&gt;fast&lt;/code&gt; and a &lt;code&gt;slow&lt;/code&gt; pointer. Assign &lt;code&gt;slow&lt;/code&gt; to start as the value of the 0th index (which itself is an index in the array), and &lt;code&gt;fast&lt;/code&gt; to the value at the index equal to the value at the 0th index. Then, continue updating &lt;code&gt;fast&lt;/code&gt; and &lt;code&gt;slow&lt;/code&gt; at this pace until &lt;code&gt;fast&lt;/code&gt; is equal to &lt;code&gt;slow&lt;/code&gt;. This means &lt;code&gt;fast&lt;/code&gt; has caught up to &lt;code&gt;slow&lt;/code&gt; in the circular pattern. To figure out what number is repeated, or, in other words, what number is indicated from two other indicies, restart one of the pointers at 0 and move each pointer to the value indicated at the index in tandem until they point to the same number. This is the result. This math works because to get to the first match, the &lt;code&gt;slow&lt;/code&gt; pointer had to travel to the location indicated by two values, then some further distance, while the &lt;code&gt;fast&lt;/code&gt; pointer had to travel both of these, plus a distance equal to the distance from the start to the location indicated by two values, plus the &amp;ldquo;further&amp;rdquo; distance again. (The &lt;code&gt;fast&lt;/code&gt; must travel twice as far as &lt;code&gt;slow&lt;/code&gt;.) Restarting one pointer to the 0th index and then moving them both forward step by step until they match means that both will meet the location indicated by two values (because both pointers are at the start of the phase that will end at the desired value).&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;261435789&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;261435789&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/find-the-duplicate-number/&#34;
  
  target=&#34;_blank&#34;
  &gt;Find the Duplicate Number&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof nums === &amp;#34;object&amp;#34; (array of numbers)

const findDuplicate = (nums) =&amp;gt; {

    let fast = nums[nums[0]]
    let slow = nums[0]
    while (fast !== slow){
        fast = nums[nums[fast]]
        slow = nums[slow]
    }
     
    fast = 0
    while (fast !== slow) {
        fast = nums[fast]
        slow = nums[slow]
    }
    
    return fast

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;h1 id=&#34;math&#34;&gt;Math&lt;/h1&gt;
&lt;p&gt;One way of converting a Roman numeral to a number involves creating a lookup object that links Roman numeral keys to their numerical values. A separate lookup object indicates which Roman numeral keys can have negative values by coming just before specific Roman numerals of greater value. A &lt;code&gt;sum&lt;/code&gt; variable is created, and then, in a loop reading the full Roman numeral from left to right, the numeral at the index is read into the &lt;code&gt;sum&lt;/code&gt; variable as if it is positive. If the next index is filled and contains a valid greater numeral, add twice the negative of the numeral at the current index to &lt;code&gt;sum&lt;/code&gt; to help reach the final true value.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;831625749&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;831625749&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/roman-to-integer/&#34;
  
  target=&#34;_blank&#34;
  &gt;Roman to Integer&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof s === &amp;#34;string&amp;#34;

const romanToInt = (s) =&amp;gt; {

    const look = {
        &amp;#39;I&amp;#39;: 1,
        &amp;#39;V&amp;#39;: 5,
        &amp;#39;X&amp;#39;: 10,
        &amp;#39;L&amp;#39;: 50,
        &amp;#39;C&amp;#39;: 100,
        &amp;#39;D&amp;#39;: 500,
        &amp;#39;M&amp;#39;: 1000
    }

    const negs = {
        &amp;#39;I&amp;#39;: new Set([&amp;#39;V&amp;#39;,&amp;#39;X&amp;#39;]),
        &amp;#39;X&amp;#39;: new Set([&amp;#39;L&amp;#39;,&amp;#39;C&amp;#39;]),
        &amp;#39;C&amp;#39;: new Set([&amp;#39;D&amp;#39;,&amp;#39;M&amp;#39;])
    }

    let sum = 0

    for (let i = 0; i &amp;lt; s.length; i&amp;#43;&amp;#43;){
        sum &amp;#43;= look[s[i]]

        if(negs[s[i]]?.has(s[i&amp;#43;1])){
           sum -= 2 * look[s[i]] 
        }

    }

    return sum

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;If a number is a palindrome, the reverse of the number is the same as the number. To reverse a number, create a &lt;code&gt;reverse&lt;/code&gt; number set to 0, then loop through each digit of the original number. To do this, at every step, multiply the &lt;code&gt;reverse&lt;/code&gt; by 10, and then add the lowest remaining digit of the original number to &lt;code&gt;reverse&lt;/code&gt;. A way of finding the lowest remaining digit is by creating a copy of the original number, checking the ones place, and then dividing by 10 (rounding down) until the number is gone.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;362197548&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;362197548&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/palindrome-number/&#34;
  
  target=&#34;_blank&#34;
  &gt;Palindrome Number&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof x === &amp;#34;number&amp;#34;

const isPalindrome = (x) =&amp;gt; {

  if (x &amp;lt; 0) return false

  let reverse = 0
  for(let i = x; i &amp;gt; 0; i = Math.floor(i/10)){
      reverse = reverse * 10 &amp;#43; i%10
  }

  return reverse === x

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To pick a random index from an input array, weighted by the integer values in the indicies (indicies with greater values have proportionally more share), you can save a pair of variables to &lt;code&gt;this&lt;/code&gt;: the &lt;code&gt;total&lt;/code&gt; of input array, and a &lt;code&gt;runningTotals&lt;/code&gt; array of equal length to the input array. Fill each index of &lt;code&gt;runningTotals&lt;/code&gt; with the sum of every value from that position to the left in the input array. In the selection method, generate a &lt;code&gt;random&lt;/code&gt; integer from 0 up to but not including the &lt;code&gt;total&lt;/code&gt;, then conduct binary search, with a floored &lt;code&gt;mid&lt;/code&gt; index, moving the &lt;code&gt;left&lt;/code&gt; pointer to &lt;code&gt;mid+1&lt;/code&gt; each time the &lt;code&gt;random&lt;/code&gt; value is greater than the value at &lt;code&gt;mid&lt;/code&gt;.  Else you can move &lt;code&gt;right&lt;/code&gt; to &lt;code&gt;mid&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;912465387&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;912465387&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/random-pick-with-weight/&#34;
  
  target=&#34;_blank&#34;
  &gt;Random Pick with Weight&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof w === &amp;#34;object&amp;#34; (array of numbers)

const Solution = function(w) {
    this.total = 0
    this.runningTotals = []
    for(let value of w) {
        this.total &amp;#43;= value
        this.runningTotals.push(this.total)
    }
}

Solution.prototype.pickIndex = function() {
    const random = Math.random() * this.total
    let left = 0
    let right = this.runningTotals.length-1
    while(left &amp;lt; right){
        const mid = left &amp;#43; Math.floor((right-left)/2)
        if(random &amp;gt; this.runningTotals[mid]){
            left = mid &amp;#43; 1
        } else {
            right = mid
        }
    }
    return left
}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To write a function that raises a number to a power, you can use a recursive algorithm. Notice that any power of 0 returns a 1. Declare a temporary &lt;code&gt;result&lt;/code&gt; as 1 and and an &lt;code&gt;absPower&lt;/code&gt; as the absolute value of the power. If &lt;code&gt;absPower&lt;/code&gt; is odd you can multiply the &lt;code&gt;result&lt;/code&gt; by the number being raised, and reduce the &lt;code&gt;absPower&lt;/code&gt; by 1. Then multiply &lt;code&gt;result&lt;/code&gt; by a recusive call of the function that squares the number being raised and uses half the &lt;code&gt;absPower&lt;/code&gt; as the power. Finally, if the original power was negative, return 1 over the &lt;code&gt;result&lt;/code&gt;, else return the &lt;code&gt;result&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;597623481&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;597623481&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/powx-n/&#34;
  
  target=&#34;_blank&#34;
  &gt;Pow(x, n)&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof x === &amp;#34;number&amp;#34;
// typeof n === &amp;#34;number&amp;#34;

const myPow = (x, n) =&amp;gt; {

    if (n===0) return 1
    
    let result = 1
    let absPower = Math.abs(n)

    if(absPower%2){
        result *= x
        absPower--
    }

    result *= myPow(x**2, absPower/2)
    
    if (n &amp;lt; 0) return 1/result
    return result

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To reverse an integer as if it is a signed 32-bit value, you can start a &lt;code&gt;reverse&lt;/code&gt; variable at 0. Declare a &lt;code&gt;clamp&lt;/code&gt; at 2 to the 31 power. Then, while the input value still exists, multiply reverse by 10, add the value of the 0s place of the input value to &lt;code&gt;reverse&lt;/code&gt;, and check if the &lt;code&gt;reverse&lt;/code&gt; value is less than negative &lt;code&gt;clamp&lt;/code&gt; or greater than &lt;code&gt;clamp-1&lt;/code&gt;. Finally, divide the input value by 10 and remove any decimal. Once the input value no longer exists, return &lt;code&gt;reverse&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;938624751&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;938624751&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/reverse-integer/&#34;
  
  target=&#34;_blank&#34;
  &gt;Reverse Integer&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof x === &amp;#34;number&amp;#34;

const reverse = (x) =&amp;gt; {

  let reverse = 0
  const clamp = Math.pow(2, 31)

  while (x) {
    reverse *= 10
    reverse &amp;#43;= x % 10
    if ( reverse &amp;lt; -clamp || reverse &amp;gt; clamp - 1) return 0
    if (x &amp;gt; 0) x = Math.floor(x/10)
    else x = Math.ceil(x/10)
  }

  return reverse

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;h1 id=&#34;heaps&#34;&gt;Heaps&lt;/h1&gt;
&lt;p&gt;To find the k closest points to the origin, sort the points by the sum of the x distance squared and the y distance squared from the origin. No need to take square root because relative order will be the same either way. Return the k closest points. This algorithm can be done with a priority queue (which could always keep the lowest point on top based on its distance) but this is not a native data struture in JavaScript.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;928473651&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;928473651&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/k-closest-points-to-origin/description/&#34;
  
  target=&#34;_blank&#34;
  &gt;K Closest Points to Origin&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof points === &amp;#34;object&amp;#34; (array of arrays with 2 numbers)
// typeof k === &amp;#34;number&amp;#34;

const kClosest=(points,k)=&amp;gt;points.map(p=&amp;gt;[...p,p[0]**2&amp;#43;p[1]**2]).sort((a,b)=&amp;gt;a[2]-b[2]).slice(0,k).map(p=&amp;gt;[p[0],p[1]])

&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;If there are some number of tasks, assigned letters that can be duplicates, how long will it take to complete all tasks if tasks assigned the same letter must be separated by some &lt;code&gt;cooldown&lt;/code&gt; number of spaces? To solve, you can fill a &lt;code&gt;letterCounts&lt;/code&gt; array, sort this from greatest to least, and note the &lt;code&gt;countOfMaximumSizeTypes&lt;/code&gt; (in other words, how many letters are in first place for maximum count). The remainder of the algorithm is just math. Note that the count of the most commmon letter minus 1 represents the number of full cooldown rounds that must take place, where the size of a cooldown round is &lt;code&gt;cooldown+1&lt;/code&gt; (because the item being cooled down must be included. Added to the product, because the last instance of the most common letter must also be included in the time, is simply the &lt;code&gt;countOfMaximumSizeTypes&lt;/code&gt;, which represents a final partial cooldown round. However, if the number of tasks is greater than the product plus sum, simply return the number of tasks.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;943785162&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;943785162&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/task-scheduler/&#34;
  
  target=&#34;_blank&#34;
  &gt;Task Scheduler&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof tasks === &amp;#34;object&amp;#34; (array of 1 character strings)
// typeof cooldown === &amp;#34;number&amp;#34;

const leastInterval = (tasks, cooldown) =&amp;gt; {

  const letterCounts = new Array(26).fill(0)
  for (let task of tasks) {
    letterCounts[task.charCodeAt(0) - 65]&amp;#43;&amp;#43;
  }
  letterCounts.sort((a,b)=&amp;gt;b-a)
  const countOfMaximumSizeTypes = letterCounts.filter((value ,index, array)=&amp;gt;value===array[0]).length

  return Math.max((letterCounts[0] - 1) * (cooldown &amp;#43; 1) &amp;#43; countOfMaximumSizeTypes, tasks.length)
}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find some number of most frequent strings in an array, sorted by frequency from most common and then by lexicographical order, you can start by creating a &lt;code&gt;counts&lt;/code&gt; object that stores the count of a given word. Then you can sort the unique words first by their count from greatest to smallest, and then as a tiebreaker, by their lexicographical order (the default &lt;code&gt;sort&lt;/code&gt;). Then return however many from the front of this list is desired.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;135274698&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;135274698&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/top-k-frequent-words/&#34;
  
  target=&#34;_blank&#34;
  &gt;Top K Frequent Words&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof words === &amp;#34;object&amp;#34; (array of strings)
// typeof k === &amp;#34;number&amp;#34;

const topKFrequent = (words, k) =&amp;gt; {
    
    const counts = {}
    for (let word of words) {
        counts[word] = counts[word] &amp;#43; 1 || 1
    }

    return Object.keys(counts).sort((a,b) =&amp;gt; { 
        return counts[b] === counts[a] ? ( a &amp;gt; b || -1 ) : counts[b] - counts[a]
    }).slice(0, k)

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find the &lt;code&gt;k&lt;/code&gt; closest elements to a given value in a sorted array of numbers, with the smaller range being chosen for a tiebreaker, one solution method is binary search. Create &lt;code&gt;left&lt;/code&gt; and &lt;code&gt;right&lt;/code&gt; index pointers with &lt;code&gt;left&lt;/code&gt; being &lt;code&gt;k-1&lt;/code&gt; and &lt;code&gt;right&lt;/code&gt; being the rightmost index of the array. Then, while &lt;code&gt;left&lt;/code&gt; is less than &lt;code&gt;right&lt;/code&gt;, create a floored &lt;code&gt;mid&lt;/code&gt;, and check if the target value minus the value at &lt;code&gt;mid-k+1&lt;/code&gt; is greater than the value at &lt;code&gt;mid+1&lt;/code&gt; minus the target value. Because we are only interested in &lt;code&gt;k&lt;/code&gt; values, not &lt;code&gt;k&lt;/code&gt;, and given that the values are sorted, the result must be adjacent numbers, one of the two compared numbers must not be included in the result. We can set &lt;code&gt;left&lt;/code&gt; to &lt;code&gt;mid+1&lt;/code&gt; if the test passes because that means the value at the low end has a greater &amp;ldquo;delta&amp;rdquo; with the target value, else we can set &lt;code&gt;right&lt;/code&gt; to &lt;code&gt;mid&lt;/code&gt;, which meets the condition of the tiebreaker. When the binary search resolves, return the &lt;code&gt;k&lt;/code&gt; points ending at &lt;code&gt;mid&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;719865324&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;719865324&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/find-k-closest-elements/&#34;
  
  target=&#34;_blank&#34;
  &gt;Find K Closest Elements&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof arr === &amp;#34;object&amp;#34; (array of numbers)
// typeof k === &amp;#34;number&amp;#34;
// typeof x === &amp;#34;number&amp;#34;

const findClosestElements = (arr, k, x) =&amp;gt; {
    
  let left = k - 1
  let right = arr.length - 1

  while (left &amp;lt; right) {
    let mid = left &amp;#43; Math.floor((right-left)/2)
    if (x - arr[mid-k&amp;#43;1] &amp;gt; arr[mid&amp;#43;1] - x) {
      left = mid &amp;#43; 1
    } else {
      right = mid
    }
  }
  return arr.slice(left - k &amp;#43; 1, left &amp;#43; 1)

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;*** Kth Largest Element in an Array goes here ***&lt;/p&gt;
&lt;p&gt;To find the median from a data stream, you can assign a function to a variable and within the function set an &lt;code&gt;array&lt;/code&gt; under &lt;code&gt;.this&lt;/code&gt;. To add a number, binary search can be conducted to find the insertion point. You can use binary search until the &lt;code&gt;left&lt;/code&gt; and &lt;code&gt;right&lt;/code&gt; pointers cross, and have a condition of the floored &lt;code&gt;mid&lt;/code&gt; value being less than the number to insert leading &lt;code&gt;left&lt;/code&gt; being set to &lt;code&gt;mid+1&lt;/code&gt;, else setting &lt;code&gt;right&lt;/code&gt; to &lt;code&gt;mid-1&lt;/code&gt;. Setting &amp;ldquo;past&amp;rdquo; the mid from both directions is required when the binary search does not resolve until the pointers cross. Finally for insert, you can insert the number with a splice. To actually find a number, if the length of the &lt;code&gt;array&lt;/code&gt; is odd yuu can simply return the floored &lt;code&gt;mid&lt;/code&gt;. Else, sum the value at &lt;code&gt;mid&lt;/code&gt; and &lt;code&gt;mid-1&lt;/code&gt; and return that value divided by 2.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;357921648&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;357921648&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/find-median-from-data-stream/&#34;
  
  target=&#34;_blank&#34;
  &gt;Find Median from Data Stream&lt;/a
&gt;
 &amp;ndash; code adapted from &lt;a
  href=&#34;https://leetcode.com/problems/find-median-from-data-stream/solutions/122701/javascript-solution-using-binary-search/&#34;
  
  target=&#34;_blank&#34;
  &gt;NinjaCod3r&amp;rsquo;s code&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

const MedianFinder = function() {
    this.array = []
}

// typeof num === &amp;#34;number&amp;#34;
MedianFinder.prototype.addNum = function(num) {
   
    let left = 0
    let right = this.array.length-1
    
    while(left &amp;lt;= right){
            const mid = left &amp;#43; Math.floor((right - left)/2)
            
            if(this.array[mid] &amp;lt; num) left = mid &amp;#43; 1
            else right = mid - 1
    }
    
    this.array.splice(left, 0, num)
    
}

MedianFinder.prototype.findMedian = function() {

    const mid = Math.floor(this.array.length/2)

    if(this.array.length%2) return this.array[mid]
    return (this.array[mid] &amp;#43; this.array[mid-1])/ 2

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To merge a number of ascending-sorted linked lists into a list that is still sorted, all values can be put into a data structure and then removed in a sorted way and put into a new linked list. A priority queue organizing nodes by value or a heap organizing values can do this by making sure the node or value of the greatest amount is always on top, but because neither is a native JavaScript data structure, a sort can be used after pushing all values to an array.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;576821493&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;576821493&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/merge-k-sorted-lists/&#34;
  
  target=&#34;_blank&#34;
  &gt;Merge k Sorted Lists&lt;/a
&gt;
 &amp;ndash; compare also  &lt;a
  href=&#34;https://leetcode.com/problems/merge-k-sorted-lists/solutions/484197/javascript/&#34;
  
  target=&#34;_blank&#34;
  &gt;282295491&amp;rsquo;s code&lt;/a
&gt;
 and &lt;a
  href=&#34;https://leetcode.com/problems/merge-k-sorted-lists/solutions/2211516/javascript-beats-90/&#34;
  
  target=&#34;_blank&#34;
  &gt;user6506l&amp;rsquo;s code&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// type of lists === &amp;#34;object&amp;#34; (array of linked lists with next and val properties)

const mergeKLists = (lists) =&amp;gt; {

  const sortable = []
  for (let node of lists){
      while(typeof node?.val === &amp;#34;number&amp;#34;){
          sortable.push(node.val)
          node = node.next
      }
  }
  sortable.sort((a,b) =&amp;gt; a-b)
  const link = new ListNode()
  let pointer = link
  for (let value of sortable){
      pointer.next = new ListNode(value)
      pointer = pointer.next
  }
  return link.next

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;h1 id=&#34;tries&#34;&gt;Tries&lt;/h1&gt;
&lt;p&gt;A trie represents words with as much overlapping prefixes as possible. To insert a word, start with an object, and for every letter of the word, set the letter&amp;rsquo;s key in the outer object (if never seen there before) to a new object, and then go into that object. When the word is over, in the object of the final letter, add a &lt;code&gt;.end&lt;/code&gt; key. Searching is similar. Descend from the top object without being able to insert anything on misses and return true only if a &lt;code&gt;.end&lt;/code&gt; key is found. Checking if a prefix (&lt;code&gt;startsWith&lt;/code&gt;) is available is a relaxed search from the top that doesn&amp;rsquo;t care about &lt;code&gt;.end&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;279851436&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;279851436&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/implement-trie-prefix-tree/description/&#34;
  
  target=&#34;_blank&#34;
  &gt;Implement Trie (Prefix Tree)&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof word === &amp;#34;string&amp;#34;
// typeof prefix === &amp;#34;string&amp;#34;

const Trie = function() {
    this.trie = {}
}

Trie.prototype.insert = function(word) {
    let trie = this.trie
    for (let letter of word) {
        !trie[letter] &amp;amp;&amp;amp; ( trie[letter] = {} )
        trie = trie[letter]
    }
    trie.end = true
}

Trie.prototype.search = function(word) {
    let trie = this.trie
    for (let letter of word) {
        if (!trie[letter]) return false
        trie = trie[letter]
    }
    return !!trie.end
}

Trie.prototype.startsWith = function(prefix) {
    let trie = this.trie
    for (let letter of prefix) {
        if (!trie[letter]) return false
        trie = trie[letter]
    }
    return true
}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;How to determine if a string is composed of any combination (including repeats) of words in a &lt;code&gt;wordDict&lt;/code&gt;? Create a &lt;code&gt;cache&lt;/code&gt; map, and then run a helper function &lt;code&gt;go&lt;/code&gt; which takes as an argument the string to be tested. If the string is empty, return &lt;code&gt;true&lt;/code&gt;, and if the string in &lt;code&gt;go&lt;/code&gt; is in the &lt;code&gt;cache&lt;/code&gt;, return whatever boolean the cache has. Else, slice at every location in the string argument starting with the 1st index. When the string up to the index is in the &lt;code&gt;wordDict&lt;/code&gt; and the remainder of the string returns &lt;code&gt;true&lt;/code&gt; from &lt;code&gt;go&lt;/code&gt;, set the &lt;code&gt;cache&lt;/code&gt; for the string to &lt;code&gt;true&lt;/code&gt; and return &lt;code&gt;true&lt;/code&gt;. Else set the &lt;code&gt;cache&lt;/code&gt; for the string to &lt;code&gt;false&lt;/code&gt; and return &lt;code&gt;false&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;671984532&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;671984532&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/word-break/&#34;
  
  target=&#34;_blank&#34;
  &gt;Word Break&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof s === &amp;#34;string&amp;#34;
// typeof wordDict === &amp;#34;object&amp;#34; (array of strings)

const wordBreak = (s, wordDict) =&amp;gt; {

    const cache = new Map()

    const go = (s) =&amp;gt; {
        if (cache.has(s)) return cache.get(s)
        if (!s) return true
        for (let i = 1; i &amp;lt;= s.length; i&amp;#43;&amp;#43;) {
            if (
                wordDict.includes(s.slice(0,i)) &amp;amp;&amp;amp;
                go(s.slice(i))
            ){
                cache.set(s, true)
                return true
            }
        }
        cache.set(s, false)
        return false
    }

    return go(s)

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To create a trie that can search for words that include &lt;code&gt;.&lt;/code&gt; characters that can match any letter, add words as in &lt;a
  href=&#34;https://leetcode.com/problems/implement-trie-prefix-tree/description/&#34;
  
  target=&#34;_blank&#34;
  &gt;Implement Trie (Prefix Tree)&lt;/a
&gt;
 but modify the search. At the length of a detected word plus 1, as before return the boolean value of if there is a &lt;code&gt;.end&lt;/code&gt;, but wrap this as a depth-first recursive search in a helper function. After the &lt;code&gt;.length&lt;/code&gt; check, proceed down a level as normal if the character at that location in the input word is not a &lt;code&gt;.&lt;/code&gt;, otherwise loop the characters at the trie location and recursively call &lt;code&gt;go&lt;/code&gt; for every character, returning &lt;code&gt;true&lt;/code&gt; if any return &lt;code&gt;true&lt;/code&gt;, and making sure to gracefully fail if &lt;code&gt;end&lt;/code&gt; becomes an argument for &lt;code&gt;go&lt;/code&gt;. Return &lt;code&gt;false&lt;/code&gt; if the pattern is never found.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;914326587&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;914326587&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/design-add-and-search-words-data-structure/&#34;
  
  target=&#34;_blank&#34;
  &gt;Design Add and Search Words Data Structure&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

const WordDictionary = function() {
    this.trie = {}
}

WordDictionary.prototype.addWord = function(word) {
    let trie = this.trie
    for (let letter of word) {
        !trie[letter] &amp;amp;&amp;amp; ( trie[letter] = {} )
        trie = trie[letter]
    }
    trie.end = true
}

WordDictionary.prototype.search = function(word) {
    
    const go = (trie, depth) =&amp;gt; {
        if (word.length === depth) return !!trie.end
        if (word[depth] !== &amp;#39;.&amp;#39;){
            return !!(trie[word[depth]] &amp;amp;&amp;amp; go(trie[word[depth]], depth&amp;#43;1))
        } else {
            const characterSteps = Object.keys(trie)
            for (let characterStep of characterSteps){
                if (go(trie[characterStep], depth&amp;#43;1)) return true
            }
            return false
        }
    }
    
    return go(this.trie, 0)

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;h1 id=&#34;recursion&#34;&gt;Recursion&lt;/h1&gt;
&lt;p&gt;To find all possible permutations of an array of integers, you can use a helper function &lt;code&gt;go&lt;/code&gt;. This takes two arguments: &lt;code&gt;remainingUnusedNumbers&lt;/code&gt;,which starts as the input array, and &lt;code&gt;buildingPermutation&lt;/code&gt;, which starts as an empty array. If &lt;code&gt;remainingUnusedNumbers&lt;/code&gt; is empty, the recursive base case is reached &amp;ndash; push &lt;code&gt;buildingPermutation&lt;/code&gt; to a &lt;code&gt;results&lt;/code&gt; array outside &lt;code&gt;go&lt;/code&gt; and return. Else loop &lt;code&gt;remainingUnusedNumbers&lt;/code&gt;, at every step generating a new &lt;code&gt;go&lt;/code&gt; function slicing out the highlighted number and adding that number to a consistant end of the &lt;code&gt;buildingPermutation&lt;/code&gt; array.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;986351742&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;986351742&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/permutations/&#34;
  
  target=&#34;_blank&#34;
  &gt;Permutations&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof nums === &amp;#34;object&amp;#34; (array of numbers)

const permute = (nums) =&amp;gt; {

    const results = []
    
    const go = (remainingUnusedNumbers,buildingPermutation) =&amp;gt; {
        if(!remainingUnusedNumbers.length){
            results.push(buildingPermutation)
            return
        }
        for (let i = 0; i &amp;lt; remainingUnusedNumbers.length; i&amp;#43;&amp;#43;){
            go(remainingUnusedNumbers.slice(0,i).concat(remainingUnusedNumbers.slice(i&amp;#43;1)),[remainingUnusedNumbers[i],...buildingPermutation])
        }
    }
    
    go(nums,[])
    return results

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;How to find all subsets of an array? Create an array &lt;code&gt;results&lt;/code&gt; and fill it with a single empty array. Then loop the array you wish to find all subsets for. For each value, create an empty &lt;code&gt;next&lt;/code&gt; array, and loop the contents of &lt;code&gt;results&lt;/code&gt;. The &lt;code&gt;results&lt;/code&gt; array should have every possible subset up to but not including the currently reviewed element. Therefore, push the concatenation of every array in &lt;code&gt;results&lt;/code&gt; with the currently reviewed element into &lt;code&gt;next&lt;/code&gt;, and when the inner loop is done, concatenate &lt;code&gt;results&lt;/code&gt; and &lt;code&gt;next&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;518936427&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;518936427&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/subsets/&#34;
  
  target=&#34;_blank&#34;
  &gt;Subsets&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof nums === &amp;#34;object&amp;#34; (array of numbers)

const subsets = (nums) =&amp;gt; {

    let results = [[]]

    for(let num of nums){
        const next = []
        for(let array of results){
            next.push(array.concat(num))
        }
        results = results.concat(next)
    }

    return results

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To find all letter combinations of a phone number, you can return an empty array if there is no number, else seed a new &lt;code&gt;results&lt;/code&gt; array with an empty string. Create a &lt;code&gt;letterLookup&lt;/code&gt; mapping digits to the letters they might represent, then loop through each &lt;code&gt;digit&lt;/code&gt; of the input number. For each digit, create a &lt;code&gt;next&lt;/code&gt; array, and loop through each &lt;code&gt;possibleLetterCombination&lt;/code&gt; stored in &lt;code&gt;results&lt;/code&gt;. Within each &lt;code&gt;possibleLetterCombination&lt;/code&gt;, for each &lt;code&gt;newLetter&lt;/code&gt; returned by the &lt;code&gt;letterLookup&lt;/code&gt; for the &lt;code&gt;digit&lt;/code&gt;, push &lt;code&gt;possibleLetterCombination + newLetter&lt;/code&gt; to &lt;code&gt;next&lt;/code&gt;. When the &lt;code&gt;results&lt;/code&gt; array is looped through (one of every valid &lt;code&gt;newLetter&lt;/code&gt; has been added to every &lt;code&gt;possibleLetterCombination&lt;/code&gt; in &lt;code&gt;results&lt;/code&gt;, so &lt;code&gt;next&lt;/code&gt; should have about three times more combinations than &lt;code&gt;results&lt;/code&gt;), replace &lt;code&gt;results&lt;/code&gt; with &lt;code&gt;next&lt;/code&gt;. When all &lt;code&gt;digits&lt;/code&gt; are exhausted &lt;code&gt;results&lt;/code&gt; should contain the answer.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;913852674&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;913852674&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/letter-combinations-of-a-phone-number/description/&#34;
  
  target=&#34;_blank&#34;
  &gt;Letter Combinations of a Phone Number&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof digits === &amp;#34;string&amp;#34;

const letterCombinations = (digits) =&amp;gt; {

    if (!digits) return []
    let results = [&amp;#34;&amp;#34;]
    const letterLookup = {
        &amp;#34;2&amp;#34;:&amp;#34;abc&amp;#34;,
        &amp;#34;3&amp;#34;:&amp;#34;def&amp;#34;,
        &amp;#34;4&amp;#34;:&amp;#34;ghi&amp;#34;,
        &amp;#34;5&amp;#34;:&amp;#34;jkl&amp;#34;,
        &amp;#34;6&amp;#34;:&amp;#34;mno&amp;#34;,
        &amp;#34;7&amp;#34;:&amp;#34;pqrs&amp;#34;,
        &amp;#34;8&amp;#34;:&amp;#34;tuv&amp;#34;,
        &amp;#34;9&amp;#34;:&amp;#34;wxyz&amp;#34;
    }
    for (let digit of digits){
        const letters = letterLookup[digit]
        const next = []
        for (let possibleLetterCombination of results){
            for (let newLetter of letters){
                next.push(possibleLetterCombination &amp;#43; newLetter)
            }
        }
        results = next
    }

    return results

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;How to modify an array of integers such that the number increases to its next greatest permutation, or moves to the lowest permutation if it is on the highest? Note that the &lt;code&gt;upperReplacedDigit&lt;/code&gt; that needs to move is the first number from left to right that is lower than a number to its right. If there is no such number, the permutation is at a maximum and the next permutation is simply the reverse. Otherwise, start again from the right (the lowest digit) and find the first number that is greater than the &lt;code&gt;upperReplacedDigit&lt;/code&gt; but a lower digit. If no such number is found, again, this a sign the array is at a maximum and the next permutation is the reverse. Otherwise, swap the &lt;code&gt;upperReplacedDigit&lt;/code&gt; with its &lt;code&gt;update&lt;/code&gt;, and then reverse every digit lower than the index of the original &lt;code&gt;upperReplacedDigit&lt;/code&gt;. This is because those numbers were in increasing order and we want to move them to their lowest-value permutation.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;935724681&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;935724681&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/next-permutation/&#34;
  
  target=&#34;_blank&#34;
  &gt;Next Permutation&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof nums === &amp;#34;object&amp;#34; (array of numbers)

const nextPermutation = (nums) =&amp;gt; {

    let upperReplacedDigit = nums.length - 1
    while(upperReplacedDigit &amp;amp;&amp;amp; nums[upperReplacedDigit] &amp;lt;= nums[--upperReplacedDigit]){}
    
    let update = nums.length - 1
    while(nums[update] &amp;lt;= nums[upperReplacedDigit]){
        if(update === upperReplacedDigit) return nums.reverse()
        update--
    }

    [nums[upperReplacedDigit], nums[update]] = [nums[update], nums[upperReplacedDigit]]
    const reverse = nums.splice(upperReplacedDigit&amp;#43;1).reverse()
    nums.splice(upperReplacedDigit&amp;#43;1,0,...reverse)
}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To generate all strings of valid parentheses off of an input number, recursion can be used. One method involves running a helper function that keeps track of &lt;code&gt;openCount&lt;/code&gt; and &lt;code&gt;closeCount&lt;/code&gt; (both starting at 0), as well as a &lt;code&gt;builder&lt;/code&gt; string (starting as empty). At each step in the helper function, immediately return if &lt;code&gt;openCount&lt;/code&gt; is ever greater than the input number, and if both the &lt;code&gt;openCount&lt;/code&gt; and &lt;code&gt;closeCount&lt;/code&gt; are equal to the input number, push &lt;code&gt;builder&lt;/code&gt; to &lt;code&gt;results&lt;/code&gt; and return. Else run a recursive call of the helper function with one new &lt;code&gt;(&lt;/code&gt; (noting this in the &lt;code&gt;openCount&lt;/code&gt; and &lt;code&gt;builder&lt;/code&gt; arguments), and if &lt;code&gt;closeCount&lt;/code&gt; is less than &lt;code&gt;openCount&lt;/code&gt;, run a recursive call of the helper function with one new &lt;code&gt;)&lt;/code&gt; (noted by &lt;code&gt;closeCount&lt;/code&gt; and &lt;code&gt;builder&lt;/code&gt;).&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;326457891&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;326457891&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/generate-parentheses/&#34;
  
  target=&#34;_blank&#34;
  &gt;Generate Parentheses&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof n === &amp;#34;number&amp;#34;

const generateParenthesis = (n) =&amp;gt; {

    const results = []

    const go = (openCount, closeCount, builder) =&amp;gt; {
        if (n &amp;lt; openCount) return
        if (n === openCount &amp;amp;&amp;amp; openCount === closeCount) return results.push(builder)
        go(openCount &amp;#43; 1, closeCount, builder &amp;#43; &amp;#34;(&amp;#34;)
        if (closeCount &amp;lt; openCount) go(openCount, closeCount &amp;#43; 1, builder &amp;#43; &amp;#34;)&amp;#34;)
    }

    go(0, 0, &amp;#34;&amp;#34;)

    return results

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;h1 id=&#34;matricies&#34;&gt;Matricies&lt;/h1&gt;
&lt;p&gt;How to return elements of a matrix in spiral order? (The top row left-to-right, continued by the left side going down, continued by the bottom row right-to-left, continued by the right side going up, continued by the second-to-top row left-to-right, etc.) If the matrix is empty, return an empty matrix. Then, create a &lt;code&gt;result&lt;/code&gt; matrix, and start four pointers, &lt;code&gt;left&lt;/code&gt;, &lt;code&gt;right&lt;/code&gt;, &lt;code&gt;top&lt;/code&gt;, and &lt;code&gt;bottom&lt;/code&gt;, which indicate the edges of the matrix, inclusive, that still need to be investigated. While the size of the matrix has not been fully explored, step across, down, back, and up the array by using the pointers, pushing to &lt;code&gt;result&lt;/code&gt;. Pull each pointer one step closer to the middle of the array after its fourth of the while loop is complete. Be careful not to move pointers in quarters where no elements are left to be pushed.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;496318275&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;496318275&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/spiral-matrix/&#34;
  
  target=&#34;_blank&#34;
  &gt;Spiral Matrix&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof matrix === &amp;#34;object&amp;#34; (array of arrays of numbers)

const spiralOrder = (matrix) =&amp;gt; {
    if (!matrix.length || !matrix[0].length) return [];
    
    const result = []
    let left = 0
    let right = matrix[0].length - 1
    let top = 0
    let bottom = matrix.length - 1
    let count = matrix.length * matrix[0].length
    
    while (true) {
        for (let i = left; i &amp;lt;= right; i&amp;#43;&amp;#43;) {
            result.push(matrix[top][i])
            count--
        }
        top&amp;#43;&amp;#43;

        for (let i = top; i &amp;lt;= bottom; i&amp;#43;&amp;#43;) {
            result.push(matrix[i][right])
            count--
        }
        if(!count) break
        right--

        for (let i = right; i &amp;gt;= left; i--) {
            result.push(matrix[bottom][i])
            count--
        }
        bottom--

        for (let i = bottom; i &amp;gt;= top; i--) {
            result.push(matrix[i][left])
            count--
        }
        if(!count) break      
        left&amp;#43;&amp;#43;
    }
    
    return result

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To determine is a Sudoku board is valid, you can loop the board, filling a &lt;code&gt;seen&lt;/code&gt; set as you go. For every location with a number, check and then add three strings to the set: a &lt;code&gt;row&lt;/code&gt; location with the &lt;code&gt;value&lt;/code&gt;, a &lt;code&gt;column&lt;/code&gt; location with the &lt;code&gt;value&lt;/code&gt;, and a 3x3 &lt;code&gt;box&lt;/code&gt; location to the &lt;code&gt;value&lt;/code&gt;. If any duplicate is found, immmediately return &lt;code&gt;false&lt;/code&gt;, else you can return &lt;code&gt;true&lt;/code&gt;.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;165842973&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;165842973&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/valid-sudoku/&#34;
  
  target=&#34;_blank&#34;
  &gt;Valid Sudoku&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof board === &amp;#34;object&amp;#34; (array of arrays of 1 character strings)

const isValidSudoku = (board) =&amp;gt; {

    const seen = new Set()

    for (let i = 0; i &amp;lt; 9; i&amp;#43;&amp;#43;) {
      for (let j = 0; j &amp;lt; 9; j&amp;#43;&amp;#43;) {
        const value = board[i][j]
        if(value != &amp;#39;.&amp;#39;) {
            const row = `row ${i} ${value}`
            const column = `column ${j} ${value}`
            const box = `box ${Math.floor(i/3)} ${Math.floor(j/3)} ${value}`
        
            if(seen.has(row) || seen.has(column) || seen.has(box)) return false

            seen.add(row)
            seen.add(column)
            seen.add(box)

          }
      }
    }
    return true

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To rotate a 2D square array in-place, 90 degrees to the right, you can notice that points can be moved in sets of four, which means that once a &amp;ldquo;triangle-like&amp;rdquo; quarter of the array starts cycles from all its points, the transformation is complete. To find the &amp;ldquo;triangle-like&amp;rdquo; array, create and inner an outer loop. The outer loop descends through every row of the input matrix (in a standard way), while the inner loop crosses rows by starting at the column index, and ending at 1 less than the column index, minus an additional value equal to the row. To understand why the inner loop starts and ends where it does, consider that the inner loop must end at least one step before the end of row because otherwise there would not be room for the start of the next clockwise side to start at a corner. The &lt;code&gt;-1&lt;/code&gt; to both sides for each step into the matrix then makes sense because each step into the matrix removes the leftmost and the rightmost columns. Finally, for the rotation itself, save the value you find at the given location. Move into this position the value found at the &lt;code&gt;[sideLength-1-column][row]&lt;/code&gt; of itself, and continue this pattern all four-step cycles are completed starting from every place the &amp;ldquo;triangle&amp;rdquo; has. Used the saved value to fill the last of the four positions.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;483512679&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;483512679&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/rotate-image/&#34;
  
  target=&#34;_blank&#34;
  &gt;Rotate Image&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof matrix === &amp;#34;object&amp;#34; (array of arrays of numbers)

const rotate = (matrix) =&amp;gt; {

    const sideLength = matrix.length
    for(let row = 0; row &amp;lt; sideLength; row&amp;#43;&amp;#43;){ 
        for(let column = row; column &amp;lt; sideLength - 1 - row; column&amp;#43;&amp;#43;){

            const startValue = matrix[row][column]

            matrix[row][column] = matrix[sideLength-1-column][row]
            matrix[sideLength-1-column][row] = matrix[sideLength-1-row][sideLength-1-column]
            matrix[sideLength-1-row][sideLength-1-column] = matrix[column][sideLength-1-row]
            matrix[column][sideLength-1-row] = startValue

        }  
    }
    
    return matrix

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;p&gt;To set 0 to all rows and columns in a matrix that contain at least one 0, in-place, parse through every row or column and if a 0 is noted, mark every nonzero element in the row or column with an intermediate value like a &amp;ldquo;c&amp;rdquo;. Then parse though whichever you did not first check, row or column, and if a 0 is noted, mark every nonzero element in the row or column with a 0. Then go through the matrix one final time and update every intermediate value to a 0 (it can no longer confuse the second scan).&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;531278496&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;531278496&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/rotate-image/&#34;
  
  target=&#34;_blank&#34;
  &gt;Rotate Image&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;
// typeof matrix === &amp;#34;object&amp;#34; (array of arrays of numbers)

const setZeroes = (matrix) =&amp;gt; {
    for (let i = 0; i &amp;lt; matrix.length; i&amp;#43;&amp;#43;){
        let clear = false
        for(let j = 0; j &amp;lt; matrix[0].length; j&amp;#43;&amp;#43;){
            if (!matrix[i][j]){
                clear = true
                break
            }
        }
        if (!clear) continue
        for(let j = 0; j &amp;lt; matrix[0].length; j&amp;#43;&amp;#43;){
           if(typeof matrix[i][j] === &amp;#34;number&amp;#34; &amp;amp;&amp;amp; matrix[i][j]) matrix[i][j] = &amp;#34;c&amp;#34; 
        }
    }
    
    for (let j = 0; j &amp;lt; matrix[0].length; j&amp;#43;&amp;#43;){
        let clear = false
        for(let i = 0; i &amp;lt; matrix.length; i&amp;#43;&amp;#43;){
            if (!matrix[i][j]){
                clear = true
                break
            }        
        }
        if (!clear) continue
        for(let i = 0; i &amp;lt; matrix.length; i&amp;#43;&amp;#43;){
           matrix[i][j] = 0
        }
    }
    
    
    for (let i = 0; i &amp;lt; matrix.length; i&amp;#43;&amp;#43;){
        for(let j = 0; j &amp;lt; matrix[0].length; j&amp;#43;&amp;#43;){
            if(matrix[i][j] === &amp;#34;c&amp;#34;) matrix[i][j] = 0
        }
    }
    
    return matrix

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


&lt;h1 id=&#34;queues&#34;&gt;Queues&lt;/h1&gt;
&lt;p&gt;To design a hit counter that counts hits in the last 300 seconds, you can assign a function to a variable that contains a &lt;code&gt;map&lt;/code&gt; under &lt;code&gt;this&lt;/code&gt;. To add a hit, add a count to the second provided in the &lt;code&gt;map&lt;/code&gt;, creating a new entry if that time has not before been seen. To look up hits, check 300 values starting at the second being checked and working backwards.&lt;/p&gt;



  &lt;div class=&#34;collapsable-code&#34;&gt;
    &lt;input id=&#34;589136247&#34; type=&#34;checkbox&#34;  /&gt;
    &lt;label for=&#34;589136247&#34;&gt;
      &lt;span class=&#34;collapsable-code__language&#34;&gt;javascript&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__title&#34;&gt;&lt;a
  href=&#34;https://leetcode.com/problems/design-hit-counter/&#34;
  
  target=&#34;_blank&#34;
  &gt;Design Hit Counter&lt;/a
&gt;&lt;/span&gt;
      &lt;span class=&#34;collapsable-code__toggle&#34; data-label-expand=&#34;Show&#34; data-label-collapse=&#34;Hide&#34;&gt;&lt;/span&gt;
    &lt;/label&gt;
    &lt;pre class=&#34;language-javascript&#34; &gt;&lt;code&gt;

const HitCounter = function () {
  this.map = new Map()
}

HitCounter.prototype.hit = function (timestamp) {
  if (!this.map.has(timestamp)){
    this.map.set(timestamp,1)
  } else {
    this.map.set(timestamp, this.map.get(timestamp)&amp;#43;1)
  }
}

HitCounter.prototype.getHits = function (timestamp) {
  let result = 0
  for (let i = 0; i &amp;lt; 300; i&amp;#43;&amp;#43;) {
    if (this.map.has(timestamp-i)){
      result &amp;#43;= this.map.get(timestamp-i)
    }
  }
  return result

}&lt;/code&gt;&lt;/pre&gt;
  &lt;/div&gt;


</content>
    </item>
    
  </channel>
</rss>
